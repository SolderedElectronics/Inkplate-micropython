# Inkplate-micropython: C/I2S/3-bit Grayscale Refactor

## Context

Repo is currently pure Python, distributed via `mip install` on top of two prebuilt firmware `.bin` files whose build source doesn't exist anywhere accessible — confirmed via exhaustive search (zero `.c`/`.h` files, no CMake/Makefile/manifest.py, no CI, no tests). All "performance-critical" code today is `@micropython.viper`-decorated Python doing raw ESP32 GPIO register bit-banging to drive the e-paper parallel bus, duplicated near-identically across 4 board packages (Inkplate6, 10, 5v2, 6FLICK — ~1700-2000 lines each, ~90% identical). Grayscale is 2-bit/4-level; full 3-bit/8-level waveform tables from the Arduino reference driver already sit in code as unused comments.

Goal: move the hot path into C, drive the panel via ESP32 I2S+DMA instead of bit-banged GPIO (classic ESP32 I2S0 supports parallel LCD-style output — same technique as the open-source `vroland/epdiy` project, useful prior art), upgrade grayscale to 3-bit/8-level, and consolidate the 4 duplicated board packages into one C driver + thin per-board Python config. User will build the MicroPython+ESP-IDF firmware from scratch (no existing build source to reuse) and has real hardware for most/all variants to verify each step. Work must be small, independently-testable tasks — no big-bang rewrites.

Confirmed in-repo facts anchoring this plan (spot-checked directly, not just from exploration agents):
- `inkplate10.py:40-42` — full Arduino `WAVEFORM3BIT[8][8]` table already transcribed as a comment, unused.
- `inkplate10.py:186-187` — `_tps65186_read` is missing `return`, silently discards the read value (`cls._temperature` always becomes `None`). Duplicated in sibling board files. Real bug, fix during consolidation.
- Pin/control-line layout (`EPD_DATA=0x0E8C0030`, CL=GPIO0, LE=GPIO2, CKV=GPIO32, SPH=GPIO33, OE/GMODE/SPV via PCAL6416A I2C expander) is identical across Inkplate6/10/5v2/6FLICK — only resolution and waveform timing differ. This is why a single config-driven C driver is realistic, not speculative.
- `Dependencies/mcp23017.py` is dead code (no board imports it).
- `touch_cypress.py` (Inkplate6FLICK) has self-documented placeholder/incomplete logic in `tsGetXY`/`tsGetResolution`.
- Inkplate4TEMPERA and Inkplate6PLUS are in-scope per user, both confirmed classic-ESP32 parallel-bus boards (same family as Inkplate6/10/5v2/6FLICK), not yet present in this repo — user will supply pin map/waveform data; slotted into Phase 8 alongside the other parallel-bus boards.

## Sequencing strategy

Firmware build (MicroPython + ESP-IDF) is the user's own responsibility — not a plan focus here, only touched where the C-porting work needs a hook into it (`USER_C_MODULES` scaffold, board variant/partition sizing). Priority is the C port itself. Bring up ONE board (**Inkplate10** — best-commented reference, largest/most demanding panel, hardware available) through the full new stack first, with de-risking stepping stones, before generalizing to the other 5 parallel-bus boards (Inkplate6, 5v2, 6FLICK, 6PLUS, 4TEMPERA — all classic ESP32) via config only. Build the board-config abstraction early (not deferred) since the pin/control layout is already confirmed near-identical across boards — designing it now avoids Inkplate10-specific assumptions leaking into "shared" code later. SPI-controller panels (Inkplate6COLOR, Inkplate2, Inkplate13SPECTRA) are also in scope for the C refactor, not just an optional assessment — see Phase 9. Every task has a concrete build/unit-test/hardware-in-the-loop (HIL) verification. Tasks needing hardware values not currently known (per-board 3-bit waveform timing, Cypress touch protocol, SPI panel command sets, 4TEMPERA/6PLUS specs) are flagged **[NEEDS INPUT]** — user will supply these (from the Arduino reference driver, datasheets, or hardware specs) per-task, don't guess.

---

## Phase 0 — Git workflow + baseline

1. **[DONE]** **Git workflow**: regular commits directly to the existing `refactor` branch, no PR/branch-per-task ceremony. Follow commit conventions specified in CLAUDE.md. Add a GitHub Actions workflow running on every push: (a) build check — compile the C module/firmware once the toolchain exists, (b) format check — `clang-format` for C, `black`/`ruff` for the Python config layer, (c) unit test runner — the host-compiled C tests introduced in later phases (board-config, waveform LUT, framebuffer packing). No hardware in CI. **Verify**: workflow runs green on a trivial commit, red on a deliberately broken one.
2. **[DONE]** **Capture baseline on real Inkplate10**: run existing mono + GS2 examples on current firmware, record timing (code already logs render times). This is the regression oracle for every later step. **Verify**: mono and GS2 examples run successfully on current firmware with render times recorded, establishing the regression oracle for later steps.
3. **[DONE]** **Restructure repo layout for the C port**: flat top-level layout, no `src/` wrapper (this repo ships multiple independent mip-installable board packages, not one pip-built package, so the CPython `src/`-layout rationale doesn't apply). `boards/<board>/` per board, collapsing the current double-nested `<board>/<board>/*.py` into a single level. `shared/` (renamed from `dependencies/`) for the shared Python (gfx/shapes/PCAL6416A) that survives consolidation. Reserve `firmware/usermods/inkplate/` as the future home for the shared C driver component (empty scaffold until Phase 1+, matches ESP-IDF's `USER_C_MODULES`/`EXTRA_COMPONENT_DIRS` convention — kept outside `boards/` since the C driver is one shared component, not per-board). `examples/` and `docs/` stay top-level. Update each board's `package.json` to the new `boards/<board>/` and `shared/` paths — breaking the current mip install paths is accepted now, not deferred, since this repo has no external users yet. **Verify**: new tree is in place with `boards/`, `shared/`, and `firmware/usermods/inkplate/` present, and `mip install` against a board's updated `package.json` paths succeeds cleanly.
4. **[DONE]** Once user has a self-built MicroPython+ESP-IDF firmware available: confirm PSRAM is enabled/detected (GS buffers are ~250KB+, must live in PSRAM) and add a `USER_C_MODULES` scaffold with one trivial function (e.g. `inkplate.version()`). **Verify (HIL)**: importable from REPL, returns expected value — proves C build/link/registration before any driver logic exists. This is the only firmware-build-adjacent task in this plan; everything else assumes the firmware toolchain is a given.

## Phase 1 — Board-config abstraction (data only, no transport)

5. **[DONE]** Define a C `board_config` struct: resolution, data-bus pin list, control pins (CL/LE/CKV/SPH), PCAL6416A pin assignments (OE/GMODE/SPV), PMIC I2C address, waveform table pointer, feature flags (touch/frontlight). Populate the Inkplate10 instance from confirmed constants above. **Verify**: host-compiled C unit test instantiates config, asserts fields match known Inkplate10 values.
6. **[DONE]** Decide + implement how C reaches the PCAL6416A-controlled lines (OE/GMODE/SPV — not in the hot per-row loop, so keep `pcal6416a.py` in Python, expose a clean C-callable interface, revisit only if timing demands otherwise). Design: C holds a Python callback (`cb(addr, pin, value)`) registered at init, invoked via `mp_call_function_n_kw` — keeps the I2C bus single-owner (Python also owns PMIC/RTC on the same bus per Phase 8 step 27). Implemented as `firmware/usermods/inkplate/expander_bridge.{h,c}`, bound into the module as `inkplate.set_expander_write_cb(cb)` / `inkplate.test_expander_write(addr, pin, value)`. **Verify (HIL)**: OE/GMODE/SPV are wired to the panel, not probeable directly, so verify the bridge mechanism itself on a free/unused expander pin instead (`_PCAL6416A_2`/addr 0x21, unused by any board function, or addr 0x20 pins 11-15) via multimeter/logic analyzer/LED. Confirm the real OE/GMODE/SPV path separately via observed PMIC/panel behavior (`power_on`/`power_off` still work correctly through the bridge).

## Phase 2 — De-risk: bit-bang control lines in C (no DMA yet)

7. **[DONE]** Port `vscan_start/write/end` and `fill_screen` (currently viper) to C, using the known-good bit-bang method — no I2S yet. Config-driven from Phase 1's struct. **Verify (HIL)**: mono example renders identically to Phase 0 baseline, now executing through C. Isolates "C can drive the panel at all" from "does I2S DMA work" before combining both unknowns.

## Phase 3 — I2S parallel transport

User will supply the I2S configuration in C directly (classic ESP32 I2S0 LCD/parallel-mode DMA setup) — no research/design-doc task needed here, just integration.

8. **[DONE]** Integrate user-supplied I2S config into a minimal spike: hardcoded-for-Inkplate10 single-row DMA transfer, framed by manual SPH/LE/CKV pulses. Explicitly throwaway. **Verify (HIL)**: full-frame hardcoded pattern via I2S visibly reaches the real panel (comparable to Phase 2's `fill_screen` check). Confirm whether GPIO matrix routing (per the supplied config) makes the current `byte2gpio` scatter/gather remap unnecessary.
9. **[DONE]** Generalize the spike into a real `inkplate_i2s` component: init/deinit, PSRAM DMA buffers, `push_row`/`push_frame` primitives, double-buffered (compute next row while current transfers), parameterized by the Phase 1 board config. **Verify (HIL)**: full-screen clear via I2S matches Phase 2's bit-bang clear (same visual result), timing recorded as new baseline.

## Phase 4 — Waveform engine + mono display (walking skeleton)

10. **[DONE]** Port `_gen_wave`/`genlut` LUT generation to C, generalized over bit-depth (so 1-bit/2-bit/3-bit all flow through one code path — this is what makes the later 3-bit switch a data change, not a rewrite). **Verify**: host-compiled unit test — generated LUTs match hand-computed values for a synthetic table, and legacy 2-bit path reproduces current Python `_wave` output byte-for-byte.
11. **[DONE]** Full mono (1bpp) display pipeline on I2S transport + waveform engine, Inkplate10. **Verify (HIL)**: text/shapes render clean and ghost-free — **first true end-to-end milestone**: our C module + I2S DMA path, working.
12. **[DONE]** Port `clean()` (pre-refresh flush patterns) to the I2S path. **Verify (HIL)**: matches baseline flush behavior, no new ghosting.

## Phase 5 — Grayscale: 2-bit parity, then 3-bit upgrade

13. **[DONE]** Port `InkplateGS2` (existing `WAVE_2B` table, current 4-level behavior) onto the I2S+engine path — no new gray levels yet, parity check only. **Verify (HIL)**: matches Phase 0 baseline 4-level output, faster.
14. **[DONE]** Switch framebuffer to 3-bit/8-level. `framebuf` has no native 3bpp mode — decide packing (likely reuse `GS4_HMSB` 4bpp storage using 8 of 16 levels, simplest reuse of existing `framebuf` primitives vs. custom 3bpp pack). Update `writePixel` and image-loader color mapping accordingly. **Verify**: unit test — pixel set/read round-trips correctly for all 8 levels; buffer allocates on-target without OOM (check PSRAM budget for 4bpp full-frame buffer, ~495KB at Inkplate10 resolution).
15. **[DONE]** User supplied the real Arduino reference driver source (Inkplate10Driver.cpp/h, pins.h, waveforms.h) directly, superseding the in-repo comment, which turned out to be wrong (8 values/row, not 9, and shifted vs. the real table). Wired in `waveform1[8][9]` (Arduino's compiled-in default, used whenever no per-device EEPROM calibration is present — 5 alternate presets + EEPROM read exist in Arduino but are out of scope here, single hardcoded table per user decision), transposed to this project's `[phase][color]` row convention (`board_config.c`'s `wave_3b_inkplate10`). `waveform.c` gained `inkplate_gen_wave_3bit` (bpp=4 pass-through through the existing generalized `inkplate_gen_nibble_lut`). `epd_i2s.c` gained a native row builder (`build_gs3_row`/`gs3_combine`, replacing the interim `gs_pack.h` GS4->GS2 fold from step 14) plus the `esp_rom_delay_us(230)` inter-phase gap and, in `inkplate_gs.py`, the trailing `clean(3,1)`+`vscan_start()` park pulse — both matching the real `display3b()`'s full conditioning sequence. Legacy 2-bit engine (`wave_2b_inkplate10`, `inkplate_gen_wave_2bit`, `build_gs_row`, `gs_pack.{c,h}`) deleted as dead code once nothing called it. **Verify (HIL)**: the real `wave_3b_inkplate10` waveform table drives correct 8-level grayscale output through `build_gs3_row`/`gs3_combine` on real Inkplate10 hardware.

    **Follow-up: classic INKPLATE10 (v1) support added [DONE]**, same split pattern step 22's follow-up already established for Inkplate6: user confirmed classic Inkplate10 also uses an MCP23017 internal expander @ 0x20 (V2 uses PCAL6416A there) with external expander @ 0x22 (V2's is @ 0x21), and also has onboard touchpads. `board_config_inkplate10` (now classic) gained `has_touch=1`; new `board_config_inkplate10v2` struct added (byte-identical otherwise). `boards/inkplate10/inkplate10.py` gained the same `_BOARD_VARIANT`/`_IS_CLASSIC`/`_EXPANDER2_ADDR` scaffold as `inkplate6.py`, defaulting to `"inkplate10v2"`, plus the same external-expander chip-shortage autodetect fix already shipped for Inkplate6. Classic Inkplate10 has no SD-card power MOSFET (shares expander pin 10 with touchpad pad1) — `init_sd_card`/`sd_card_sleep`/`sd_card_wake` are no-ops on classic. **Verify (HIL)**: classic Inkplate10 (`has_touch=1`) and Inkplate10 V2 both initialize correctly, with touchpads and expander autodetect working on classic hardware.

    **Second follow-up: variant selection moved to a constructor arg [DONE]**, both Inkplate6 and Inkplate10 — the module-level `_BOARD_VARIANT` constant was ergonomically worse than a runtime parameter, and there's no way to auto-detect classic-vs-V2 from hardware (neither expander chip exposes an ID/WHOAMI register), so a param was the only option anyway. Replaced with `_DEFAULT_VARIANT` + `_VALID_VARIANTS` (raising `ValueError` on typos) and a per-call `variant=_DEFAULT_VARIANT` kwarg threaded through `Inkplate.__init__`/`begin()`. Renamed the classic variant's selector string for symmetry with V2 (`"inkplate6"`/`"inkplate10"` -> `"inkplate6v1"`/`"inkplate10v1"`). Both boards default to their V2 variant unchanged. **Verify**: passing `variant="inkplate6v1"`/`variant="inkplate10v1"` to the constructor correctly threads through to `_Inkplate.init()` and selects classic hardware behavior, with V2 remaining the unchanged default.

## Phase 6 — Partial update (Inkplate10)

Moved ahead of board generalization: no cross-board dependency (mono-only, already
config-driven), and finishing Inkplate10's feature set before fanning out to 5 more boards
means Phase 8 generalizes a fully-proven mono+GS+partial stack in one pass instead of
porting partial update separately, later, per board.

16. **[DONE]** Port `InkplatePartial` diffing/shift-out to C, mono only, **over I2S DMA** (not bit-bang) on Inkplate10 — matches how the real Arduino reference driver does it, and keeps partial update on the same transport as `mono_display`/`gs_display`/`clean()` instead of being the only bit-banged display path left. `firmware/usermods/inkplate/epd_partial_lut.{h,c}` builds a 256-entry old-nibble/new-nibble -> 2bpp-code LUT from the real reference driver's `LUTW`/`LUTB` tables via `diffw = old & ~new`, `diffb = ~old & new`. No per-region shortcut: the real reference driver's `partialUpdate()` takes no x/y/w/h and always walks the full frame — `InkplatePartial.display()`'s signature dropped `x/y/w/h` entirely to match. Bound as `inkplate.partial_display(old_fb, new_fb)`. **Verify (HIL)**: only changed region refreshes, correct threshold-triggered full refresh, timing compared against the superseded bit-bang attempt below.
    - Superseded first attempt (bit-bang, deleted): a mono bit-bang C port (`firmware/usermods/inkplate/epd_partial.{h,c}`, an earlier version of `epd_partial_lut.{h,c}` — a literal 1:1 port of the old direct-GPIO-register `_send_row`/`_skip_rows`, plus a generalized byte2gpio scatter off `cfg->data_pins`). Superseded because partial update needed to be on I2S like the rest of the pipeline, matching the real reference driver above, not because it didn't work — deleted once the I2S port landed.

## Phase 7 — Image decode + dithering: grayscale path (Inkplate10)

Moved ahead of board generalization (Phase 8) and SPI-panel bring-up (Phase 9) per user
request: image loading is a user-visible feature gap on the one board that's fully built
and HIL-provable today (Inkplate10), and doesn't need any of the other 5 parallel-bus
boards or the SPI color panels to exist first. Not a from-scratch C port of the current
Python decode loops — embed existing, already-optimized C decoder libraries instead. Only
the dithering/quantization step (RGB → this project's specific bit-depth output) is newly
written C, since that logic is Inkplate-specific and can't come from a generic library.
The general N-color palette dithering path (color/BWR panels) stays deferred to Phase 10,
after Phase 9's SPI boards exist to verify it against — no point writing untestable code
now.

17. **[DONE]** Port `shared/gfx.py` + `shared/shapes.py` (line/rect/circle/triangle/text primitives, pixel-callback based) to C, Inkplate10 first. Put first in this phase since image drawing (decoded/dithered output) and shape drawing land on the same pixel-write path — porting gfx now gives the decoders below a proven C drawing primitive layer to render into, instead of crossing back into Python per pixel/shape. **Verify**: host-compiled unit test — for line/rect/circle/triangle/fill primitives, compare the set of (x,y[,color]) pixel-callback coordinates against the current Python implementation's output for matched inputs, coordinate-for-coordinate. **Verify (HIL)**: existing shape-drawing examples (`basic_bw.py` etc.) render identically to before, now through the C path.
18. **[DONE]** Enable ESP-IDF's bundled software JPEG decoder (`tjpgd`) at firmware build time — no external library needed, no porting, just component config. Wire it into a thin C call producing decoded RGB output per row/tile. **Verify**: host-compiled unit test decodes a known-good sample JPEG to the expected pixel buffer.
19. **[DONE]** No IDF-bundled PNG decoder exists — user to supply a PNG decoder library to embed as a firmware component, same treatment as JPEG (no porting, just wire in). **Verify**: host-compiled unit test decodes a known-good sample PNG to the expected pixel buffer.
20. **[DONE]** Write a BMP decoder directly in C (format is simple enough that no external library is needed). Scope: 24-bit uncompressed (BI_RGB), plus 1/4/8-bit indexed (palette, uncompressed) and 16-bit (555/565, uncompressed) — RLE4/RLE8 and 32-bit stay out of scope (not worth the added decode complexity/test surface for this project's use case). **Verify**: host-compiled unit test decodes a known-good sample BMP correctly per supported depth.

    **Ahead of schedule, HIL-verified**: same "wire a placeholder draw path now" treatment steps 18/19 gave JPEG/PNG. `firmware/usermods/inkplate/bmp_draw.{h,c}` (`bmp_draw_gs4`) mirrors `jpeg_draw_gs4`/`png_draw_gs4`: parses the whole in-memory BMP buffer via `bmp_parse_header`/`bmp_parse_palette`/`bmp_parse_bitfields`, then walks rows through `bmp_decode_row` into a static (not stack/heap) RGB888 row-scratch buffer sized for the widest board resolution in scope, quantizing each pixel to the nearest of 8 gray levels via `gfx_set_pixel` — no dithering, matching the JPEG/PNG placeholders. Bound as `inkplate.bmp_draw_gs4(fb, phys_w, phys_h, rotation, x0, y0, bmp_bytes) -> (width, height)`. **Verify (HIL)**: `bmp_draw_gs4` decodes an in-memory BMP and blits it into a GS4 framebuffer via `gfx_set_pixel`, quantized to 8 gray levels with no dithering, matching the JPEG/PNG placeholder treatment.
21. **[DONE]** Port the existing dithering/quantization logic to C — this already existed in `inkplate10.py`'s `write_row`/`write_image`/`decode_png_to_framebuffer` (`@micropython.viper`, Floyd-Steinberg/JJN/Stucki/Burkes kernels), but the three implementations disagreed with each other and two of them had dead mono-dithering code — ported+fixed instead, disclosed rather than silently changed. New `firmware/usermods/inkplate/dither.{h,c}` generalizes scalar error diffusion over `display_mode` (mono/3-bit GS) with all 4 kernels as data tables; GS3 quantization standardized on the round-to-nearest formula steps 18-20's non-dither placeholders already used; `invert` now applies as a final XOR on the quantized level instead of before computing the error-diffusion reconstruction value. `bmp_draw_gs4`/`jpeg_draw_gs4`/`png_draw_gs4` extended with `display_mode`/`invert`/`dither`/`kernel_type` params. BMP dithers inline row-by-row; JPEG/PNG buffer the whole image's luma into a capped scratch buffer first, then dither in one guaranteed row-major pass afterward, since neither tjpgd's tile order nor pngle's Adam7 passes guarantee full-scanline order. `boards/inkplate10/inkplate10.py`'s `draw_bmp_from_sd/web`, `draw_jpg_from_sd/web`, `draw_png_from_sd/web` now call these C bindings directly; old Python dither code (~950 lines) deleted. Kernel tables ported faithfully including a pre-existing bug: the Stucki table's weights sum to 32 but are divided by 42, under-diffusing ~24% of each pixel's error — not fixed, since correcting it changes visual output beyond this step's scope. **Verify (HIL)**: mono and GS3 examples render with correct dithering across all four kernels (Floyd-Steinberg/JJN/Stucki/Burkes), with invert applied correctly after quantization.

    **Follow-up, HIL-verified on Inkplate6COLOR and Inkplate13SPECTRA**: prompted by a PSRAM-capacity question, ported the same row-band buffering `jpeg_draw_palette` uses onto `jpeg_draw_gs4` — no more whole-image buffer or height cap for JPEG grayscale, since tjpgd's tiles arrive in strict row-band raster order; width is still capped (`JPEG_DRAW_BAND_MAX_W`), rejected via a `pre_cb` before any MCU decoding. PNG is different — Adam7 interlacing genuinely re-sweeps the whole image, so an interlaced source still needs the whole-image buffer — but interlacing is opt-in and rare, so `png_draw_gs4` now peeks IHDR's interlace flag (`png_peek_interlace`) and splits into two paths: non-interlaced dithers inline per pixel with no buffer at all and no cap, interlaced falls back to the existing capped whole-image buffer. **Verify (HIL)**: JPEG grayscale draws via row-band buffering with no whole-image buffer beyond `JPEG_DRAW_BAND_MAX_W`; non-interlaced PNG dithers inline with no buffer, interlaced PNG falls back to the whole-image buffer.

    User pushback caught a real scope gap: `png_draw_palette` (the color/palette PNG path Inkplate6COLOR/Inkplate13SPECTRA/Inkplate2 actually use) had the exact same unconditional whole-image-buffer bug. Same fix applied there: non-interlaced sources now dither inline per pixel via `dither_rgb_ctx_t`, no scratch buffer and no `max_width`/`max_height` cap at all; only a genuinely Adam7-interlaced source (or a failed peek) still falls back to the caller-supplied `scratch_rgb` buffered path. **Verify**: `png_draw_palette` for non-interlaced sources dithers inline via `dither_rgb_ctx_t` with no scratch buffer or size cap; interlaced sources still use the buffered `scratch_rgb` fallback.

    Root-caused a real-hardware `MemoryError` on Inkplate6COLOR (not guessed): the bug wasn't in the C fix at all — `boards/inkplate6color/inkplate6_color.py`'s (and 13spectra's, and inkplate2's) `draw_png_from_sd/web` unconditionally allocated a fresh `_DITHER_SCRATCH_BYTES` scratch buffer *before* ever calling into C, every time `dither=True`, regardless of whether the now-mostly-unnecessary buffer would ever be used. Fixed by removing the eager allocation across all three boards and deleting the now-dead `_DITHER_SCRATCH_W/H/BYTES` constants. **Verify**: `draw_png_from_sd/web` on Inkplate6COLOR/Inkplate13SPECTRA/Inkplate2 no longer eagerly allocates a dither scratch buffer before calling into C.

## Phase 8 — Generalize to remaining parallel-bus boards (config only)

22. **[DONE]** Inkplate6 (800x600): add config + its 3-bit waveform table (user supplied the real Arduino reference driver: `pins.h`/`waveforms.h`/`Inkplate6Driver.{h,cpp}`, covering both the classic INKPLATE6 and INKPLATE6V2 hardware revisions). **Verify (HIL)**: mono + 8-level GS + partial update + basic examples on real hardware.

    **Follow-up correction, real hardware bug found and fixed, not guessed**: this step's "PCAL6416A internal-expander addresses... identical to Inkplate10" claim was wrong for classic (non-V2) INKPLATE6 — classic INKPLATE6's internal expander (addr 0x20) is genuinely an MCP23017; only INKPLATE6V2 uses a PCAL6416A there. `shared/mcp23017.py` gained `pin_mode`/`digital_write`/`digital_read` methods matching `PCAL6416A`'s shape so the existing `GpioPin` wrapper and `_expander_write_cb`'s addr-routed dispatch work unchanged regardless of which chip is behind `_expander1`/`_expander2`. `boards/inkplate6/inkplate6.py` now branches expander class construction on `_IS_CLASSIC`. Also wired the onboard touchpads (classic-only, same expander pins V2 repurposes for `SD_ENABLE`) — classic v1 has no SD-card power MOSFET at all, so `init_sd_card`/`sd_card_sleep`/`sd_card_wake` now skip that toggle on classic. **Verify (HIL)**: classic (non-V2) Inkplate6 initializes correctly through the MCP23017-backed internal expander, with touchpads TOUCH1/2/3 readable and SD_ENABLE correctly skipped on classic hardware.
23. **[DONE]** Inkplate5v2 (1280x720): same treatment (user supplied the real Arduino reference driver: `pins.h`/`waveforms.h`/`Inkplate5V2Driver.cpp`). **Verify (HIL)**: mono + 8-level GS + partial update + basic examples on real hardware.
24. **[DONE]** Inkplate6FLICK (1024x758): display + partial path only via config; explicitly defer touch/frontlight to Phase 11 (user supplied the real Arduino reference driver: `Inkplate6FLICKDriver.h`'s `pins.h`/waveform macro, `Inkplate6FLICKDriver.cpp`, plus `TPS65186.h`/`.cpp`). This board's `display1b()` uses 4 black-push phases instead of the usual 5, followed by its own separate discharge pass — `inkplate_gen_mono_wave` took a `black_phases` param instead of a hardcoded constant to accommodate this. **Verify (HIL)**: mono + GS + partial update work.

    SD card wired this pass too, following Inkplate6/5v2's exact `machine.SDCard` SPI-mode pattern plus this board's own real SD-power-MOSFET pin: `init_sd_card()`/`sd_card_sleep()`/`sd_card_wake()` added to `inkplate6_flick.py`, toggling `SD_ENABLE` at `IO_PIN_B5` (pin 13) on the internal expander (0x20). Also fixed a mislabeled example that contained a copy of the WiFi/URL example instead of an actual SD example. **Verify (HIL)**: SD mounts cleanly and `draw_jpg_from_sd`/`draw_png_from_sd`/`draw_bmp_from_sd` decode and display correctly.
25. **[DONE]** Inkplate6PLUS: same treatment (classic ESP32, parallel-bus, confirmed by user) — user supplied the real Arduino reference driver (`waveforms.h`, `pins.h`, `Inkplate6PLUSDriver.{h,cpp}`, `GraphicsDefs.h`). **V2 revision only** wired this pass (IO_EXT_ADDR 0x21; classic INKPLATE6PLUS's 0x22 isn't supported by this file) — touch/frontlight/VCOM-EEPROM/battery/temperature deliberately out of scope, same precedent as Inkplate6FLICK's first pass. **Verify (HIL)**: mono + 8-level GS + partial update work on real Inkplate6PLUSV2 hardware, matching the scope already established for Inkplate6FLICK's first pass.

    **Real bug found and fixed via HIL, not guessed**: mono `display1b()` initially came out uniformly dark/washed on real hardware (background never reached true white) while GS3 rendered correctly — isolating the fault to the mono engine's `black_phases` parameter. Root-caused by decoding this board's own `GraphicsDefs.h` `LUTW`/`LUTB` tables: this board's real per-pixel scheme is the *mirror image* of every other wired board's — repeated phases push **white** (black pixels skip), one final phase pushes **black** (white pixels skip). Added `inkplate_gen_mono_wave_white_first()` (`waveform.{h,c}`) and special-cased `board_config_inkplate6plusv2` onto it in `inkplatemodule.c`'s `inkplate_mono_display`, by pointer check — every other board's path is untouched. **Verify (HIL)**: mono renders with correct polarity (background white, no ghosting) using the white-first waveform scheme; GS3 remains unaffected.

    SD card also wired this pass — `init_sd_card()`/`sd_card_sleep()` added to `inkplate6plusv2.py`, following Inkplate6/5v2's exact `machine.SDCard` SPI-mode pattern, but using this board's own real SD-P-MOS-enable pin (13, `SD_PMOS_PIN`/`IO_PIN_B5`, different from Inkplate6/5v2's pin 10). Started at the same 4MHz SD clock those boards settled on. **Verify (HIL)**: SD mounts cleanly at 4MHz and `draw_jpg_from_sd`/`draw_png_from_sd`/`draw_bmp_from_sd` decode and display correctly.

    Still open for a future pass: classic (non-V2) INKPLATE6PLUS variant; `power_on()`/`power_off()` PMIC sequence (carries over Inkplate6/5v2's generic sequence — this board's real pasted `einkOn()` shows a more specific one, but its internals were never pasted, so it can't be replicated exactly without guessing); touch, frontlight, VCOM/EEPROM, battery, temperature.
26. **[DONE]** Inkplate4TEMPERA: same treatment (classic ESP32, parallel-bus, confirmed by user) — user supplied the real Arduino reference driver (`Inkplate4TEMPERADriver.{h,cpp}`, `pins.h`, `waveforms.h`, `GraphicsDefs.h`). First square panel wired (600x600). Display path + SD card only, same precedent as Inkplate6FLICK/6PLUSV2's own first pass — touch/frontlight/battery/BME688/APDS9960/accelerometer/buzzer/VCOM-EEPROM deliberately out of scope despite this board's Arduino driver carrying all of them. Two genuine quirks confirmed against the pasted source: `display3b()`'s real table is 9 phases (bookended, matching every other board), not the 8 an initial paste truncation suggested; `display1b()` uses 10 black-push phases, not the usual 5 (`INKPLATE_MONO_WAVE_MAX_PHASES` bumped 6→11, `black_phases=10` special-cased in `inkplatemodule.c`) — confirmed via this board's own `GraphicsDefs.h` that it's the standard scheme just run 10 times, not a reversed-role variant like Inkplate6PLUSV2. `partial_reps=9`. This board's GS4 nibble-packing order is also the opposite of Inkplate6PLUSV2's (even x writes the high nibble), derived directly from its own pasted `writePixelInternal`. Added `board_config_inkplate4tempera`; `boards/inkplate4tempera/*.py` written C-usermod-backed from scratch, mirroring Inkplate6PLUSV2's 4-file structure. SD wired this pass on `SD_PMOS_PIN` (pin 11). `power_on()`/`power_off()` PMIC sequence carries over Inkplate6/5v2/6PLUSV2's generic sequence (not independently confirmed). **Verify (HIL)**: mono (10-phase `black_phases`) and GS3 (9-phase bookended waveform table) both render correctly with correct polarity, and partial update + SD card function correctly, on real Inkplate4TEMPERA hardware.
27. **[DONE]** Collapse duplicated Python (RTC, power) into shared `shared/` versions across all 6 parallel-bus boards; delete per-board copies. `gfx.py` dedup is superseded here if Phase 7 step 17's C port already landed for Inkplate10 — extend that C path to the other boards instead of deduping the Python copy. Once every board is off `shared/gfx.py`/no longer needs it, delete `shared/shapes.py` too. Fix the `_tps65186_read` missing-`return` bug once, in the shared layer. Fix the standing `power_off` TODO (`# TODO: also tri-state gpio pins to avoid current leakage during deep-sleep`) — real power-draw bug on a battery-powered device, cheap to fix once, here. **Verify (HIL)**: all boards still render post-dedup; `_tps65186_read` returns real temperature; measure deep-sleep current draw before/after the tri-state fix to confirm the leakage is actually gone.

    **All pieces done.** TPS65186 (`shared/tps65186.py`, real enable-rails → poll `PWR_GOOD` → VCOM order, fixes the missing-`return` and battery-drain bugs), RTC (`shared/rtc.py`, wired onto all 6 boards), the `gfx.py` C-port extension (discovered already complete — all 5 other boards already call the same 12 `inkplate.gfx_*` bindings Inkplate10 got in step 17, just never logged as closing this item), `shared/shapes.py`+`shared/gfx.py` deletion (confirmed zero importers repo-wide once Inkplate2/6COLOR/13SPECTRA's own diverged local copies were confirmed separate), and the `power_off` tri-state fix: new `shared/epd_power_pins.py` (`tristate_display_pins`/`restore_display_pins`) sets the native control/data bus plus the expander-driven OE/GMODE/SPV lines to `INPUT` in `power_off()` and restores them in `power_on()` (`TPS_WAKEUP`/`PWRUP`/`VCOM` deliberately left driven, not floated). **Verify (HIL)**: `power_on`/`power_off`/`read_temperature`/`read_battery`/`rtc_set_time`/`rtc_set_date` all work correctly on real hardware; a full power_off→power_on cycle tri-states then restores all pins correctly, with display still functional afterward.
    - **Not yet done**: the actual deep-sleep current-draw measurement (multimeter/power profiler) this step's own Verify criteria calls for — needs real hardware measurement the user will do separately.
28. **[ATTEMPTED, REVERTED]** Tried replacing `pngle.c`'s per-pixel-callback PNG decode with `stb_image.h` (`stbi_load_from_memory`) to speed up PNG decode (measured ~18s vs JPEG's ~6s at the same resolution/dithering, traced to pngle's per-pixel callback overhead and per-pixel software divides on classic ESP32's Xtensa LX6, which has no hardware integer divide). **Verify (HIL)**: stb_image decodes typical panel-sized photos within available PSRAM. Reverted: stb_image fully materializes the decoded image in RAM (peak ~2x the native image size), which reliably exceeded available PSRAM headroom for ordinary panel-sized photos on 4MB-PSRAM boards — reverted back to `pngle.c` rather than keep patching around the memory ceiling.
    - **Revised plan, [ATTEMPTED, REVERTED]**: instead of swapping to a fully-materializing decoder, tried optimizing `pngle.c` itself, since its streaming architecture is the right shape for this memory-constrained board. Two targeted fixes: (a) batch pngle's draw callback to fire once per row instead of once per pixel; (b) replace `adjust_color`'s RGBA-scaling software divides with a precomputed scale strategy. Clean host build/test and clean real xtensa build. **Verify (HIL)**: row-batched, divide-free `pngle.c` decodes the same test PNG faster than the unmodified baseline.
    - **HIL measurement told a different story than the theory** — measured slower, not faster, after both fixes; reverted. All code changes reverted; board reflashed back to the unmodified firmware and reconfirmed working. **Not re-attempted for now** — if revisited, profile first rather than trusting the algorithmic-complexity argument that motivated this attempt, since it didn't hold up against real measurement.

## Phase 9 — SPI-controller-panel family: C port (Inkplate6COLOR, Inkplate2, Inkplate13SPECTRA)

Confirmed in-scope for the C refactor, not deferred as optional. Architecturally separate from the I2S/parallel-bus work (these panels have an onboard timing-controller ASIC driven over SPI with CS/DC/RST/BUSY) — can proceed in parallel with Phases 2-8 once Phase 1's config-struct pattern exists to mirror.

29. **[DONE]** Define a per-panel SPI config struct (mirrors Phase 1's parallel-bus struct): SPI pins (CS/DC/RST/BUSY/SCK/MOSI), panel resolution, command set pointer. Note Inkplate13SPECTRA is ESP32-S3, not classic ESP32 — needs its own firmware target, separate from the Inkplate10-family build. **Verify**: host-compiled unit test on struct fields per panel.
30. **[DONE for Inkplate6COLOR]** Reordered ahead of Inkplate2 at user's request (user supplied Inkplate6COLOR's real Arduino reference driver directly). Ported the command-send loop to C, config-driven, generic across every single-chip panel in this family so Inkplate2 can reuse it unchanged later. `firmware/usermods/inkplate/epd_spi.{h,c}` — init/deinit, reset, busy-wait, send-command/send-data (with explicit settle delays transcribed from the real Arduino driver), and chunked transfer (Inkplate6COLOR's full-framebuffer write is far larger than ESP-IDF's default single-transaction cap). Bound into the `inkplate` module as `select_spi_panel(name)`/`spi_panel_init()`/etc. `boards/inkplate6color/inkplate6_color.py` rewired onto these bindings: only the actual bit transfer moved to C, command/data *sequences* stay Python. SD card wired this pass too, same `machine.SDCard`/4MHz pattern every parallel-bus board uses; the panel's SPI DMA channel was pinned explicitly (channel 2) since classic ESP32 has only 2 SPI-DMA channels total and an auto-picked channel could otherwise collide with `machine.SDCard`'s own hardcoded channel. **Verify (HIL)**: `begin()`/`clean()`/shape-drawing/`display()` render correctly through the new C SPI transport, and SD mounts cleanly alongside it, on real Inkplate6COLOR hardware.
31. **[DONE]** Repeat command-port for Inkplate2, then Inkplate13SPECTRA (ESP32-S3 build, and its real 2-SPI-chip requirement). Image decode/dithering for these panels is handled in Phase 10, not here. Added `spi_panel_config_inkplate2` and registered it; `boards/inkplate2/inkplate2.py` rewired onto the shared `epd_spi` C transport the same way as step 30, with its busy-wait timeouts corrected to match the reference driver's two distinct values. Three real hardware-behavior gaps resolved with the user: sleep/wake pattern kept 6COLOR's simpler existing pattern (accepting a small idle-current tradeoff); reset pulse width bumped to 100ms/200ms low/settle to match both panels' specs; BUSY pin pull-up enabled to match Inkplate2's real spec. **Verify (HIL)**: each panel's examples render correctly post-port — shapes, text, and `display()` all correct (colors right, not garbled/inverted/mirrored) on real hardware.

    **Inkplate13SPECTRA** (user supplied the real Arduino reference driver directly, including `display()`/`displayPartial()`). Unlike 6COLOR/Inkplate2, this panel's real protocol has no DC phase at all and needs a dual-chip CS selection (master/slave/both, one SPI-controller ASIC per screen half sharing SCK/MOSI) — added a parallel `epd_spi_dual_*` API and `spi_panel_config_inkplate13spectra` (`chip_count = 2`, plus new `pin_cs2`/`pin_pwr_en`/`pin_bs0`/`pin_bs1` fields). `boards/inkplate13spectra/inkplate13_spectra.py` rewired the same way as the other two boards. **Deliberately not ported this pass**: `displayPartial()` — new functionality beyond what the pre-refactor Python driver had, with intricate rotation/alignment math risky to ship unverified with no ESP32-S3 hardware access this pass; flagged as a real gap vs. the reference driver. `epd_i2s.c` (parallel-bus I2S transport, still compiled into every board's usermod binary) needed a `#if CONFIG_IDF_TARGET_ESP32` guard with no-op stubs on ESP32-S3, since classic ESP32's I2S1 register fields it uses don't exist on S3 (which uses GDMA instead). **Verify (HIL)**: full firmware build succeeds on `ESP32_GENERIC_S3`, and `begin()`/`fill_rect()` in all four testable colors/`display()` render correctly in all four quadrants on real Inkplate13SPECTRA hardware, with no DMA-channel conflict against the SD path.

## Phase 10 — Image decode + dithering: color/palette path (SPI panels)

Completes the shared dithering engine scaffolded in Phase 7 (JPEG/PNG/BMP decoders, grayscale dithering) with the general-palette path — deferred until here since it needs Phase 9's SPI-controller boards to verify against, not just Inkplate10.

32. **[DONE, HIL-verified on Inkplate13SPECTRA and Inkplate6COLOR]** Extended `dither.c`/`dither.h` with the general N-color RGB palette path: `dither_palette_entry_t` (RGB + output `value`), `dither_quantize_palette` (brute-force nearest-search), `dither_invert_palette_bw` (swaps only a pure-black/pure-white entry pair), and a 3-channel `dither_rgb_ctx_t`. Added matching `bmp_draw_palette`/`jpeg_draw_palette`/`png_draw_palette` (callback-shaped, `write_pixel(ctx, x, y, value)`) since 6COLOR/13SPECTRA/Inkplate2 each need real per-board packing/rotation — all in a new `spi_panel_palette.c`/`.h`, ported verbatim from each board's current viper code. Palette RGB values transcribed verbatim from each board's already-HIL-validated nearest-color search, except Inkplate2, generalized to a real 3-point (black/white/red) nearest-RGB search since its existing code used inconsistent RGB-threshold heuristics instead. JPEG's palette path dithers through a small static per-MCU-band buffer (no heap allocation) since tjpgd delivers tiles in strict row-band order; PNG keeps a caller-supplied `bytearray` scratch buffer (subject to GC-heap fragmentation, not fully solved) since Adam7 interlacing can require the whole image. An oversized-for-dither image now returns a distinct `-2` code (surfaced as `ValueError`) instead of silently redrawing non-dithered. **Verify (HIL)**: SD-card BMP/JPEG/PNG images decode, dither, and display correctly on real Inkplate13SPECTRA and Inkplate6COLOR hardware; oversized sources correctly raise instead of silently degrading.
33. **[DONE, HIL-verified on Inkplate13SPECTRA and Inkplate6COLOR]** Wired `draw_bmp_from_sd/web`, `draw_jpg_from_sd/web`, `draw_png_from_sd/web` across all three SPI color boards to the new C pipeline (`inkplate.bmp_draw_palette`/`jpeg_draw_palette`/`png_draw_palette`, panel/palette/packing auto-resolved server-side from the already-`select_spi_panel()`-selected board); deleted the old per-board viper decode/dither code once every call site was repointed. Two side-effect fixes: 6COLOR/13SPECTRA's JPEG path previously referenced a MicroPython built-in `jpeg` module not present on this firmware build (dead code, not a working feature being swapped out); Inkplate2's JPEG path previously hardcoded `x=0, y=0` regardless of caller args — the new path honors them like every other format already did. **Verify (HIL)**: SD-card jpg/png/bmp loading on Inkplate13SPECTRA/Inkplate6COLOR renders with correct orientation and color through the new C pipeline.

## Phase 11 — Remaining peripherals, touch

34. **[DONE, HIL-verified]** Finish Inkplate6FLICK Cypress touch placeholders (`tsGetXY`/`tsGetResolution`). User supplied the real Arduino `TouchCypress.cpp/.h`/`TouchCypressTypedefs.h` directly. Comparing line-by-line against it surfaced more than the two named placeholders: `ts_get_xy`/`ts_get_resolution` had no reference equivalent and nothing called either (deleted); `touch_in_area`'s invented rescale to a hardcoded resolution was wrong (deleted, `ts_get_data` already scales via `ts_scale`); `ts_get_data`'s per-finger loop fixed to `range(fingers)`; `ts_get_touch_data` now clamps finger count to 2, matching the real driver; the real per-rotation coordinate remap (swap/flip for rotation 1/2/3), previously unported, ported as `_arduino_map()` plus the rotation branches. `touch_cypress.Touch` wasn't wired into `inkplate6_flick.py` at all — wired in via `Touch.init(i2c, PCAL6416A_1, self)` in `begin()` plus delegating methods on `Inkplate`. **Known real bug in the reference itself, deliberately not ported**: `Touch::scale()`'s real driver never actually rescales to screen pixels due to a variable-reuse typo — kept Python's existing bug-free scaling instead. **Verify (HIL)**: `ts_init`/`ts_get_data`/`touch_in_area` correctly report real touches scaled to screen coordinates, with `examples/inkplate6flick/touchscreen.py` detecting in-rectangle touches and redrawing via partial update.

    **Inkplate4TEMPERA touch, Elan controller [DONE]**: Inkplate4TEMPERA and Inkplate6PLUS/6PLUSV2 share the same touch controller/driver (`TouchElan`), separate from Inkplate6FLICK's Cypress controller. User supplied the real driver plus Inkplate4TEMPERA's own touch pin defines; wired for Inkplate4TEMPERA first. New `boards/inkplate4tempera/touch_elan.py`, mirroring `touch_cypress.py`'s structure. Two real vendor bugs found and fixed rather than ported literally: `Touch::power()` hardcoded the wrong expander for this board (fixed to route onto expander2); `touchInArea()`'s `memcpy` copied bytes instead of array elements for finger 2 (fixed to copy both fingers). The vendor's `rotation==0` formula also assumed the wrong native-axis polarity for this unit (fixed via a new `x_raw_inverted` correction flag) and a follow-up 4-corner test caught an axis swap invisible in a single-corner test (fixed via a new `xy_swapped` correction flag). Wired into `inkplate4tempera.py`'s `begin()` plus delegation methods on `Inkplate`. **Verify (HIL)**: `ts_init` handshake succeeds, INT line toggles correctly with real touches, and a four-corner touch test decodes each corner inside/near its true target box on real hardware. Only `rotation==0` has been exercised; rotations 1-3 use the same corrections but haven't been individually HIL-confirmed.

    **Inkplate6PLUS/6PLUSV2 touch, same Elan controller [DONE, HIL-verified]**: user supplied this board's own touch pin defines — EN/RST sit on the internal expander (0x20) here, no collision with that expander's other pins, so `Touch::power()`'s vendor-literal expander target is correct as written for this board (unlike Inkplate4TEMPERA's fix above). `touch_elan.py` promoted from a per-board file to `shared/touch_elan.py`, parameterized with `en_pin`/`rst_pin`/`width`/`height`/`x_raw_inverted`/`xy_swapped`/`xy_flipped` (all default `False`); each board's own `Touch.init(...)` call passes only the flags its own HIL testing confirmed it needs — Inkplate4TEMPERA passes `x_raw_inverted=True, xy_swapped=True`, Inkplate6PLUSV2 passes `xy_flipped=True` (an independent per-board bug shape: a straight double mirror, not a swap). Wired into `inkplate6plusv2.py`'s `begin()` plus the same delegation methods. **Verify (HIL)**: a four-corner touch test decodes each corner inside/near its true target box on real Inkplate6PLUSV2 hardware. Only `rotation==0` has been exercised on real hardware for this board too.
35. **[DONE, HIL-verified on Inkplate4TEMPERA/6PLUSV2/6FLICK]** Ported the real Arduino `Frontlight` driver (gated for Inkplate6PLUS/6PLUSV2/4TEMPERA/6FLICK) as a single `shared/frontlight.py` (`Frontlight.init(i2c, pcal, en_pin)`, `set_brightness(v)`/`set_state(enable)`). `setBrightness()`'s `63 - (v & 0x3F)` inversion to a fixed I2C digital-pot chip ported literally, identical on all three frontlight-capable boards. All three put `FRONTLIGHT_EN` on the internal PCAL6416A expander (0x20) at different pins per board's own pasted `pins.h`. Wired into each board's `begin()` plus `set_frontlight(state)`/`set_frontlight_brightness(v)` delegation on `Inkplate`. Fixed a stale pre-existing Inkplate6FLICK frontlight example that called a nonexistent API.

    **Verify (HIL)**: `set_frontlight(True)` plus a brightness ramp through 0/15/31/47/63 visibly turns the frontlight on and ramps brighter in both directions, on all three real boards.

    **Also flagged for later**: Inkplate4TEMPERA reads battery via a real fuel-gauge IC, not a resistor-divider+ADC like every other wired board — can't be wired from `pins.h` alone. **[NEEDS INPUT]** user to supply the fuel-gauge driver source/datasheet.
36. **[DONE, HIL-verified]** Ported the real Arduino `Buzzer` driver (Inkplate4TEMPERA-only peripheral). `BUZZ_EN` gates the buzzer on/off (active-low) on the internal expander; pitch is set via a separate MCP4018 digital potentiometer. New `shared/mcp4018.py` (`MCP4018`) and `shared/buzzer.py` (`Buzzer`: `init`/`beep_on`/`beep_off`/`beep`). Pitch formula and wiper-percent rounding ported verbatim, including their real C integer-truncation/rounding behavior. Wired into `inkplate4tempera.py`'s `begin()` plus delegation on `Inkplate`.

    **Verify (HIL)**: `beep_on(freq)` produces an audible tone at the requested pitch on real Inkplate4TEMPERA hardware.
37. Ported the real SparkFun BQ27441 fuel-gauge library (vendored under Soldered's wrapper) — resolves the Phase 8 step 27 `[NEEDS INPUT]` flag. New `shared/bq27441.py`, full API surface (`begin`/`voltage`/`current`/`capacity`/`power`/`soc`/`soh`/`temperature`/GPOUT config/etc.). Every sensor on this board is asleep by default — added `INKPLATE_BUZZER`/`ACCELEROMETER`/`BME688`/`APDS9960`/`FUEL_GAUGE` bitmask constants and `wake_peripheral`/`sleep_peripheral` classmethods; `begin()` now ends asleep, matching real hardware default. Known, deliberately-unfixed quirk: `pins.h` has `INKPLATE_FUEL_GAUGE` colliding with `INKPLATE_BUZZER`'s bitmask (kept as pasted, per user's explicit call). Wired into `inkplate4tempera.py`: `read_battery()`/`read_battery_soc()` delegation. Fixed a real bug in `shared/pcal6416a.py`'s `pin_mode()`: pull-up/pull-down modes set the pull-select register but never the pull-enable register, so both modes actually configured a floating input — fixed by also setting the pull-enable bit. **Verify (HIL)**: `wake_peripheral(INKPLATE_FUEL_GAUGE)` brings the chip up, `device_type()` reads back the correct device ID, and `voltage()`/`soc()` return plausible readings, on real Inkplate4TEMPERA hardware.
38. **[DONE, HIL-verified]** Ported the real Bosch `bme68x` driver (vendored in-repo) via Soldered's wrapper, scoped to only the slice the wrapper exercises: I2C only, forced mode only, one fixed TPH/filter/heater config applied once at `begin()`, floating-point compensation only. New `shared/bme688.py` (`BME688`): `init`/`begin`/`read_temperature`/`read_pressure`/`read_humidity`/`read_gas_resistance`/`read_sensor_data`/`read_altitude`. Unlike the fuel gauge, BME688 has no true I2C-shutdown mode — `sleep_peripheral` is a bit-clear only, `begin()`'s real chip bring-up only happens inside `wake_peripheral`. Real bug found via HIL, in the port itself: oversampling was landing as 0 (skipped) instead of the intended 16x, since this chip's register map does not auto-increment through a plain multi-byte burst write the way the PCAL6416A/MCP4018 on this board do — the real driver interleaves `[addr,data]` pairs into one write transaction instead; fixed with a new `_write_regs()` helper replicating that. Wired into `inkplate4tempera.py`'s `begin()` plus `read_temperature_bme()` delegation. **Verify (HIL)**: post-fix, `ctrl_meas`/`ctrl_hum` read back the expected oversampling setting, and `read_sensor_data()` returns plausible real-world temperature/humidity/pressure/gas readings on real Inkplate4TEMPERA hardware.
39. **[DONE, HIL-verified]** Ported the real SparkFun APDS9960 library (vendored under Soldered's wrapper) — full API surface (gesture detection + proximity + ambient light/color + interrupt config) per explicit user request, not a trimmed subset. New `shared/apds9960.py`; repeated register-accessor bitfield boilerplate collapsed into `_get_bits`/`_set_bits` helpers. Gesture detection's FIFO-buffered ratio-delta state machine ported as literally as MicroPython allows, including its C-style truncate-toward-zero integer division (`_c_idiv`). Real bug found in the vendored reference itself, not ported: `wireWriteDataBlock()`'s inner loop calls the wrong Wire method — dead code, never called from anywhere else. APDS9960's `wake_peripheral` branch only powers the chip on (matching the real driver, which leaves chip configuration to the caller) — unlike BME688's full reinit. Wired into `inkplate4tempera.py`'s `begin()` plus `read_proximity()`/`read_gesture()` delegation. **Verify (HIL)**: `begin()` passes its chip-ID check, `enable_proximity_sensor()`/`enable_light_sensor()` return plausible readings, and `read_gesture()` returns cleanly (no hang) across repeated calls with real hand-waving, on real Inkplate4TEMPERA hardware.
40. **[DONE, HIL-verified]** Ported the real SparkFun LSM6DS3 library (vendored under Soldered's wrapper) — full accel/gyro/temperature/FIFO API, the last of the five sensors named in this board's pins.h. New `shared/lsm6ds3.py`; settings are plain class attributes instead of the real driver's settings-struct-by-pointer. Unlike BME688, this chip supports plain auto-increment multi-byte I2C reads/writes, so no interleaved-pair write trick is needed. `wake_peripheral`/`sleep_peripheral` both call the same `begin()` function with enable flags toggled, a third distinct bring-up shape from BME688's and APDS9960's. Wired into `inkplate4tempera.py`'s `begin()` plus `read_accelerometer()` delegation, completing this board's sensor set. **Verify (HIL)**: `WHO_AM_I` reads back the correct device ID, and at-rest accelerometer/gyro/temperature readings are all physically plausible (accel magnitude ~1g, small gyro bias, few-degrees-above-ambient temperature) on real Inkplate4TEMPERA hardware.

## Phase 12 — Test hardening (threaded throughout, not just at the end)

36. **[DONE]** Promote the host-compiled unit tests from Phases 1/4/5/7/9/10 into a native test target (board-config, waveform LUT, framebuffer packing, image decode/dither, SPI config). **Verify**: tests catch a deliberately introduced regression.
37. Documented, repeatable manual HIL checklist per board (clear, mono text, grayscale ramp, photo, partial update, touchscreen/frontlight where applicable). Not automated, but standardized across releases. **Verify**: checklist exists, covers every board and check type, and each board's row gets checked off only once actually flashed and eyeballed — see "Manual HIL checklist" near the end of this document.
38. **[DONE, HIL-verified]** Merge each parallel-bus board's `<board>_partial.py`/`<board>_gs.py`/`<board>_mono.py` into one file per board, now that Phase 8 has reduced all of them to thin C-binding wrappers — fewer files to keep in sync when iterating. **Verify (HIL)**: each board's examples still render correctly post-merge.
39. **[DONE, HIL-verified]** Reorganize `firmware/usermods/inkplate/`'s flat layout (31 files and growing) into subfolders before it gets worse — `image/` for the bmp/jpeg/png decode+draw pairs, pngle, and dither.c; `display/` for epd_bitbang, epd_i2s, epd_partial_lut, waveform, board_config, expander_bridge, gfx; `inkplatemodule.c`/`micropython.cmake` stay at root as the module entry point. **Verify**: CI build/test/format jobs stay green; HIL smoke-test one board post-move to confirm nothing broke.
40. **[DONE, HIL-verified]** Audit every board's `package.json` install manifest for drift — `shared/` dedup (step 27) and the per-board file merge (step 38) both change which files each board actually needs. **Verify (HIL)**: fresh `mip install` of each board pulls exactly the files it needs, nothing stale, nothing missing.
41. **[DONE, HIL-verified]** Unify `write_pixel`/`draw_bitmap` onto the C drawing path — deferred explicitly in Phase 7 step 17 (`gfx_set_pixel` is a second, C-side reimplementation of the same per-pixel logic `write_pixel_viper` already does, kept separate at the time rather than risk a regression mid-port). Revisit now that the rest of the drawing/image pipeline is fully C-side: replace `write_pixel_viper` with a thin call into `gfx_set_pixel`, and give `draw_bitmap` a real C bitmap-blit primitive (source bitmap + w/h + dest x/y/color, looping entirely in C) instead of its current per-set-bit `write_pixel` Python loop. **Verify (HIL)**: bitmap/pixel-level examples render identically; timing improves for `draw_bitmap` the same way `write_fill_rect` did in step 17.
42. **[DONE]** Make `ci.yml`'s build/test jobs self-discovering instead of hand-listed — every new host-testable `.c` file currently needs a manually-added `gcc -c ...` line (build job) and matching test-run block (test job), and forgetting one has caused two real bugs already (steps 11 and 14 both forgot to wire a new file into `micropython.cmake`/the test job, only caught later). Replace the hardcoded per-file lines with something that globs `firmware/usermods/inkplate/**/*.c` for the build job and pairs each `tests/test_*.c` with its same-named source for the test job. **Verify**: CI still catches a deliberately broken/omitted file the same as before; adding a new source file requires no `ci.yml` edit.
43. **[DONE, HIL-verified]** Prune the `display_image_sd_{jpeg,png,bmp}_c.py` examples now that they're redundant with the high-level API — they were written as placeholders demonstrating the raw `jpeg_draw_gs4`/`png_draw_gs4`/`bmp_draw_gs4` bindings directly "ahead of step 21," but `draw_jpg_from_sd`/`draw_png_from_sd`/`draw_bmp_from_sd` now call those same bindings, making the low-level examples mostly duplicate demonstrations. Keep one example per format (jpeg/png/bmp stay as three separate files, not merged into one combined example), just updated to show the real `draw_*_from_sd` high-level call instead of the raw binding. **Verify (HIL)**: all three updated examples still render correctly.
44. Clean up code comments across `firmware/usermods/inkplate/*.c` and the board `.py` files — this refactor's own working style has been to narrate real-bug-found-on-real-hardware stories inline as they happened (useful mid-refactor, since each step had to re-derive context fresh), but a lot of that is commit-message/plan-doc material, not something a future reader needs sitting in the source permanently. Pass to trim comments down to WHY-only (hidden constraints, subtle invariants, non-obvious workarounds), cutting narrative history, "step N found X" callouts, and anything a well-named identifier already says. **Verify**: `git diff` is comment-only (no logic changes); CI still green.

    - **Scope decision on this plan doc itself**: the per-step HIL/verification narrative was trimmed out of this document and discarded (not archived elsewhere) once the work it described was long done, matching the same WHY-only-comment philosophy this same step applies to the source code. `docs/hil_checklist.md`'s manual per-board checklist was folded into this same file's "Manual HIL checklist" section rather than kept as a separate file.
    - **Convention: no vendor/Arduino porting provenance.** Comments must not name or point to the Arduino/vendor source a driver was ported from (Arduino, SparkFun, Bosch, Soldered, ST, `.cpp`/`.h` filenames, "ported from", "the real driver", etc.), and no manufacturer-name prefixes in chip descriptions (`BME688`, not `Bosch BME688`). Where such a comment carried real substance (HIL-confirmed hardware facts, register quirks, dead code kept on purpose), reword to keep the substance and drop the attribution — don't just delete the comment.
    - **Convention: PEP 8 comment capitalization/punctuation.** Capitalize the first word unless it's a lowercase code identifier (`mode_output`, `os.stat`, `n`) — never alter identifier case. Full-sentence/block comments end with a period; short inline trailing fragments (`# Bank selector`, unit labels like `# ms`) may omit it.
    - **Convention: Doxygen-style function documentation (C only).** Every function gets a Javadoc-style block (`/** @brief ... @param ... @return ... */`). For files with a header, the block goes on the declaration in the `.h` only — not duplicated above the `.c` definition, to avoid two copies drifting apart. For a `.c` file with no matching header (`inkplatemodule.c`), the block goes directly above the definition. Struct/typedef fields, macros, and enum values keep plain WHY-only trailing comments (not Doxygen blocks) — this convention covers functions only. Same capitalization/punctuation rule above applies inside the Doxygen block text. Every `.c`/`.h` file also gets a top-of-file `/** @file name.ext \n * @brief ... */` block (the existing WHY-only file banner becomes the block's `@brief`/detail text) — `.h` files already carried a banner to convert; `.c` files that had none got a new short one added, so every physical file is independently discoverable in generated Doxygen output.
    - **Progress**: `shared/*.py` and all 9 board `.py` files (`boards/*/*.py`) done — comment trim (WHY-only), vendor-provenance strip, and PEP 8 capitalization pass all applied, AST-verified (docstrings excluded) identical to pre-pass logic, and syntax/`ruff format`-checked. Also fixed a real stale comment found during this pass: `inkplate6flick/inkplate6_flick.py`'s "KNOWN ISSUE: chained display-affecting operations unreliable" comment described a bug that a later HIL retest confirmed was never real (a testing-methodology mistake, not a firmware fault) -- removed rather than kept as a guardrail, since it no longer describes current behavior.
    - **Progress (C files)**: `firmware/usermods/inkplate/display/*.{c,h}` and `image/*.{c,h}` (excluding the vendored `image/pngle.{c,h}`) plus `inkplatemodule.c` done — same WHY-only trim/vendor-provenance strip/capitalization-punctuation pass, plus the new Doxygen convention below applied to every function. `tests/*.c` left untouched (host-test harnesses, not public API, no header to doc against). Verified comment/doc-only: `clang-format-18 --dry-run --Werror` clean on every touched file, `run_ci.py build`+`test` both green (11/11 test binaries), and a line-level diff audit confirmed every changed source line differs only in its trailing/leading comment text, not code.
45. Update `README.md` (currently stale — doesn't list Inkplate5v2/6FLICK/13SPECTRA) once the new architecture stabilizes. **Verify**: README lists all current boards (Inkplate5v2/6FLICK/13SPECTRA included) and matches the current architecture.
46. **[DONE]** Delete dead `byte2gpio`/`gen_byte2gpio` from all 6 parallel-bus boards, and unify `InkplateMono.clear()`/`InkplateGS2.clear()` onto a shared C binding. **Verify**: `ast.parse`/`ruff format --check`/`run_ci.py build`+`test` all clean; `git diff` shows no behavior change (`clear()`'s caller signature unchanged).
47. **[DONE, HIL-verified]** The 3 SPI-color boards (`boards/inkplate6color/gfx.py`, `inkplate13spectra/gfx.py`, `inkplate2/gfx.py`) never got the shape/text-drawing C port Phase 7/8 gave the 6 parallel-bus boards — each still carried its own ~520-608 line, largely-duplicated pixel-callback `GFX` class. Only their image-decode/dither path went through the Phase 10 C generalization (`spi_panel_palette.c`); shapes/lines/circles/text for these 3 boards were still pure Python, triplicated. **Verify (HIL)**: shape/line/circle/text drawing on Inkplate6COLOR/Inkplate2/Inkplate13SPECTRA all render correctly through the shared C `gfx_*` bindings, matching their pre-port Python output.
    - All 3 boards' shape/text/bitmap drawing now calls `inkplate.gfx_*` directly, same shape as the 6 parallel-bus boards. Each board's own `set_rotation()` already swaps its logical width/height for odd rotations, so each board's rotation scheme reduces to a fixed permutation of `gfx_set_pixel`'s existing rotation parameter: Inkplate2 and Inkplate6COLOR use `gfx_rotation = (board_rotation + 2) % 4`; Inkplate13SPECTRA uses the mirrored `gfx_rotation = (2 - board_rotation) % 4`. Each board computes this once into a `_gfx_rotation` class attribute, kept separate from the board's own public `rotation` attribute since the SPI palette image-decode path still expects native rotation numbering.
    - **Inkplate2**: reuses `gfx_set_pixel` via `display_mode=0`, called twice per shape (once per BW/RED bitplane) from a `_plane_colors(c)` helper.
    - **6COLOR/13SPECTRA**: both reuse `gfx_set_pixel`'s existing 4bpp branch directly, using the already-existing `gfx_set_gs4_nibble_swap(True)` toggle (the same one Inkplate4TEMPERA's `init()` calls) since both store color with the opposite nibble parity from `gfx_set_pixel`'s default. 13SPECTRA additionally maps its 0-5 color index through `_color_palette` once per `gfx_*` call.
    - Deleted all 3 boards' `gfx.py` and the matching `package.json` manifest entries; each board now imports `gfx_standard_font_01` directly, same pattern as the 6 parallel-bus boards.

---

## Critical files (starting points)

- `boards/inkplate10/inkplate10.py` — canonical reference: pin/mask constants, `vscan_*`, `byte2gpio`, `fill_screen`, `Inkplate` API, image loaders. Source of truth for the C interface contract and board-config struct.
- `boards/inkplate10/inkplate_gs.py` — 2-bit waveform/LUT generation (`_gen_wave`/`genlut`) — primary target for the N-bit engine and 2→3-bit switch.
- `boards/inkplate10/inkplate_mono.py` — 1bpp waveform + row shift-out — the walking-skeleton target (Phase 4).
- `boards/inkplate10/inkplate_partial.py` — partial-update diff/skip logic (Phase 6).
- `firmware/usermods/inkplate/gfx.c` — pixel-callback drawing primitives (C port landed Phase 7 step 17, extended to all 6 parallel-bus boards by step 27); `shared/gfx.py`/`shared/shapes.py` deleted (step 27) once nothing imported them anymore.
- `shared/pcal6416a.py` — shared I2C GPIO-expander driver, stays Python-side per Phase 1 decision.
- `shared/tps65186.py`, `shared/rtc.py` — shared PMIC/RTC drivers (step 27), stay Python-side, same reasoning as `pcal6416a.py`.
- Same 4-file pattern under `boards/inkplate6/`, `boards/inkplate5v2/`, `boards/inkplate6flick/` for Phase 8.
- `boards/inkplate6color/inkplate6_color.py`, `boards/inkplate2/inkplate2.py`, `boards/inkplate13spectra/inkplate13_spectra.py` — inline SPI command sequences (Phase 9); image decode/dithering (Phase 10) now thin wrappers over `inkplate.{bmp,jpeg,png}_draw_palette`.
- `firmware/usermods/inkplate/spi_panel_palette.{c,h}` — Phase 10's per-board palette tables + packing/rotation, kept separate from `dither.c`/`bmp_draw.c` etc. (which stay board-agnostic) and from `board_config.c`/`spi_panel_config.c` (pins/transport, not pixel format).
- `shared/touch_cypress.py` — placeholder touch logic (Phase 11), Inkplate6FLICK-only for now.

## Verification approach summary

Every task falls into one of three checks: (a) build/CI check for scaffolding, (b) host-compiled C unit test for pure logic (LUT generation, config struct, framebuffer packing) — runnable without hardware, (c) hardware-in-the-loop check on real Inkplate hardware comparing against the Phase 0 baseline. No task is marked done on "should work" — timing and visual output get compared against baseline photos/logs at each hardware checkpoint.

## Manual HIL checklist

Repeatable smoke-test run by hand on real hardware, per board, per release.
Not automated (e-paper visual/bistable behavior can't be asserted in a host
test) — this catches what `run_ci.py test` structurally can't.

Not yet run against real hardware as of step 37 above — checklist authored
ahead of first real pass; check off / date each board's row only once
actually flashed and eyeballed.

### Per-board checks

Parallel-bus mono/grayscale boards (Inkplate10 v1/v2, Inkplate6 v1/v2, 5v2,
6FLICK, 6PLUSv2, 4TEMPERA): clear/mono/grayscale/photo/partial all apply.

SPI color-panel boards (Inkplate2, 6COLOR, 13SPECTRA): palette-driven,
full-refresh-only — grayscale ramp and partial update are N/A.

6FLICK, 6PLUSv2, 4TEMPERA additionally have a touchscreen + frontlight
(Elan touch controller on PLUSv2/4TEMPERA, Cypress on 6FLICK) — extra columns,
N/A elsewhere.

| Board             | Clear | Mono text | Grayscale ramp | Photo (dithered) | Partial update  | Touchscreen | Frontlight |
|-------------------|-------|-----------|----------------|------------------|-----------------|-------------|------------|
| Inkplate10v1      | [x]   | [x]       | [x]            | [x]              | [x]             | N/A         | N/A        |
| Inkplate10v2      | [x]   | [x]       | [x]            | [x]              | [x]             | N/A         | N/A        |
| Inkplate6v1       | [x]   | [x]       | [x]            | [x]              | [x]             | N/A         | N/A        |
| Inkplate6v2       | [x]   | [x]       | [x]            | [x]              | [x]             | N/A         | N/A        |
| Inkplate5v2       | [x]   | [x]       | [x]            | [x]              | [x]             | N/A         | N/A        |
| Inkplate6FLICK    | [x]   | [x]       | [x]            | [x]              | [x]             | [x]         | [x]        |
| Inkplate6PLUSv2   | [x]   | [x]       | [x]            | [x]              | [x]             | [x]         | [x]        |
| Inkplate4TEMPERA  | [x]   | [x]       | [x]            | [x]              | [x]             | [x]         | [x]        |
| Inkplate2         | [x]   | [x]       | N/A            | [x]              | N/A             | N/A         | N/A        |
| Inkplate6COLOR    | [x]   | [x]       | N/A            | [x]              | N/A             | N/A         | N/A        |
| Inkplate13SPECTRA | [x]   | [x]       | N/A            | [x]              | [x]             | N/A         | N/A        |

### Check definitions

- **Clear** — `display.clear()` (or equivalent), screen goes to blank white, no ghosting from prior content.
- **Mono text** — draw text, confirm crisp black-on-white, no missing/garbled glyphs.
- **Grayscale ramp** — draw all supported gray levels side by side, confirm each level visually distinct, no banding/reversal (see `mono_waveform_polarity_per_board` — a board's GS levels can come out inverted if LUT roles are wrong).
- **Photo (dithered)** — draw a real photo via `draw_jpg_from_sd`/`draw_png_from_sd`/`draw_bmp_from_sd`, confirm recognizable image, correct orientation/rotation.
- **Partial update** — change a small region, confirm only that region refreshes, fast, no visible flash/ghosting outside the changed area.
- **Touchscreen** — touch a known region, confirm `touch_in_area` reports the touch.
- **Frontlight** — `set_frontlight(True)`/`set_frontlight_brightness(v)`, confirm light turns on and brightness visibly changes.
