@ -0,0 +1,108 @@
# Inkplate-micropython: C/I2S/3-bit Grayscale Refactor

## Context

Repo is currently pure Python, distributed via `mip install` on top of two prebuilt firmware `.bin` files whose build source doesn't exist anywhere accessible — confirmed via exhaustive search (zero `.c`/`.h` files, no CMake/Makefile/manifest.py, no CI, no tests). All "performance-critical" code today is `@micropython.viper`-decorated Python doing raw ESP32 GPIO register bit-banging to drive the e-paper parallel bus, duplicated near-identically across 4 board packages (Inkplate6, 10, 5v2, 6FLICK — ~1700-2000 lines each, ~90% identical). Grayscale is 2-bit/4-level; full 3-bit/8-level waveform tables from the Arduino reference driver already sit in code as unused comments.

Goal: move the hot path into C, drive the panel via ESP32 I2S+DMA instead of bit-banged GPIO (classic ESP32 I2S0 supports parallel LCD-style output — same technique as the open-source `vroland/epdiy` project, useful prior art), upgrade grayscale to 3-bit/8-level, and consolidate the 4 duplicated board packages into one C driver + thin per-board Python config. User will build the MicroPython+ESP-IDF firmware from scratch (no existing build source to reuse) and has real hardware for most/all variants to verify each step. Work must be small, independently-testable tasks — no big-bang rewrites.

Confirmed in-repo facts anchoring this plan (spot-checked directly, not just from exploration agents):
- `inkplate10.py:40-42` — full Arduino `WAVEFORM3BIT[8][8]` table already transcribed as a comment, unused.
- `inkplate10.py:186-187` — `_tps65186_read` is missing `return`, silently discards the read value (`cls._temperature` always becomes `None`). Duplicated in sibling board files. Real bug, fix during consolidation.
- Pin/control-line layout (`EPD_DATA=0x0E8C0030`, CL=GPIO0, LE=GPIO2, CKV=GPIO32, SPH=GPIO33, OE/GMODE/SPV via PCAL6416A I2C expander) is identical across Inkplate6/10/5v2/6FLICK — only resolution and waveform timing differ. This is why a single config-driven C driver is realistic, not speculative.
- SPI-controller-chip panels (Inkplate6COLOR, Inkplate2, Inkplate13SPECTRA — the last on ESP32-S3) are architecturally unrelated to the bit-banged bus; I2S migration doesn't apply to them. Separate track.
- `Dependencies/mcp23017.py` is dead code (no board imports it).
- `touch_cypress.py` (Inkplate6FLICK) has self-documented placeholder/incomplete logic in `tsGetXY`/`tsGetResolution`.
- Inkplate4TEMPERA and Inkplate6PLUS are in-scope per user, both confirmed classic-ESP32 parallel-bus boards (same family as Inkplate6/10/5v2/6FLICK), not yet present in this repo — user will supply pin map/waveform data; slotted into Phase 8 alongside the other parallel-bus boards.

## Sequencing strategy

Firmware build (MicroPython + ESP-IDF) is the user's own responsibility — not a plan focus here, only touched where the C-porting work needs a hook into it (`USER_C_MODULES` scaffold, board variant/partition sizing). Priority is the C port itself. Bring up ONE board (**Inkplate10** — best-commented reference, largest/most demanding panel, hardware available) through the full new stack first, with de-risking stepping stones, before generalizing to the other 5 parallel-bus boards (Inkplate6, 5v2, 6FLICK, 6PLUS, 4TEMPERA — all classic ESP32) via config only. Build the board-config abstraction early (not deferred) since the pin/control layout is already confirmed near-identical across boards — designing it now avoids Inkplate10-specific assumptions leaking into "shared" code later. SPI-controller panels (Inkplate6COLOR, Inkplate2, Inkplate13SPECTRA) are also in scope for the C refactor, not just an optional assessment — see Phase 9. Every task has a concrete build/unit-test/hardware-in-the-loop (HIL) verification. Tasks needing hardware values not currently known (per-board 3-bit waveform timing, Cypress touch protocol, SPI panel command sets, 4TEMPERA/6PLUS specs) are flagged **[NEEDS INPUT]** — user will supply these (from the Arduino reference driver, datasheets, or hardware specs) per-task, don't guess.

---

## Phase 0 — Git workflow + baseline

1. **[DONE]** **Git workflow**: regular commits directly to the existing `refactor` branch, no PR/branch-per-task ceremony. Follow commit conventions specified in CLAUDE.md. Add a GitHub Actions workflow running on every push: (a) build check — compile the C module/firmware once the toolchain exists, (b) format check — `clang-format` for C, `black`/`ruff` for the Python config layer, (c) unit test runner — the host-compiled C tests introduced in later phases (board-config, waveform LUT, framebuffer packing). No hardware in CI. **Verify**: workflow runs green on a trivial commit, red on a deliberately broken one. **Verified**: workflow runs green on real pushes.
2. **[DONE]** **Capture baseline on real Inkplate10**: run existing mono + GS2 examples on current firmware, record timing (code already logs render times). This is the regression oracle for every later step. **Measured** (`basic_grayscale.py` total runtime via added timer): Inkplate10 13147 ms, Inkplate6 20185 ms (bonus board, not required by this task).
3. **[DONE]** **Restructure repo layout for the C port**: flat top-level layout, no `src/` wrapper (this repo ships multiple independent mip-installable board packages, not one pip-built package, so the CPython `src/`-layout rationale doesn't apply). `boards/<board>/` per board, collapsing the current double-nested `<board>/<board>/*.py` into a single level. `shared/` (renamed from `dependencies/`) for the shared Python (gfx/shapes/PCAL6416A) that survives consolidation. Reserve `firmware/usermods/inkplate/` as the future home for the shared C driver component (empty scaffold until Phase 1+, matches ESP-IDF's `USER_C_MODULES`/`EXTRA_COMPONENT_DIRS` convention — kept outside `boards/` since the C driver is one shared component, not per-board). `examples/` and `docs/` stay top-level. Update each board's `package.json` to the new `boards/<board>/` and `shared/` paths — breaking the current mip install paths is accepted now, not deferred, since this repo has no external users yet. **Verified**: new tree in place; `mip install github:SolderedElectronics/Inkplate-micropython/boards/inkplate6@refactor` pulled cleanly on real hardware, `import inkplate6` succeeded.
4. **[DONE]** Once user has a self-built MicroPython+ESP-IDF firmware available: confirm PSRAM is enabled/detected (GS buffers are ~250KB+, must live in PSRAM) and add a `USER_C_MODULES` scaffold with one trivial function (e.g. `inkplate.version()`). **Verify (HIL)**: importable from REPL, returns expected value — proves C build/link/registration before any driver logic exists. This is the only firmware-build-adjacent task in this plan; everything else assumes the firmware toolchain is a given. **Verified**: PSRAM confirmed on ESP32_GENERIC/SPIRAM build; `firmware/usermods/inkplate/` scaffold (`micropython.cmake` + `inkplatemodule.c`) added; `import inkplate; inkplate.version()` returns `'0.0.1'` on real hardware.

## Phase 1 — Board-config abstraction (data only, no transport)

5. **[DONE]** Define a C `board_config` struct: resolution, data-bus pin list, control pins (CL/LE/CKV/SPH), PCAL6416A pin assignments (OE/GMODE/SPV), PMIC I2C address, waveform table pointer, feature flags (touch/frontlight). Populate the Inkplate10 instance from confirmed constants above. **Verify**: host-compiled C unit test instantiates config, asserts fields match known Inkplate10 values. **Verified**: `firmware/usermods/inkplate/board_config.{h,c}` + `tests/test_board_config.c`, host-compiled with gcc, all assertions pass; CI `build`/`format`/`test` jobs wired to real checks (were placeholders since Phase 0).
6. **[DONE]** Decide + implement how C reaches the PCAL6416A-controlled lines (OE/GMODE/SPV — not in the hot per-row loop, so keep `pcal6416a.py` in Python, expose a clean C-callable interface, revisit only if timing demands otherwise). Design: C holds a Python callback (`cb(addr, pin, value)`) registered at init, invoked via `mp_call_function_n_kw` — keeps the I2C bus single-owner (Python also owns PMIC/RTC on the same bus per Phase 8 step 27). Implemented as `firmware/usermods/inkplate/expander_bridge.{h,c}`, bound into the module as `inkplate.set_expander_write_cb(cb)` / `inkplate.test_expander_write(addr, pin, value)`. **Verify (HIL)**: OE/GMODE/SPV are wired to the panel, not probeable directly, so verify the bridge mechanism itself on a free/unused expander pin instead (`_PCAL6416A_2`/addr 0x21, unused by any board function, or addr 0x20 pins 11-15) via multimeter/logic analyzer/LED. Confirm the real OE/GMODE/SPV path separately via observed PMIC/panel behavior (`power_on`/`power_off` still work correctly through the bridge). **Verified**: toggled addr-0x21 pin 0 HIGH/LOW/HIGH on real Inkplate10 via `inkplate.test_expander_write` through the registered Python callback — full path (C `mp_call_function_n_kw` → Python callback → `PCAL6416A.digitalWrite` → I2C write) confirmed working. OE/GMODE/SPV use the identical call path (same `expander_bridge_write`, only `addr`/`pin` differ) — mechanism verified generically, no separate per-pin test needed; real OE/GMODE/SPV wiring stays covered by observed `power_on`/`power_off` behavior once Phase 2 routes through the bridge.

## Phase 2 — De-risk: bit-bang control lines in C (no DMA yet)

7. **[DONE]** Port `vscan_start/write/end` and `fill_screen` (currently viper) to C, using the known-good bit-bang method — no I2S yet. Config-driven from Phase 1's struct. **Verify (HIL)**: mono example renders identically to Phase 0 baseline, now executing through C. Isolates "C can drive the panel at all" from "does I2S DMA work" before combining both unknowns. **Verified**: `firmware/usermods/inkplate/epd_bitbang.{h,c}` implements `epd_vscan_start/write/end`/`epd_fill_screen`, bound into the module (`inkplate.vscan_start()` etc.) and called from `boards/inkplate10/inkplate10.py`. Real bug found+fixed on real hardware: C port dropped the `delayMicroseconds(0)` calls the Arduino reference has around CKV transitions in `vscan_start`/`vscan_write` (relied on instead by the old non-viper Python for its call-overhead margin) — panel showed nothing until `esp_rom_delay_us(0)` was restored at those points, confirmed against `~/Documents/GitHub/Inkplate-Arduino-library`. `basic_bw.py` mono `display()` runs correctly through the C path on real Inkplate10: 1785ms total (1343ms clean + 442ms draw), partial updates 454ms/frame (110us/row) — both confirmed visually correct on the panel.

## Phase 3 — I2S parallel transport

User will supply the I2S configuration in C directly (classic ESP32 I2S0 LCD/parallel-mode DMA setup) — no research/design-doc task needed here, just integration.

8. **[DONE]** Integrate user-supplied I2S config into a minimal spike: hardcoded-for-Inkplate10 single-row DMA transfer, framed by manual SPH/LE/CKV pulses. Explicitly throwaway. **Verify (HIL)**: full-frame hardcoded pattern via I2S visibly reaches the real panel (comparable to Phase 2's `fill_screen` check). Confirm whether GPIO matrix routing (per the supplied config) makes the current `byte2gpio` scatter/gather remap unnecessary. **Verified**: `firmware/usermods/inkplate/epd_i2s_spike.{h,c}` ports the Arduino reference driver's `UtilI2S::I2SInit`/`sendDataI2S` (I2S1, not I2S0 -- I2S0 doesn't support 8-bit parallel/LCD mode on classic ESP32, contra this section's intro), wired via `esp_rom_gpio_connect_out_signal` mapping `data_pins[i]` 1:1 to `I2S1O_DATA_OUT<i>_IDX` and `pin_cl` to `I2S1O_BCK_OUT_IDX` -- no `byte2gpio` remap needed, direct matrix routing works. Bound as `inkplate.i2s_spike_init()`/`i2s_spike_frame()`, HIL-tested on real Inkplate10: a single raw pass showed data reaching the panel (fine stripes) but an incomplete frame-tail (undeveloped white band); repeating the full pass 8x (matching how the real Arduino driver always repeats `vscan_start`->rows->`vscan_end` 4-18x per waveform phase with GLUT-encoded data, never a single raw pass) made the white band disappear entirely -- confirms the I2S transport itself works, the single-pass artifact was expected given no real waveform encoding is used yet. A secondary run-to-run-variable contrast artifact appeared under repetition; its non-determinism rules out a fixed hardware/panel-driver split and points instead at DC/charge-balance drift from repeating a non-waveform-encoded raw pattern -- expected and not investigated further here, since real waveform encoding (Phase 4/5) is what actually produces deterministic output.
9. **[DONE]** Generalize the spike into a real `inkplate_i2s` component: init/deinit, PSRAM DMA buffers, `push_row`/`push_frame` primitives, double-buffered (compute next row while current transfers), parameterized by the Phase 1 board config. **Verify (HIL)**: full-screen clear via I2S matches Phase 2's bit-bang clear (same visual result), timing recorded as new baseline. **Verified**: `firmware/usermods/inkplate/epd_i2s.{h,c}` replaces the throwaway spike -- `epd_i2s_init`/`epd_i2s_deinit` (deinit reconnects `data_pins`/`pin_cl` back to plain GPIO via `SIG_GPIO_OUT_IDX`, restoring `epd_bitbang.c`), `epd_i2s_push_row`/`epd_i2s_push_frame(cfg, fill_byte)` ping-pong between 2 internal-RAM DMA buffers. Content stays fill-byte-only (mirrors `epd_fill_screen`'s contract) since the real per-row waveform engine is Phase 4; the double-buffer plumbing is scaffolding for that, not a measurable win yet with constant content. HIL on real Inkplate10 (`i2s_push_frame` vs bit-bang `fill_screen`, both driven with `clean()`'s own pattern bytes -- 0xAA=white, 0x55=black; 0x00/0xFF were tried first and wrongly showed nothing, since the real wire format is 2-bit-per-pixel and 0xFF decodes to all-"skip"/no-op, not "all white" -- confirmed by reading `inkplate_mono.py`'s `_gen_luts`) confirmed visually identical white/black full-screen fills via both transports, 12x-repeat runs (matching `clean()`'s real repeat cadence): bit-bang 324ms vs I2S 132-134ms (~2.4x faster). Bit-bang fill_screen/vscan_* confirmed still working correctly after `i2s_deinit()`, proving the GPIO-matrix restore.

## Phase 4 — Waveform engine + mono display (walking skeleton)

10. **[DONE]** Port `_gen_wave`/`genlut` LUT generation to C, generalized over bit-depth (so 1-bit/2-bit/3-bit all flow through one code path — this is what makes the later 3-bit switch a data change, not a rewrite). **Verify**: host-compiled unit test — generated LUTs match hand-computed values for a synthetic table, and legacy 2-bit path reproduces current Python `_wave` output byte-for-byte. **Verified**: `firmware/usermods/inkplate/waveform.{h,c}` implements `inkplate_gen_nibble_lut(op, bpp, out)`, generalizing `inkplate_gs.py`'s `genlut` (bpp=2) and `inkplate_mono.py`'s per-bit `_gen_luts` pack (bpp=1) into one formula, valid for bpp∈{1,2,4} — 4 is the ceiling since Phase 5 stores 3-bit grayscale in a 4bpp buffer. `inkplate_gen_wave_2bit` wraps it per-phase for the legacy 4-level table. Host-compiled `tests/test_waveform.c`: synthetic-table assertions match hand-derived values; legacy-parity assertions match the real `WAVE_2B`/`genlut()` output from `inkplate_gs.py`, computed independently via a `python3` one-liner, byte-for-byte across all 8 phases. Bug found+fixed in the process: `board_config.c`'s `wave_2b_inkplate10` (from Phase 1, step 5) held fabricated waveform data matching neither the shape nor values of the real `WAVE_2B` — struct was documented as a `[level_from][level_to]` transition table but `genlut`'s real semantics are phase-row × color-column; fixed struct layout/docs and transcribed the real 8-phase table, `test_board_config.c` updated to match. CI (`ci.yml`) build/test jobs extended to cover `waveform.c`/`test_waveform.c` (previously hardcoded to `board_config.c` only).
11. **[DONE]** Full mono (1bpp) display pipeline on I2S transport + waveform engine, Inkplate10. **Verify (HIL)**: text/shapes render clean and ghost-free — **first true end-to-end milestone**: our C module + I2S DMA path, working. **Verified**: `waveform.c`'s `inkplate_gen_mono_wave` (fixed 5x-"push toward black"+1x-"final black/white" op-code scheme, board-independent, transcribed from `inkplate_mono.py`'s old `_gen_luts`) generates the phase LUTs; `epd_i2s.c`'s `epd_i2s_push_mono_frame` drives the per-phase row loop over I2S DMA, bound as `inkplate.mono_display(framebuf)` and called from `InkplateMono.display()` (bracketed by `inkplate.i2s_init()`/`i2s_deinit()` so the still-bit-bang `clean()` calls before/after keep working -- clean() itself moves to I2S in step 12). Two real bugs found+fixed on real Inkplate10 hardware, not just in review: (1) `waveform.c` was never added to `micropython.cmake`'s source list back in step 10 (only ever exercised by host-compiled tests before) -- surfaced as a linker error (`undefined reference to inkplate_gen_mono_wave`) the moment firmware code actually called it; (2) `build_mono_row` walked framebuf bytes in plain last-to-first order, but I2S1's `tx_fifo_mod = 1` ("0A0B_0C0D packing, dual mono single data") reorders byte-pairs internally, so the source row needs the adjacent-byte-pair pre-swapped to compensate -- confirmed by cross-referencing the real Arduino Inkplate6 I2S driver's `display1b()`, which does the identical swap (`dram2`'s nibbles emitted before `dram1`'s). Missing the swap produced a visible column shift on real content, invisible in step 9's constant-fill-byte HIL check (swapping two identical bytes is a no-op). After both fixes: `examples/inkplate10/basic_bw.py` (shapes + bitmap) and `examples/inkplate10/hello_world.py` (text) both render clean and ghost-free on real Inkplate10; mono `draw` time 177-195ms vs the Phase 2 bit-bang baseline of 442ms (~2.4x faster, matching step 9's measurement).
12. **[DONE]** Port `clean()` (pre-refresh flush patterns) to the I2S path. **Verify (HIL)**: matches baseline flush behavior, no new ghosting. **Verified**: `_Inkplate.clean(patt, rep)` (`boards/inkplate10/inkplate10.py`) now loops `inkplate.i2s_push_frame(c)` instead of bit-bang `vscan_start`+`fill_screen`; caller assumes I2S already inited (same precondition as `push_frame`/`push_row`). Real bug found+fixed via HIL, not just review: initial patch only bracketed 2 of 4 real call sites (`Inkplate.clean(self)` wrapper and `InkplateMono.display()`), missing `InkplatePartial.display()` (its trailing `clean(2,2)`/`clean(3,1)` after the bit-bang partial-draw loop) and `InkplateGS2.display()` (`inkplate_gs.py`) — running `examples/inkplate10/basic_bw.py` on real Inkplate10 crashed (`Guru Meditation Error`, `StoreProhibited`, `EXCVADDR 0x0`) inside `epd_i2s_push_frame`'s `memset` the moment `InkplatePartial.display()` called the now-I2S `clean()` without ever calling `i2s_init()` -- confirmed via `xtensa-esp-elf-addr2line` against the flashed ELF, and via a baseline (pre-step-12) re-run of the identical script completing cleanly, isolating the regression to this change. Fixed by bracketing all 4 call sites; `InkplateGS2.display()` needed a *split* bracket (deinit before its still-bit-bang draw loop, reinit after) rather than one wide bracket, since `epd_i2s_init()` reroutes `data_pins`/`pin_cl` through the GPIO matrix to I2S1 -- holding it through the bit-bang loop would silently swallow that loop's direct GPIO register writes (`epd_i2s_deinit()`'s own header comment already flagged this). `examples/inkplate10/low_level_test.py` (standalone Phase 2 diagnostic, no draw loop) got a single bracket. Re-ran `basic_bw.py` end-to-end on real Inkplate10 post-fix: full display, 4x partial update, final full display, all clean, no crash -- mono clean phase now ~915ms via I2S vs ~1334ms bit-bang baseline.

## Phase 5 — Grayscale: 2-bit parity, then 3-bit upgrade

13. **[DONE]** Port `InkplateGS2` (existing `WAVE_2B` table, current 4-level behavior) onto the I2S+engine path — no new gray levels yet, parity check only. **Verify (HIL)**: matches Phase 0 baseline 4-level output, faster. **Verified**: `epd_i2s.c`'s `epd_i2s_push_gs_frame`/`build_gs_row` drive the 8-phase `WAVE_2B` table (via `waveform.c`'s already-written `inkplate_gen_wave_2bit`, sourced generically from `cfg->waveform` so future per-board tables need no rework) over I2S DMA, bound as `inkplate.gs_display(framebuf)` and called from `InkplateGS2.display()` (`inkplate_gs.py`) — `_send_row`/`_gen_wave`/`WAVE_2B`-as-Python-tuple/the raw ESP32 GPIO register constants all deleted, matching step 11's mono cleanup. Two real bugs found+fixed via HIL, not just review: (1) added the C binding but forgot the `_Inkplate.gs_display` Python forwarder (`inkplate10.py`, parallel to `mono_display`) — surfaced immediately as `AttributeError: type object '_Inkplate' has no attribute 'gs_display'`; (2) `build_gs_row`'s first attempt mirrored `build_mono_row`'s byte-pair swap at the wrong granularity (adjacent-pair, chunk-of-2) — rendered with a visible column shift on real Inkplate10. Fixed by cross-referencing the real Arduino Inkplate6 I2S driver (`~/Documents/GitHub/Inkplate-Arduino-library/src/boards/Inkplate6/Inkplate6Driver.cpp`, `display1b`/`display3b`): I2S1's `tx_fifo_mod=1` reordering is actually a chunk-of-4 half-swap (`[o0,o1,o2,o3] -> [o2,o3,o0,o1]`) — `build_mono_row`'s dram2-before-dram1 pattern was already this same rule expressed at the nibble level (one input-byte-pair's 2+2 split); GS2 has no nibble split (1 input byte = 1 output byte) so the same rule was re-derived at the whole-byte level over 4-byte blocks instead. Re-ran `basic_grayscale.py` on real Inkplate10 post-fix: 4 gray bars + Soldered logo render aligned, no shift, no ghosting vs Phase 0 baseline. Also confirmed there's a separate `inkplate10.py`/`inkplate_gs.py` filesystem copy on-device (`:lib/`, synced via `mpremote fs cp`, distinct from the frozen firmware image) — remember to re-sync both after any C rebuild+flash when iterating on this path. Draw time (the only part this step touches — `clean()` was already optimized in step 12) dropped from 1125ms to 349ms per `display()` call (~3.2x faster), consistent with mono's 2.4x finding in step 11; total script runtime in `basic_grayscale.py`'s own timer (13147ms baseline -> ~9585-9662ms) understates this since most of that time is the 25KB embedded bitmap literal parsing, unrelated to the display path.
14. **[DONE]** Switch framebuffer to 3-bit/8-level. `framebuf` has no native 3bpp mode — decide packing (likely reuse `GS4_HMSB` 4bpp storage using 8 of 16 levels, simplest reuse of existing `framebuf` primitives vs. custom 3bpp pack). Update `writePixel` and image-loader color mapping accordingly. **Verify**: unit test — pixel set/read round-trips correctly for all 8 levels; buffer allocates on-target without OOM (check PSRAM budget for 4bpp full-frame buffer, ~495KB at Inkplate10 resolution). **Verified**: `InkplateGS2` (`inkplate_gs.py`) now stores `framebuf.GS4_HMSB` (raw pixel values 0-7, 495KB at Inkplate10 resolution, matching the estimate above exactly), replacing `GS2_HMSB`. `write_pixel_viper`'s GS branch and all three image loaders (`write_row`/BMP, `process_image`/PNG, `write_image`/JPEG, `inkplate10.py`) repacked from 2-bit/4px-per-byte to 4-bit/2px-per-byte, `inv_mask`/dithering `quant_val` rescaled to the 0-7 range. Since the real 8-level waveform table is step 15 (blocked), the C consumer (`epd_i2s.c`'s `build_gs_row`, HIL-verified in step 13) would otherwise need re-deriving for the new storage shape — instead added `firmware/usermods/inkplate/gs_pack.{h,c}` (`inkplate_gs4_row_to_gs2`), a pure-logic bridge folding each GS4 row to the old 4-level `WAVE_2B` shape (`raw >> 1`) so `build_gs_row`'s already-verified I2S byte-order handling stays untouched until step 15. Host-compiled `tests/test_gs_pack.c` covers all 8×8×8×8 raw-level combinations plus a multi-byte-row case, wired into `ci.yml` and `micropython.cmake` (remembering step 11's lesson about forgetting the CMake source-list entry). `examples/inkplate10/basic_grayscale.py`'s hardcoded fill levels updated (0,1,2,3 → 0,2,4,6) so the interim `raw >> 1` fold reproduces the exact same 4 visible bars. Rebuilt firmware (`make ... clean` first, since a new `.c` file was added) and reflashed real Inkplate10, resynced `:lib/inkplate10.py`+`:lib/inkplate_gs.py` (per step 13's on-device-copy gotcha): `basic_grayscale.py` ran with no crash — 4 gray bars + Soldered logo visually confirmed correct, no column shift, no ghosting, matching pre-step-14 output exactly. Draw time rose from step 13's 349ms to ~749-750ms per `display()` call — expected, from the added per-row GS4→GS2 conversion pass; step 15 removes this bridge entirely once real 8-level output lands.
15. **[DONE]** User supplied the real Arduino reference driver source (Inkplate10Driver.cpp/h, pins.h, waveforms.h) directly, superseding the in-repo comment, which turned out to be wrong (8 values/row, not 9, and shifted vs. the real table). Wired in `waveform1[8][9]` (Arduino's compiled-in default, used whenever no per-device EEPROM calibration is present -- 5 alternate presets + EEPROM read exist in Arduino but are out of scope here, single hardcoded table per user decision), transposed to this project's `[phase][color]` row convention (`board_config.c`'s `wave_3b_inkplate10`, cross-checked via an independent python3 transpose, not hand-derived). `waveform.c` gained `inkplate_gen_wave_3bit` (bpp=4 pass-through through the existing generalized `inkplate_gen_nibble_lut`, already anticipated by its own doc comment). `epd_i2s.c` gained a native row builder (`build_gs3_row`/`gs3_combine`, replacing the interim `gs_pack.h` GS4->GS2 fold slated for removal since step 14) plus the `esp_rom_delay_us(230)` inter-phase gap and, in `inkplate_gs.py`, the trailing `clean(3,1)`+`vscan_start()` park pulse -- both matching the real `display3b()`'s full conditioning sequence (the pre-clean sequence was already correct from step 13). Legacy 2-bit engine (`wave_2b_inkplate10`, `inkplate_gen_wave_2bit`, `build_gs_row`, `gs_pack.{c,h}`) deleted as dead code once nothing called it. **Verified (HIL)**: real bug found+fixed on real Inkplate10 -- the first `gs3_combine` cut a literal port of Arduino's high/low-nibble bit assignment, but this project's `write_pixel_viper` packs GS4_HMSB with the *opposite* even/odd-pixel-to-nibble convention from Arduino's `DMemory4Bit` (even-x -> low nibble here vs. high nibble there); the mismatch was invisible inside a solid-color bar (swapping two equal values is a no-op) but produced a one-pixel shift at every gray-level boundary in an 8-bar ramp -- fixed by swapping which nibble each pixel reads from in `gs3_combine`, re-verified with a clean, unshifted 8-bar ramp on the real panel. `examples/inkplate10/basic_grayscale.py` updated to draw all 8 real levels (was the old 4-bar fold-compensated hack) and to place the Soldered logo in the blank strip beside the bars instead of overlapping them. Also fixed an unrelated ESP-IDF v5.5 deprecation warning surfaced during this rebuild (`driver/periph_ctrl.h` -> `esp_private/periph_ctrl.h`). Draw time ~780ms/`display()` call (9 native phases, comparable to step 14's 749-750ms fold-based interim).

    **Follow-up: classic INKPLATE10 (v1) support added [DONE]**, same split pattern step 22's follow-up already established for Inkplate6: user confirmed classic Inkplate10 also uses an MCP23017 internal expander @ 0x20 (V2 uses PCAL6416A there) with external expander @ 0x22 (V2's is @ 0x21, per the real `pins.h`), and also has onboard touchpads (`touchpad.h`: pad1/2/3 -> expander1 pins 10/11/12, same pins V2 repurposes for `SD_ENABLE`). `board_config_inkplate10` (now classic) gained `has_touch=1`; new `board_config_inkplate10v2` struct added (byte-identical otherwise — `pin_oe/gmode/spv`/PMIC addr/waveform/`partial_reps` unchanged, since the real driver's `partialUpdate()`/`display1b()` hardcode `_repeat=5` unconditionally for both variants, unlike Inkplate6 where they genuinely differ). `select_board()` extended with `"inkplate10v2"`; `tests/test_board_config.c`'s single-struct block converted to the same paired-variant loop already used for Inkplate6/6V2. `boards/inkplate10/inkplate10.py` gained the same `_BOARD_VARIANT`/`_IS_CLASSIC`/`_EXPANDER2_ADDR` scaffold as `inkplate6.py` (`_expander1`/`_expander2` renamed from `_PCAL6416A_1`/`_PCAL6416A_2`, `MCP23017` import added, `TOUCH1/2/3` wired classic-only, `SD_ENABLE` V2-only), defaulting to `"inkplate10v2"` to keep the existing shipped default unchanged for current V2 users. Also folded in the same external-expander chip-shortage autodetect fix this session already shipped for Inkplate6 (`i2c.scan()` before constructing `_expander2`, `None`+warning if absent, guarded `_expander_write_cb`/`gpio_expander_pin`) since user confirmed classic Inkplate10 has the same shortage-unit variant. Classic Inkplate10 has no SD-card power MOSFET (real `pins.h`'s `SD_PMOS_PIN` shares expander pin 10 with touchpad pad1 — same finding as classic Inkplate6, user-confirmed rather than assumed) — `init_sd_card`/`sd_card_sleep`/`sd_card_wake` are no-ops on classic. **Verified (HIL)** on two different real classic Inkplate10 units (temporarily flipping `_BOARD_VARIANT` to `"inkplate10"` for testing, reverted after): `begin()` clean on both, `_expander1` at 0x20, touch1/2/3 readable; first unit had its external expander present (0x22 detected, no warning), second unit was itself a shortage unit (0x22 absent, warning printed, no crash) — confirming the autodetect fix works on real classic hardware too. First unit showed only 2 of 4 expected partial updates and GS3 collapsed to apparent black/white with no gray levels; investigated as a possible software regression (ruled out: `has_touch` is the only difference in the C struct dispatch and is unused outside the test file, confirmed via repo-wide grep; `MCP23017`/`PCAL6416A` `digital_write()` do the same single-register I2C write, no timing asymmetry; TPS65186 VCOM register readback returned a plausible-looking non-zero value, -1.45V) — user confirmed the panel itself was faulty; swapping to a second physical unit reproduced clean mono (full-display shapes, no partial-update artifacts) and clean GS3 (distinct 8-level gray ramp), closing out the investigation as a hardware fault, not a driver bug.

    **Second follow-up: variant selection moved to a constructor arg [DONE]**, both Inkplate6 and Inkplate10 -- the module-level `_BOARD_VARIANT` constant (edit the driver source, then reflash/`mip install`) was ergonomically worse than a runtime parameter, and there's no way to auto-detect classic-vs-V2 from hardware (neither MCP23017 nor PCAL6416A exposes an ID/WHOAMI register to probe), so a param was the only option anyway, not a tradeoff. `_BOARD_VARIANT`/`_IS_CLASSIC`/`_EXPANDER2_ADDR` module constants replaced with `_DEFAULT_VARIANT` + `_VALID_VARIANTS` (a validated tuple, raising `ValueError` on typos instead of silently misconfiguring hardware) and per-call `cls._variant`/`_is_classic`/`_expander2_addr` set inside `_Inkplate.init(cls, i2c, variant=_DEFAULT_VARIANT)`; `Inkplate.__init__(self, mode, variant=_DEFAULT_VARIANT)` stores it and threads it through `begin()`. Also renamed the classic variant's selector string for symmetry with V2 (`"inkplate6"`/`"inkplate10"` -> `"inkplate6v1"`/`"inkplate10v1"`, `"inkplate6v2"`/`"inkplate10v2"` unchanged) -- `board_config_inkplate6`/`board_config_inkplate10` C structs and their `.name` fields renamed to `..._inkplate6v1`/`..._inkplate10v1` to match, `select_board()`'s `strcmp` chain and both test files updated. Board-family module names/files (`inkplate6.py`/`inkplate10.py`, still imported as `from inkplate6 import Inkplate`) are unaffected -- only the variant-selector string and the constructor kwarg changed. Both boards default to their V2 variant unchanged; canonical examples (`basic_bw.py`/`basic_grayscale.py`, both boards) gained a comment showing how to pass `variant="inkplate6v1"`/`variant="inkplate10v1"` for classic hardware. **Verified**: both host-compiled tests (`test_board_config`, `test_waveform`) pass; `ruff format`/`py_compile` clean on both driver files. **Verified (HIL)** on real hardware: reflashed and re-tested Inkplate10 V2 (`basic_bw.py`, default variant, unchanged from before this rename -- clean mono + all 4 partial updates, matching pre-refactor timings) and Inkplate10 classic v1 (`Inkplate(mode, variant="inkplate10v1")` via manual exec -- `begin()` clean, `_is_classic=True`, expander1 @ 0x20, touch1/2/3 readable) on two separate physical boards, confirming the constructor-arg threading actually reaches `_Inkplate.init()` and produces the same behavior as the superseded module-constant mechanism.

## Phase 6 — Partial update (Inkplate10)

Moved ahead of board generalization: no cross-board dependency (mono-only, already
config-driven), and finishing Inkplate10's feature set before fanning out to 5 more boards
means Phase 8 generalizes a fully-proven mono+GS+partial stack in one pass instead of
porting partial update separately, later, per board.

16. **[DONE]** Port `InkplatePartial` diffing/shift-out to C, mono only, **over I2S DMA** (not bit-bang) on Inkplate10 — matches how the real Arduino reference driver does it (`Inkplate6Driver.cpp::partialUpdate()`, provided by user), and keeps partial update on the same transport as `mono_display`/`gs_display`/`clean()` instead of being the only bit-banged display path left. **Verify (HIL)**: only changed region refreshes, correct threshold-triggered full refresh, timing compared against the superseded bit-bang attempt below. **Verified**: `firmware/usermods/inkplate/epd_partial_lut.{h,c}` (pure logic, host-tested) builds a 256-entry old-nibble/new-nibble -> 2bpp-code LUT from the real Arduino reference driver's `LUTW`/`LUTB` tables (`GraphicsDefs.h`, user-supplied) via `diffw = old & ~new`, `diffb = ~old & new`, combined per nibble as `LUTW[diffw] & LUTB[diffb]`. Real bug caught during derivation, not by guessing: the reference driver's own `partialUpdate()` computes `diffw = DMemoryNew & ~_partial` and reads as if `DMemoryNew` is "new" and `_partial` is "old", but `DMemoryNew` is only `memcpy`'d from `_partial` at the *end* of the function — during the diff computation it still holds the *previous* display's content, so the names are backwards (`DMemoryNew` is actually old, `_partial` is actually new). Caught by decoding `LUTW`/`LUTB`/`LUT2` bit-by-bit instead of trusting the variable names: with the correction, a black->white transition pushes white(2) and a white->black transition pushes black(1), which is physically sane; taking the names at face value would have silently inverted every partial-update transition. `firmware/usermods/inkplate/epd_i2s.c` gained `build_partial_row`/`epd_i2s_push_partial_frame`, structurally mirroring `build_mono_row`/`epd_i2s_push_mono_frame` (same byte-pair swap for I2S1's `tx_fifo_mod=1` reordering) but combining an (old,new) nibble pair through the 256-entry LUT instead of a single-value 16-entry one; the resulting per-pixel codes get pushed unchanged over I2S DMA `cfg->partial_reps` times (a new board-config field, `= 5` for Inkplate10 — kept from this project's own prior-verified value rather than adopting either Inkplate6 variant's number, since no Inkplate10-specific figure was available), 230us gap after each repeat matching `display3b()`'s inter-phase delay. No per-region shortcut: the real reference driver's `partialUpdate()` takes no x/y/w/h and always walks the full frame (unchanged pixels get skip-code(3) from the diff, not a transmission shortcut) — `InkplatePartial.display()`'s signature dropped `x/y/w/h` entirely to match (the bit-bang version's `x`/`w` were already unused vestiges; `y`/`h` became unused too once I2S removed the row-skip fast path). Bound as `inkplate.partial_display(old_fb, new_fb)` (2 fixed args, `MP_DEFINE_CONST_FUN_OBJ_2` — no var-arity macro needed here, unlike the superseded 4-arg bit-bang version). `basic_bw.py` (full display -> 4x partial update through shape rotations -> final full display) ran clean on real Inkplate10, confirmed visually correct (shapes update properly, right colors, no inversion/ghosting/shift). Partial draw time 211ms/frame — faster than both the Phase 2 bit-bang baseline (454ms/frame) and the superseded bit-bang C port below (251ms/frame).
    - Superseded first attempt (bit-bang, deleted): a mono bit-bang C port (`firmware/usermods/inkplate/epd_partial.{h,c}`, an earlier version of `epd_partial_lut.{h,c}` — a literal 1:1 port of the old direct-GPIO-register `_send_row`/`_skip_rows`, plus a generalized byte2gpio scatter off `cfg->data_pins`) was built, flashed, and HIL-verified correct on real Inkplate10 (251ms/frame vs the pre-C-port Python baseline's 454ms/frame). Superseded because partial update needed to be on I2S like the rest of the pipeline, matching the real reference driver above, not because it didn't work — deleted once the I2S port landed.

## Phase 7 — Image decode + dithering: grayscale path (Inkplate10)

Moved ahead of board generalization (Phase 8) and SPI-panel bring-up (Phase 9) per user
request: image loading is a user-visible feature gap on the one board that's fully built
and HIL-provable today (Inkplate10), and doesn't need any of the other 5 parallel-bus
boards or the SPI color panels to exist first. Not a from-scratch C port of the current
Python decode loops — embed existing, already-optimized C decoder libraries instead. Only
the dithering/quantization step (RGB → this project's specific bit-depth output) is newly
written C, since that logic is Inkplate-specific and can't come from a generic library.
The general N-color palette dithering path (color/BWR panels) stays deferred to Phase 10,
after Phase 9's SPI boards exist to verify it against — no point writing untestable code
now.

17. **[DONE]** Port `shared/gfx.py` + `shared/shapes.py` (line/rect/circle/triangle/text primitives, pixel-callback based) to C, Inkplate10 first. Put first in this phase since image drawing (decoded/dithered output) and shape drawing land on the same pixel-write path — porting gfx now gives the decoders below a proven C drawing primitive layer to render into, instead of crossing back into Python per pixel/shape. **Verify**: host-compiled unit test — for line/rect/circle/triangle/fill primitives, compare the set of (x,y[,color]) pixel-callback coordinates against the current Python implementation's output for matched inputs, coordinate-for-coordinate. **Verify (HIL)**: existing shape-drawing examples (`basic_bw.py` etc.) render identically to before, now through the C path. **Verified**: `firmware/usermods/inkplate/gfx.{h,c}` ports `shared/gfx.py`'s GFX class (line/rect/fill_rect/circle/fill_circle/triangle/fill_triangle/round_rect/fill_round_rect + a merged text-glyph blit) to C for Inkplate10 — `shared/shapes.py`'s `Shapes` mixin turned out to be dead code (grepped every board + example: `Shapes.__mix_me_in(InkplateMono/InkplateGS2)` runs on all 6 boards but none of its methods are ever called; all real shape drawing goes through `shared/gfx.py`'s `GFX` class instead), so it's left untouched rather than ported unused — flagged for deletion alongside Phase 8 step 27's dedup pass. `gfx_set_pixel` implements the same rotation-aware logical-bounds check / physical remap / 1bpp-or-GS4_HMSB packing as `inkplate10.py`'s existing `write_pixel_viper`, and every new C primitive funnels through it — but `write_pixel_viper` itself was deliberately left in place (see the write_pixel/draw_bitmap note below), so this is a second, C-side implementation of the same logic, not a replacement of the first. Real bug found and fixed during the port, not by guessing: `shared/gfx.py`'s `_print_text`/`_draw_char_2bpp` still packed text at 2bpp (4px/byte, colors 0-3) into `ipg._framebuf` — but step 14 switched GS storage to `GS4_HMSB` (4bpp, 2px/byte, colors 0-7), and `write_pixel_viper` was updated for that at the time while the text path was missed, so text drawn in grayscale mode had been silently corrupting the framebuffer (wrong stride/packing) since step 14. Fixed by replacing `_draw_char_1bpp`/`_draw_char_2bpp` with one `gfx_draw_char` that dispatches on `display_mode` like every other primitive, instead of a separate caller-supplied `bpp` kwarg that could disagree with the real storage. `boards/inkplate10/inkplate10.py`'s `write_line`/`draw_rect`/`draw_circle`/`fill_circle`/`draw_triangle`/`fill_triangle`/`draw_round_rect`/`fill_round_rect` now call the new `inkplate.gfx_*` C bindings directly; `_print_text` (new instance method, replacing `self.GFX._print_text`) ports the font/string-walking control flow from `shared/gfx.py` unchanged but blits through `inkplate.gfx_draw_char`. `self.GFX`/the `GFX(...)` instance construction in `Inkplate.begin()`/`set_rotation()`/`set_font()` are removed for Inkplate10 (font state moves to plain `self.font_family`/`self.font` attributes); `shared/gfx.py` itself is untouched and still used as-is by the other 5 not-yet-ported boards. `tests/test_gfx.c` (host-compiled, wired into `micropython.cmake` and all three `ci.yml` jobs): shape-primitive expected coordinate sets were generated by actually running `shared/gfx.py`'s `GFX` class with a pixel-recorder callback for the same inputs (not hand-derived), including cases that exercise `gfx_fill_triangle`'s floor-division (Python's `//` semantics, not C's truncating `/`, needed once a triangle's vertices aren't x-sorted) and its flat-top-edge branch; separate hand-computed cases cover `gfx_set_pixel`'s rotation remap (all 4 rotations) and GS4 nibble packing (even-x→low nibble, odd-x→high nibble, raw 0-7 not clamped to 0-3), plus `gfx_draw_char` at 1x/2x scale in both mono and GS4 modes (regression coverage for the bug above). All 5 host-compiled test binaries pass; `gcc -Wall -Wextra -Werror` and `clang-format --dry-run --Werror` clean. **Verified (HIL)** on real Inkplate10: `basic_bw.py` (all 8 shape primitives × 4 rotations + Soldered logo), `hello_world.py` (mono text), and a throwaway GS4-mode script (8 gray-level bars + text + circle + triangle together, specifically exercising the bug fix above) all rendered correctly, no crash, no shift, no corruption — confirmed visually on the panel. Mono draw time unchanged at 177ms/`display()` (matches step 11's baseline; this step doesn't touch the display/waveform path). Follow-up done in the same pass, prompted by a real timing question (`basic_grayscale.py`'s 25.8s runtime looked unexplained): `write_fill_rect`/`write_fast_hline`/`write_fast_vline` predate `shared/gfx.py`'s `GFX` class (`GFX.fill_rect`/`hline`/`vline` are bound *to* these, not the other way around) so they were out of scope for the initial pass even though `gfx_fill_rect`/`gfx_hline`/`gfx_vline` already existed and were already tested — wired them onto the C primitives (2 new bindings, `gfx_hline`/`gfx_vline`, added to `inkplatemodule.c`; `gfx_fill_rect` binding already existed). This collapses each call from an O(w×h)/O(n) per-pixel Python loop (each iteration a full `write_pixel` → `write_pixel_viper` call) into a single C call. HIL-confirmed: `basic_grayscale.py` total runtime dropped from 25832ms to 7167ms (3.6x) — the 3× `display()` calls stayed at ~1.6s each (unchanged, that's the already-fast I2S/waveform path from Phases 4-5), so the win is entirely in the 8-bar `write_fill_rect` loop (824×825 ≈ 680k pixels) collapsing from ~21s to ~2.4s (which also still includes the ~25KB embedded-bitmap-literal parse cost and `draw_bitmap`, both untouched). `basic_bw.py` re-verified after this change (uses `draw_fast_hline`/`draw_fast_vline`): still renders correctly, timing unchanged (177ms — was already fast). **Considered and deliberately deferred**: `write_pixel` and `draw_bitmap` were *not* wired onto the C path despite looking like the same pattern. `write_pixel` already calls `write_pixel_viper`, a MicroPython viper (near-native-compiled) function — replacing a single-pixel viper call with a single-pixel C-extension call doesn't collapse a loop (there's no loop to collapse; it's already one call), and a C-extension call pays comparable `mp_obj_t` argument-marshaling overhead to a viper call, so there's no clear win and a real risk of a regression for single/few-pixel callers. `draw_bitmap`'s cost is its own Python double-loop calling `write_pixel` per set bit (matching the pattern that made `write_fill_rect` slow) — fixing that for real needs a *new* C bitmap-blit primitive (source bitmap + w/h + dest x/y/color, looping entirely in C), which is new, untested logic, not just wiring to something that already exists and was already verified — left for a future step rather than rushed in here. **Also done**: with `self.GFX`/the `Shapes` mixin confirmed unused on Inkplate10, removed the now-dead `from gfx import GFX` + `cls.GFX = GFX(...)` in `_Inkplate.begin()` (that classmethod is itself never called anywhere and was already broken — it referenced `cls.write_pixel`/`write_fast_hline`/etc., which only exist on the unrelated `Inkplate` class, not `_Inkplate`) and the dead `from shapes import Shapes` + `Shapes.__mix_me_in(...)` in `inkplate_mono.py`/`inkplate_gs.py`; dropped `gfx.py`/`shapes.py` from `boards/inkplate10/package.json`'s install manifest. `shared/gfx.py`/`shared/shapes.py` themselves stay in the repo — inkplate6/5v2/6flick still import from them (Phase 8 not done). **Verified (HIL)**: removed `gfx.py`/`shapes.py` from the device's `:lib/` entirely and re-ran `basic_bw.py`/`hello_world.py`/the GS4 regression script — all still render correctly, confirming no hidden dependency.
18. **[DONE]** Enable ESP-IDF's bundled software JPEG decoder (`tjpgd`) at firmware build time — no external library needed, no porting, just component config. Wire it into a thin C call producing decoded RGB output per row/tile. **Verify**: host-compiled unit test decodes a known-good sample JPEG to the expected pixel buffer. **Verified, scope adjusted**: TJpgDec turned out to be baked directly into ESP32's mask ROM (`rom/tjpgd.h`, exposed by ESP-IDF v5.5.2 with zero component/build-system changes — even lighter than the `esp_jpeg`/`esp32-camera` component route originally assumed) — `firmware/usermods/inkplate/jpeg_decode.{h,c}` is a thin wrapper over `jd_prepare`/`jd_decomp`, streaming one MCU tile at a time through a caller callback (`jpeg_tile_t`: x/y/w/h + RGB888), no full-image buffer. Because the decode logic lives in mask ROM, not portable source, it can't be linked by this repo's host-gcc test harness (`ci.yml`'s `test` job) the way `bmp_decode.c`/`gfx.c` can — **Verify was changed to HIL-only** (user decision, weighed against pulling in the `esp_jpeg` component purely to get a host-compilable copy of the same decoder). Two real bugs caught building against the actual header (not guessed): `jd_decomp`'s output callback returns `UINT`, not `int` (`rom/tjpgd.h`'s actual signature, confirmed only once IDF was available to grep) — host build failed loudly on the mismatch; more seriously, an initial draft swapped `JDEC.device` from the input-stream context to the output-tile context between `jd_prepare()` and `jd_decomp()`, not realizing `jd_decomp()` still calls the stashed `infunc` internally (via `jd->device`) to keep pulling entropy-coded bytes while it decodes — the swap would have hard-crashed or corrupted mid-decode the first time a real multi-tile JPEG was decoded; fixed by folding both the input cursor and the output callback into one `jpeg_session_t` that rides in `device` for the whole session instead of being swapped. Confirmed building clean against real ESP-IDF v5.5.2 (`get_idf_5`, `BOARD=ESP32_GENERIC BOARD_VARIANT=SPIRAM`) and flashing/booting on real Inkplate10 hardware without crash. **Verified (HIL), ahead of schedule at user's request**: rather than stopping at decode-only, wired a placeholder end-to-end path — `firmware/usermods/inkplate/jpeg_draw.{h,c}` (`jpeg_draw_gs4`) decodes via `jpeg_decode()` and blits straight into a GS4 framebuffer through `gfx_set_pixel` (step 17's rotation-aware packer), quantizing each pixel's ITU-R BT.601 luma to the nearest of 8 gray levels with **no error diffusion** — a deliberate stand-in for step 21's real Floyd-Steinberg/etc. dithering, expected to band. Bound as `inkplate.jpeg_draw_gs4(fb, phys_w, phys_h, rotation, x0, y0, jpeg_bytes) -> (width, height)` in `inkplatemodule.c`. New example `examples/inkplate10/displayimagesd/display_image_sd_jpeg_c.py` calls this binding directly (not through `draw_image()`/`draw_jpg_from_sd()` — those stay wired to step 21's future C pipeline; today `draw_jpg_from_sd` is actually dead code, importing a nonexistent `jpeg` module). On real hardware: decoded and drew the repo's existing `examples/inkplate10/displayimagesd/mountain.jpg` (1200x825, full Inkplate10 panel) in 1557ms decode + 1595ms display, visually confirmed correct by the user (banding present as expected, no crash/corruption/shift). Real, unrelated bug found and fixed along the way: `init_sd_card()` (`boards/inkplate10/inkplate10.py`) hardcoded the SD-over-SPI clock at `freq=80000000` — far above what SPI-mode SD supports — which failed to mount on real hardware with `OSError(16)` even with a known-good, correctly-seated, correctly-formatted card; root-caused (not guessed) by bypassing the function's swallowed `except Exception: print(...)` to see the real failure, then binary-searching the actual parameter, landing on the standard SD-over-SPI init clock `freq=400000` (400kHz), which mounts reliably — not yet tested whether a higher steady-state speed post-init would also work. Fixed in place and re-verified: real `init_sd_card()` now mounts clean, `mountain.jpg` reads and decodes correctly end to end. Also found and fixed (unrelated pre-existing bug, flagged then corrected at user's request): `display_image_sd.py`'s existing example and docstring passed `fastBoot=`, but the real keyword is `fast_boot` — would have raised `TypeError` if run as checked in; both the call and the docstring comment now say `fast_boot`.
19. **[DONE]** No IDF-bundled PNG decoder exists — user to supply a PNG decoder library to embed as a firmware component, same treatment as JPEG (no porting, just wire in). **Verify**: host-compiled unit test decodes a known-good sample PNG to the expected pixel buffer. **Verified, scope adjusted**: user supplied `pngle` (MIT, kikuchan/pngle, a single-file streaming PNG parser), vendored as-is at `firmware/usermods/inkplate/pngle.{c,h}`. pngle's own dependency is a `miniz.h` (zlib-style inflate: `tinfl_decompressor`/`tinfl_decompress`/`mz_crc32`) — rather than vendoring miniz too, that header resolves against ESP32's mask-ROM-resident miniz (`esp_rom/include/miniz.h`, already on the include path from step 18's ROM-tjpgd wiring; confirmed byte-for-byte API match against pngle's usage, including `ESP_ROM_HAS_MZ_CRC32=1` on classic ESP32 giving a real ROM `mz_crc32`, not just a CRC-32 lookalike). Because the inflate half lives in mask ROM, this can't be host-gcc-compiled/tested the same way `bmp_decode.c` can — **Verify was changed to HIL-only**, same reasoning as step 18. `firmware/usermods/inkplate/png_decode.{h,c}` is a thin wrapper over `pngle_feed`, streaming one output pixel at a time through a caller callback (`png_pixel_cb_t`: x/y + RGBA8888), expanding pngle's own w×h draw-callback blocks (>1×1 during Adam7 interlace passes) into a flat per-pixel raster stream. `firmware/usermods/inkplate/png_draw.{h,c}` (`png_draw_gs4`) mirrors step 18's `jpeg_draw_gs4`: decodes via `png_decode()` and blits into a GS4 framebuffer through `gfx_set_pixel`, same ITU-R BT.601 luma → nearest-of-8-levels quantization, no error diffusion (step 21's job) — alpha (`rgba[3]`) is ignored, pixels treated fully opaque, since neither JPEG nor this format's target use case (background images) need compositing yet. Bound as `inkplate.png_draw_gs4(fb, phys_w, phys_h, rotation, x0, y0, png_bytes) -> (width, height)`, alongside `jpeg_draw_gs4` in `inkplatemodule.c`. New example `examples/inkplate10/displayimagesd/display_image_sd_png_c.py` calls this binding directly, same shape as the JPEG example (no sample PNG ships in-repo; the example's docstring says to copy one onto the SD card as `/sd/coastal.png`). `pngle.c`/`pngle.h` are excluded from `ci.yml`'s clang-format check (third-party vendor code, kept in its original upstream style). **Verified (HIL)**: built and flashed clean against real ESP-IDF v5.5.2 (`get_idf_5`, `BOARD=ESP32_GENERIC BOARD_VARIANT=SPIRAM`); on real Inkplate10 hardware, decoded and drew an 800x600 PNG (`coastal.png`, 947KB) in 2811ms decode + 1610ms GS2 display (825ms clean + 785ms draw), visually confirmed correct, repeatable across runs, no crash. A first attempt with a larger (4.45MB) PNG (`plitvice.png`) hit `MemoryError: memory allocation failed, allocating 1439744 bytes` reading the whole file into one `bytes` object — root-caused (not guessed) by measuring: `gc.mem_free()` after `begin()+init_sd_card()` is ~3.34MB, so a 4.45MB single-buffer read can never fit regardless of fragmentation. This is a real scaling limit of `png_decode`'s (and `jpeg_decode`'s) whole-buffer API, not a decode bug — noted here for whoever revisits large-image support later; worked around for this verification by using the smaller `coastal.png` instead. Also found and fixed (unrelated pre-existing bug, same file step 18 already touched): `init_sd_card()`'s SD-over-SPI `freq` was set to 400000 (400kHz) under the mistaken assumption that the *whole* session had to run at the identification-phase clock — in fact ESP-IDF's sdspi host driver always runs `CMD0`/`CMD8`/`ACMD41` identification at its own ≤400kHz internally regardless of the `freq` argument, which only caps the post-identification data-transfer clock; 400kHz was therefore throttling every SD read/write, not just mounting. Root-caused by binary-searching real hardware (not guessed): tested 400kHz/1MHz/4MHz/10MHz/20MHz/25MHz directly against the board, confirmed byte-identical reads at every step, `coastal.png`'s 947KB read dropping from 37098ms (400kHz) to 12129ms (20MHz) with diminishing returns above ~10MHz (other overhead dominates past that point). Fixed to `freq=20000000` (matches `machine.SDCard`'s own built-in default, safely under the 80MHz that failed to mount in step 18) — whole example's wall-clock time dropped from ~40s to ~19.7s end to end. This SD-speed fix isn't PNG-specific; it benefits every SD-card image path (JPEG included).
20. **[DONE]** Write a BMP decoder directly in C (format is simple enough that no external library is needed). Scope: 24-bit uncompressed (BI_RGB), plus 1/4/8-bit indexed (palette, uncompressed) and 16-bit (555/565, uncompressed) — RLE4/RLE8 and 32-bit stay out of scope (not worth the added decode complexity/test surface for this project's use case). **Verify**: host-compiled unit test decodes a known-good sample BMP correctly per supported depth. **Verified**: `firmware/usermods/inkplate/bmp_decode.{h,c}` — pure logic, no I/O, no ESP-IDF/hardware headers, same tier as `waveform.c`/`epd_partial_lut.c`. `bmp_parse_header` reads the file/info header into an extended `bmp_header_t` (width/height/data_offset/row_size/flip_y/bpp, plus per-depth palette/16-bit-mask fields) — accepts classic 40-byte `BITMAPINFOHEADER` plus its BITMAPV4HEADER (108) / BITMAPV5HEADER (124) supersets (same first-40-bytes layout; real-world files, e.g. Photoshop exports, commonly use V4/V5 — found via HIL below, not guessed upfront) and any bit depth outside {1,4,8,16,24} is rejected. Indexed depths (bpp<=8) get `palette_offset`/`palette_count` (from `biClrUsed`, defaulting to `2^bpp`); `bmp_parse_palette` reads the BGRA-quad palette table once into a 256-entry `uint8_t palette[256][3]` embedded directly in the header struct (design choice: embed rather than pass separately per `bmp_decode_row` call, so the hot per-row/per-pixel path stays a single lookup with no extra params) — unused slots beyond `palette_count` are zeroed so any 0-255/0-15/0-1 index is always in-bounds. 16-bit resolves its R/G/B bit masks/shifts/bit-widths either immediately in `bmp_parse_header` (BI_RGB, implicit 555: masks `0x7C00`/`0x03E0`/`0x001F`) or via a separate `bmp_parse_bitfields` call once the caller reads the 12-byte BI_BITFIELDS mask section the header points to via `bitfield_offset` — mask-to-shift/bit-width resolution is generic (finds the lowest set bit and counts contiguous run length), so it correctly decodes any valid 16-bit RGB bitfield layout, not just a hardcoded exactly-565 check; non-contiguous or empty masks are rejected. `bmp_decode_row` dispatches on `hdr->bpp`: 24-bit unchanged from the original port; 16-bit unpacks via the resolved masks and rounds each channel up to 8 bits (`(v*255+max/2)/max`); 8-bit is a direct palette lookup; 4-bit unpacks 2 pixels/byte (high nibble first); 1-bit unpacks 8 pixels/byte (MSB first) — same palette lookup for all three. `tests/test_bmp_decode.c` extended with one hand-crafted 2x2 sample per depth (8/4/1-bit indexed sharing one red/green/blue/yellow palette; 16-bit 555 via BI_RGB and 16-bit 565 via BI_BITFIELDS, both producing the same red/green/blue/yellow expected RGB888 output as the original 24-bit sample once channel-scaling rounding is accounted for; a 24-bit sample re-wrapped in a 124-byte BITMAPV5HEADER) plus negative cases: unsupported header size (not one of {40,108,124}) rejected, truncated palette/bitfield buffers rejected, non-contiguous bitfield mask rejected. Original 24-bit sample's pre-existing "unsupported bit depth" negative test used depth=8 as the example, which this step turned into a *valid* depth — changed to depth=2 (still genuinely unsupported) so the assertion still tests what it claims to. All assertions pass, `gcc -Wall -Wextra -Werror` and `clang-format --dry-run --Werror` clean; no `micropython.cmake`/`ci.yml` changes needed for `bmp_decode.c`/`test_bmp_decode.c` themselves (already wired in from the 24-bit-only pass).

    **Ahead of schedule, HIL-verified**: same "wire a placeholder draw path now" treatment steps 18/19 gave JPEG/PNG. `firmware/usermods/inkplate/bmp_draw.{h,c}` (`bmp_draw_gs4`) mirrors `jpeg_draw_gs4`/`png_draw_gs4`: parses the whole in-memory BMP buffer via `bmp_parse_header`/`bmp_parse_palette`/`bmp_parse_bitfields`, then walks rows through `bmp_decode_row` into a static (not stack/heap) RGB888 row-scratch buffer sized for the widest board resolution in scope (1600px, headroom over Inkplate5v2's 1280), quantizing each pixel to the nearest of 8 gray levels via `gfx_set_pixel` — no dithering, matching the JPEG/PNG placeholders. Bound as `inkplate.bmp_draw_gs4(fb, phys_w, phys_h, rotation, x0, y0, bmp_bytes) -> (width, height)`; new example `examples/inkplate10/displayimagesd/display_image_sd_bmp_c.py` calls it directly, same shape as the JPEG/PNG C examples. `micropython.cmake`/`ci.yml`'s build job gained `bmp_draw.c` (host-compilable, no ESP-IDF dependency, unlike `jpeg_draw.c`/`png_draw.c` which need their ROM-backed decoders).

    Two real, HIL-found issues, neither a decoder bug: (1) the SD card's actual `coastal.bmp` (800x600, 24-bit) turned out to use BITMAPV5HEADER (`biSize=124`) — caught by inspecting the real file's header on-device before assuming the pre-existing classic-only scope would work, which is what motivated widening `bmp_parse_header` above; cross-checked that BI_BITFIELDS mask offsets stay fixed at `14+40` (not `14+biSize`) for V4/V5 too, since V4/V5's own RedMask/GreenMask/BlueMask struct fields sit at that same fixed relative position — only `palette_offset` scales with the real header size, not `bitfield_offset`. (2) the example script's first version (`with open(...) as f: bmp_data = f.read()`) hit `MemoryError: memory allocation failed, allocating 1439744 bytes` on the 1.44MB file despite ~3.3MB free heap measured at that exact point (`gc.mem_free()` right after `begin()`+`init_sd_card()`) — root-caused, not guessed: a plain `bytearray(1440138)` at the same point allocates fine, so it's specifically MicroPython's whole-file `read()` (which grows its buffer geometrically and can transiently need more than the final size) misbehaving on this file/size, not a hard memory ceiling. Fixed by preallocating via `os.stat`+`readinto()` instead. Re-ran clean on real Inkplate10 post-fix: `decoded 800x600 BMP in 414 ms`, GS2 refresh `823ms clean + 784ms draw = 1607ms` total — reproduced identically across a firmware reflash and repeat runs. Visually confirmed correct on the panel by the user (banding present as expected, no crash/corruption/shift).
21. **[DONE]** Port the existing dithering/quantization logic to C — this already existed in `inkplate10.py`'s `write_row`/`write_image`/`decode_png_to_framebuffer` (`@micropython.viper`, Floyd-Steinberg/JJN/Stucki/Burkes kernels), but the three implementations disagreed with each other and two of them had dead mono-dithering code (error buffers were read but never written to in the mono branch, so `dither=True` was a silent no-op for mono BMP/JPEG images) — not a single faithful port, ported+fixed instead, disclosed rather than silently changed. New `firmware/usermods/inkplate/dither.{h,c}` generalizes scalar error diffusion over `display_mode` (mono/3-bit GS) with all 4 kernels as data tables (host-tested, `tests/test_dither.c`); GS3 quantization standardized on the round-to-nearest formula steps 18-20's non-dither placeholders already used (dropping the old dithered path's separate floor-bucket quantizer); `invert` now applies as a final XOR on the quantized level instead of before computing the error-diffusion reconstruction value (the old order corrupted diffused error whenever invert+dither were combined). `bmp_draw_gs4`/`jpeg_draw_gs4`/`png_draw_gs4` (steps 18-20's placeholders) extended with `display_mode`/`invert`/`dither`/`kernel_type` params instead of new parallel functions. BMP dithers inline row-by-row (bmp_decode.c already decodes strictly top-to-bottom); JPEG/PNG buffer the whole image's luma into a capped scratch buffer first, then dither in one guaranteed row-major pass afterward -- ROM-tjpgd delivers MCU tiles in tile-raster order (a tile can span multiple rows before its right-hand neighbor tile decodes) and pngle's Adam7 interlace passes re-sweep the whole image, so neither guarantees the full-scanline order row-by-row diffusion needs. `boards/inkplate10/inkplate10.py`'s `draw_bmp_from_sd/web`, `draw_jpg_from_sd/web`, `draw_png_from_sd/web` now call these C bindings directly (BMP also switches from its old 24-bit-only decode onto step 20's `bmp_decode.c`, adding indexed/16-bit support as a side effect); `write_row`/`write_image`/`decode_png_to_framebuffer` (~950 lines) deleted. `draw_jpg_from_sd/web` were dead code before this (imported a nonexistent `jpeg` module, per step 18's own note) -- fixed as a side effect of the rewire. Kernel tables ported faithfully including a pre-existing bug: the Stucki table's weights (truncated to 2 rows, missing the textbook 3rd row) sum to 32 but are divided by 42, under-diffusing ~24% of each pixel's error -- not fixed, since correcting it changes visual output beyond this step's scope. **Verified (HIL)** on real Inkplate10: mono (`basic_bw.py`) and GS3 (`basic_grayscale.py`) regressions unaffected (same timings as Phase 4/5 baselines). JPEG dithering (`mountain.jpg`, via `jpeg_draw_gs4`) reproduced clean twice, no banding artifacts from the placeholder path. PNG dithering (`coastal.png`) confirmed correct at least once end-to-end (decode+dither+display, ~5.6s) but the SD/serial connection proved intermittently flaky across reruns on this session's hardware (see below) -- code path confirmed correct, hardware reliability not. BMP dithering (`coastal.bmp`, 1.44MB, 1/4/8/16/24-bit path from step 20) confirmed visually correct by the user with a white-screen-then-image check to rule out stale panel content. Two real bugs found and fixed during HIL, neither guessed: (1) `jpeg_draw.c`/`png_draw.c`'s luma scratch buffer used plain `malloc()`, which draws from ESP32's small internal-DRAM pool and fails outright for a buffer this size -- switched to `heap_caps_malloc(..., MALLOC_CAP_SPIRAM)`, matching how this project's framebuffers already have to reach PSRAM (Phase 0 step 4); (2) the buffer was originally sized 1600x1200 (headroom for boards not yet in this repo, matching `bmp_draw.c`'s row-buffer precedent) but even with the PSRAM fix this full 2D image buffer failed to allocate alongside the framebuffer + MicroPython's own PSRAM-backed heap + the decoded file's Python-side buffer -- shrunk to Inkplate10's actual 1200x825 (the only board this repo supports today); revisit when Phase 8 lands bigger boards. Separately (not a step-21 regression, but surfaced during this HIL pass): step 20's SD-over-SPI clock (20MHz, previously measured reliable up to 25MHz) reproducibly hung the board mid-read for files over ~900KB on this session's hardware, confirmed with plain `f.read()`/`readinto()` calls with zero decode/dither code involved, surviving multiple power-cycles and an SD card reseat -- dropped to 4MHz (`boards/inkplate10/inkplate10.py`'s `init_sd_card`), confirmed hang-free at that speed for both the 947KB PNG and 1.44MB BMP; root cause (card wear, cable, connector, or a marginal driver setting) not identified, flagged in-code for whoever revisits SD throughput next.

    **Follow-up, HIL-verified on Inkplate6COLOR and Inkplate13SPECTRA**: prompted by a PSRAM-capacity question (why an 8MB WROVER-E only exposes 4MB to `malloc` — a classic-ESP32 hardware/MMU limit, `esp_himem` bank-switching being the only path to the rest, and a poor fit for gfx.c's random-access framebuffer). Checking whether `jpeg_draw.c`/`png_draw.c`'s whole-image `luma` scratch buffers (the thing that hit the PSRAM ceiling above) were even structurally necessary turned up: JPEG's `jpeg_decode()` already reports width/height via a `pre_cb` *before* MCU decoding starts, and tjpgd's tiles arrive in strict row-band raster order — the grayscale path (`jpeg_draw_gs4`) just hadn't been updated to use either, unlike `jpeg_draw_palette` (step 32's follow-up), which already proved the row-band approach works. Ported the same row-band buffering `jpeg_draw_palette` uses onto `jpeg_draw_gs4` (renamed the shared band constants off "palette", scalar luma instead of RGB565) — no more whole-image buffer or `JPEG_DRAW_MAX_HEIGHT` cap for JPEG grayscale; width is still capped by the band buffer (`JPEG_DRAW_BAND_MAX_W`), now rejected via `pre_cb` before any MCU decoding (a new `-2` return, `inkplatemodule.c`'s `jpeg_draw_gs4` binding updated to raise the same "too wide to dither" message `jpeg_draw_palette` already does). PNG is different — Adam7 interlacing genuinely re-sweeps the whole image in up to 7 non-monotonic passes, so an interlaced source still needs the whole-image buffer — but Adam7 is opt-in at encode time and rare in practice, and pngle already exposes IHDR's interlace flag; added `png_peek_interlace` (same fixed-offset IHDR peek as the existing `png_peek_dimensions`) and split `png_draw_gs4` into two paths: non-interlaced (the common case) dithers inline per pixel with no buffer at all and no cap, interlaced falls back to the existing capped whole-image buffer unchanged.

    User pushback caught a real scope gap: this first pass only touched the grayscale path, but `png_draw_palette` (the color/palette PNG path Inkplate6COLOR/Inkplate13SPECTRA/Inkplate2 actually use) had the exact same unconditional whole-image-buffer bug — every PNG, interlaced or not, went through the buffered `scratch_rgb` path. Same fix applied there: non-interlaced sources now dither inline per pixel via `dither_rgb_ctx_t`, no scratch buffer and no `max_width`/`max_height` cap at all; only a genuinely Adam7-interlaced source (or a failed peek) still falls back to the caller-supplied `scratch_rgb` buffered path.

    First real-hardware attempt (Inkplate6COLOR, `coastal.png`, `dither=True`) still threw the exact `MemoryError: memory allocation failed, allocating 1980000 bytes` this whole effort was meant to fix — root-caused, not guessed: the bug wasn't in the C fix at all, it was one layer up. `boards/inkplate6color/inkplate6_color.py`'s (and `inkplate13spectra/inkplate13_spectra.py`'s, and `inkplate2/inkplate2.py`'s) `draw_png_from_sd`/`draw_png_from_web` unconditionally allocated a fresh `_DITHER_SCRATCH_BYTES` (1.98MB) `bytearray` scratch buffer *before* ever calling into C, every single time `dither=True` — regardless of whether the now-mostly-unnecessary buffer would ever be used. That eager allocation, not the C-side cap, is what reliably `MemoryError`'d on real hardware. Fixed by removing the eager allocation across all three boards' PNG draw methods (pass `None` for the scratch param unconditionally) and deleting the now-dead `_DITHER_SCRATCH_W/H/BYTES` constants; `inkplatemodule.c`'s `png_draw_palette` `-2` error message updated to mention the now-real possibility of an Adam7-interlaced source (since no board pre-allocates a fallback buffer anymore, an interlaced PNG with `dither=True` now cleanly returns `-2` — "too wide, or an Adam7-interlaced PNG" — instead of crashing or silently degrading; Inkplate2's own PNG path is untouched otherwise, not HIL-tested, no board on hand). Builds clean (`make BOARD=ESP32_GENERIC BOARD_VARIANT=SPIRAM` and, for Inkplate13SPECTRA, `BOARD=ESP32_GENERIC_S3 BOARD_VARIANT=SPIRAM_OCT`), `clang-format --dry-run --Werror`/`ruff format --check` clean.

    **Verified (HIL)** on real hardware, both boards freshly reflashed, board `.py` driver files re-pushed: **Inkplate6COLOR** — `coastal.png` (800x600, non-interlaced, `dither=True`) via `draw_png_from_sd` completed successfully (previously a guaranteed crash): 20151ms decode+dither (barely more than the 18727ms undithered baseline measured on the same file/session — confirms the per-pixel diffusion overhead is the trivial cost the math predicts, not a regression), 31319ms `display()`, `gc.mem_free()` stable before/after (3,272,048 → 3,272,016). Visually confirmed correct by the user (dithered, right colors, not garbled). **Inkplate13SPECTRA** (ESP32-S3) — same `coastal.png` via `draw_png_from_sd`: 12885ms decode+dither, 21585ms display, mem_free stable (14,633,680 → 14,633,696); `mountain.jpg` (1200x825, exercising the JPEG band-buffer path as a regression check, unaffected by the PNG-specific fix) via `draw_jpg_from_sd`: 5277ms decode+dither, 21630ms display, mem_free stable (14,633,696 → 14,633,632). Both images visually confirmed correct by the user. One HIL red herring worth flagging for whoever tests this next: the very first script run immediately after reflashing + pushing a new board `.py` file appeared to hang for 5+ minutes and was killed — but killing the host-side `mpremote` client doesn't stop code already running on the ESP32, and the device turned out to still be running normally the whole time (a clean immediate re-run completed in ~51s, matching every other measurement here). Don't diagnose a performance bug from one interrupted first-run timing — get an uninterrupted repeat measurement first.

## Phase 8 — Generalize to remaining parallel-bus boards (config only)

22. **[DONE]** Inkplate6 (800x600): add config + its 3-bit waveform table (user supplied the real Arduino reference driver: `pins.h`/`waveforms.h`/`Inkplate6Driver.{h,cpp}`, covering both the classic INKPLATE6 and INKPLATE6V2 hardware revisions). **Verify (HIL)**: mono + 8-level GS + partial update + basic examples on real hardware. **Verified**: pins/data bus/PCAL6416A internal-expander addresses (OE/GMODE/SPV) and PMIC I2C address are identical to Inkplate10 (same reference schematic) -- only the waveform table, external-expander address (classic=0x22, V2=0x21), and burn-in `clean()`-cycle rep counts differ. Added `board_config_inkplate6`/`board_config_inkplate6v2` to `firmware/usermods/inkplate/board_config.{h,c}` (waveform transposed from the Arduino driver's `[color][phase]` layout to this struct's `[phase][color]` convention, same as step 15 did for Inkplate10); `select_board()` in `inkplatemodule.c` extended to recognize both names. Host test cases added to `tests/test_board_config.c` (all pass). `boards/inkplate6/inkplate6.py`/`inkplate_mono.py`/`inkplate_gs.py`/`inkplate_partial.py` replaced with the C-usermod-backed versions (mirroring `inkplate10.py`'s already-refactored structure, dropping the old viper/bit-bang driver and its `shared/gfx.py`/`shared/shapes.py` dependency, confirmed dead the same way step 27 already expected). Two real conflicts surfaced between this repo's pre-existing (pre-refactor) Inkplate6 driver and the Arduino reference, resolved by user decision rather than guessed: burn-in `clean()` reps use the Arduino reference's real value (18, uniform across mono/GS) instead of the old driver's Inkplate10-borrowed 12/11; battery-read formula and SD init frequency (4MHz) were kept identical to Inkplate10's own scheme/value, flagged in-code as carried over rather than independently derived for this board. Defaults to the INKPLATE6V2 variant (external expander 0x21), matching what the old driver already assumed about this hardware. **Verified (HIL)** on real Inkplate6 hardware: firmware built and flashed clean (no `make clean` needed -- no registered-module/symbol-table changes, just new config data + an extended dispatch). `basic_bw.py` (mono + partial update + shape/line/circle/triangle drawing across all 4 rotations) and `basic_grayscale.py` (GS3) both ran clean with no exceptions across repeated runs, visually confirmed correct on the panel. Image decode confirmed too: JPEG (`mountain.jpg`, dithered, ~6.1s), PNG (`coastal.png`, dithered, ~18.9s), and BMP (`coastal.bmp`, dithered, ~8.4s) all decoded and displayed correctly via SD card, no crash, no SD hang at 4MHz, no PSRAM allocation failure. Also fixed an unrelated pre-existing bug hit while running the SD image example: `examples/inkplate6/displayimagesd/display_image_sd.py` called `init_sd_card(fastBoot=True)` but every board's method takes `fast_boot` (snake_case) -- same latent bug also present in the inkplate5v2/6color/13spectra examples (not fixed here, out of scope for this step).

    **Follow-up correction, real hardware bug found and fixed, not guessed**: this step's "PCAL6416A internal-expander addresses... identical to Inkplate10" claim was wrong for classic (non-V2) INKPLATE6 -- confirmed both against this repo's own pre-refactor tve-authored driver (which used `MCP23017(i2c)`, not `PCAL6416A`, for the 0x20 internal expander) and against the real Arduino reference `pins.h`/`touchpad.h` the user pasted this pass: classic INKPLATE6's internal expander (OE/GMODE/SPV/TPS_*/etc, addr 0x20) is genuinely an MCP23017; only INKPLATE6V2 uses a PCAL6416A there. Since all expander I2C register access happens in Python (`expander_bridge.h`'s callback design, not C), this was a Python-only fix with zero `board_config.c` structural changes needed -- `board_config_inkplate6.has_touch` flipped 0->1 (pure metadata, never branched on in C) is the only C-side change. `shared/mcp23017.py` (previously fully dead code, no board imported it) gained `pin_mode`/`digital_write`/`digital_read` methods matching `PCAL6416A`'s shape (same 0/1/2/3 mode-constant convention as `pcal6416a.py`, kept as plain ints rather than a cross-module import) so the existing `GpioPin` wrapper and `_expander_write_cb`'s addr-routed dispatch work unchanged regardless of which chip is actually behind `_expander1`/`_expander2` (renamed from `_PCAL6416A_1`/`_PCAL6416A_2`, no longer PCAL-specific). `boards/inkplate6/inkplate6.py` now branches expander class construction on `_IS_CLASSIC`. Also wired the onboard touchpads while in there (user asked for both together, matching the real Arduino `touchpad.h`'s `expander1.digitalRead(10/11/12)` for TOUCH1/2/3): classic-only, same expander pins V2 repurposes for `SD_ENABLE` (real Arduino `pins.h` defines `SD_PMOS_PIN=10` unconditionally for both variants, but `touchpad.h` puts TOUCH1 on that same pin for classic only -- confirmed against real hardware that classic v1 has no SD-card power MOSFET at all, so `init_sd_card`/`sd_card_sleep`/`sd_card_wake` now skip that toggle on classic instead of colliding with the touch pin). `tests/test_board_config.c`'s shared inkplate6/6v2 loop updated to expect `has_touch` per-variant instead of a blanket 0. **Verified (HIL)** on real classic (non-V2) Inkplate6 hardware (first-ever flash+test on this specific variant -- step 22's original HIL pass ran on V2 hardware only, per its "Defaults to the INKPLATE6V2 variant" note): fresh firmware flash succeeded clean; `_expander1`/`_expander2` construct as `MCP23017` at 0x20/0x22 with no I2C errors. TOUCH1/2/3 each independently read 0 at rest and flipped to 1 only while their own pad was pressed, reverting cleanly. Mono (`draw_circle`+`draw_line`, full `display()`) and GS3 (8 gray bars 0-7 + a mid-gray circle, full `display()`) both rendered correctly on the panel with no exceptions -- confirms the MCP23017-driven OE/GMODE/SPV/TPS sequencing (the actual bug this pass fixes) works correctly through the existing generic C/I2S engine. SD card: mounted clean at the carried-over 4MHz (previously flagged as unverified on real Inkplate6 hardware -- now confirmed, at least for this variant), `os.listdir("/sd")` succeeded. Image decode: JPEG (`mountain.jpg`, dithered, 6066ms), PNG (`coastal.png`, dithered, 18215ms), BMP (`coastal.bmp`, dithered, 8491ms) all decoded and displayed correctly, no crash -- timings essentially match step 22's original (V2) numbers.
23. **[DONE]** Inkplate5v2 (1280x720): same treatment (user supplied the real Arduino reference driver: `pins.h`/`waveforms.h`/`Inkplate5V2Driver.cpp`). **Verify (HIL)**: mono + 8-level GS + partial update + basic examples on real hardware. **Verified**: pins/data bus are identical to Inkplate6/10 (same reference schematic); this board has only one PCAL6416A expander (`IO_INT_ADDR = 0x20`) carrying OE/GMODE/SPV plus TPS_*/VBAT_EN/SD_ENABLE -- no separate external expander like Inkplate6/6V2's 0x21/0x22. No touch, no frontlight. Burn-in `clean()`-cycle reps are 11 (own reference value, uniform mono/GS, distinct from Inkplate6's 18); `partial_reps = 4` from the real `partialUpdate()` `for(k<4)` loop. Added `board_config_inkplate5v2` to `board_config.{h,c}` (waveform transposed the same way as steps 15/22); `select_board()` extended; host test cases added to `tests/test_board_config.c` (all pass). `boards/inkplate5v2/*.py` replaced with the C-usermod-backed versions, mirroring Inkplate6's already-refactored structure, single-expander-adjusted. Two real, board-specific deviations from copying Inkplate6's port verbatim, both confirmed against the pasted Arduino source rather than assumed: the trailing `vscan_start()` call is commented out (never executed) in Inkplate5V2Driver.cpp's `display3b()`/`display1b()`/`partialUpdate()` (unlike Inkplate6's, where it's live), so it's omitted here too; and a real hardware bug was found and fixed during HIL testing (see below). **Verified (HIL)** on real Inkplate5V2 hardware: firmware built and flashed clean (no `make clean` needed). `basic_bw.py` (mono + partial update + shape/line/circle/triangle drawing across all 4 rotations + Soldered logo) and a throwaway 8-level GS3 test script (8 gray bars 0-7 + text + circle + triangle) both ran clean with no exceptions, visually confirmed correct on the panel after a fix (below). Image decode confirmed too: JPEG (`mountain.jpg`, dithered, ~6.0s), PNG (`coastal.png`, dithered, ~18.0s), and BMP (`coastal.bmp`, dithered, ~8.1s) all decoded and displayed correctly via SD card at 4MHz, no crash, no hang. Hit the same pre-existing `fastBoot`/`fast_boot` kwarg-case bug step 22 already flagged for this board's example (`examples/inkplate5v2/displayimagesd/display_image_sd.py`) -- worked around it inline for testing rather than fixing, consistent with step 22's "not fixed here, out of scope" call. **Real bug found and fixed, not guessed**: the initial HIL pass showed Inkplate5V2's Soldered logo/text rendering left-right mirrored (shapes unaffected, since solid fills are insensitive to it) -- traced to this panel's column/data-shift-register scanning the opposite direction from Inkplate6/10's, a genuine hardware difference between panel models, not a logic bug (the shared bit-packing/row-reversal code was confirmed identical to Inkplate6's already-verified path). Fixed at two independent points, since draw_pixel/draw_bitmap and gfx_*/shapes/text use two different code paths that both needed the same compensation: `write_pixel_viper` in `inkplate5v2.py` now flips `px = w - 1 - px` after the rotation remap; `gfx.c` gained a new `gfx_set_mirror_x()` toggle (static session state, not a per-call param -- deliberately different from every other `gfx_*` param since this is a session-constant hardware trait like `select_board()`, not a per-draw-call value), called once from `inkplate5v2.py`'s `_Inkplate.init()`. Inkplate6/10 never call it, so their behavior is unchanged; host tests (`test_gfx.c`, `test_board_config.c`) still pass, defaults to off.
24. **[DONE]** Inkplate6FLICK (1024x758): display + partial path only via config; explicitly defer touch/frontlight to Phase 11 (user supplied the real Arduino reference driver: `Inkplate6FLICKDriver.h`'s `pins.h`/waveform macro, `Inkplate6FLICKDriver.cpp`, plus `TPS65186.h`/`.cpp`). **Verify (HIL)**: mono + GS + partial update work. **Wired**: pins/data bus/PCAL6416A internal-expander addresses (OE/GMODE/SPV) and burn-in `clean()`-cycle rep counts (5/15/1/15/1/15/1/15/1, identical for mono and GS) transcribed directly from the pasted Arduino source; PMIC I2C address confirmed 0x48. `partial_reps = 5` from the real `partialUpdate()` `for(k<5)` loop. Added `board_config_inkplate6flick` to `board_config.{h,c}` (waveform transposed the same way as steps 15/22/23); `select_board()` extended; host test cases added to `tests/test_board_config.c`/`test_waveform.c` (all pass, `gcc`/`ruff format`/`clang-format` clean). One real, user-confirmed deviation from every other wired board: this board's `display1b()` uses 4 black-push phases instead of the usual 5, followed by its own separate discharge pass (`delayMicroseconds(230)` then a zero-fill frame) that the shared 5-black-phase mono engine doesn't produce -- `inkplate_gen_mono_wave` (`waveform.{h,c}`) took a `black_phases` param instead of the previous hardcoded `INKPLATE_MONO_WAVE_PHASES` constant (still 5 for every other board, unchanged), and `inkplatemodule.c`'s `inkplate_mono_display` special-cases this board to 4; the discharge tail itself is just `clean(2, 1)` from `boards/inkplate6flick/inkplate_mono.py`. `boards/inkplate6flick/inkplate6_flick.py`/`inkplate_mono.py`/`inkplate_gs.py`/`inkplate_partial.py` replaced with the C-usermod-backed versions (mirroring Inkplate6's structure); `touch_cypress.py` and its Phase-11 placeholder logic left untouched. SD/battery/RTC/VCOM-EEPROM helpers Inkplate6's wrapper carries were deliberately not ported here (out of scope, not all confirmable from the pasted source).

    **Verified (HIL)** on real Inkplate6FLICK hardware: mono (shapes/rotations + Soldered logo, both a single full `display()` and the full `basic_bw.py` sequence of `display()` + 4x `partial_update()` + `display()`) and GS2 (`basic_grayscale.py`, two chained `display()` calls) all rendered correctly, repeatably, across multiple runs and reflashes -- no exceptions, no corruption.

    SD card wired this pass too (user asked to test image loading with a card plugged in), following Inkplate6/5v2's exact `machine.SDCard` SPI-mode pattern (same miso/mosi/sck/cs pins, 4MHz) plus this board's own real SD-power-MOSFET pin: `init_sd_card()`/`sd_card_sleep()`/`sd_card_wake()` added to `inkplate6_flick.py`, toggling `SD_ENABLE` at `IO_PIN_B5` (pin 13) on the internal expander (0x20) -- confirmed against this board's real `pins.h` (`SD_PMOS_PIN`), same pin Inkplate6PLUSV2 (step 25) independently confirmed for its own SD-enable line. Also corrected `board_config.c`'s comment, which had wrongly guessed SD/touchscreen lived on the second expander (0x21) -- the real `pins.h` puts SD_PMOS and most touchscreen lines (`TOUCHSCREEN_EN`=12, `TOUCHSCREEN_RST`=10, `TOUCHSCREEN_IO_EXPANDER`=`IO_INT_ADDR`) on the internal expander instead; only `TOUCHSCREEN_INT` (GPIO 36) is a direct ESP32 pin. The second expander's actual purpose is still unconfirmed. Touch/frontlight themselves remain deferred to Phase 11. Also fixed a mislabeled example: `examples/inkplate6flick/displayimagesd/display_image_sd.py` contained a copy of the WiFi/URL example instead of an actual SD example -- replaced with a real one mirroring Inkplate6's. **Verified (HIL)**: SD mounted clean, `os.listdir("/sd")` matches the same test card used for other boards. JPEG (`mountain.jpg`, dithered, 5933ms), PNG (`coastal.png`, dithered, 18033ms), and BMP (`coastal.bmp`, dithered, 8459ms) all decoded and displayed correctly, no crash -- timings in line with every other board.
25. **[DONE]** Inkplate6PLUS: same treatment (classic ESP32, parallel-bus, confirmed by user) — user supplied the real Arduino reference driver (`waveforms.h`, `pins.h`, `Inkplate6PLUSDriver.{h,cpp}`, `GraphicsDefs.h`). **V2 revision only** wired this pass (IO_EXT_ADDR 0x21; classic INKPLATE6PLUS's 0x22 isn't supported by this file) — touch/frontlight/VCOM-EEPROM/battery/temperature deliberately out of scope, same precedent as Inkplate6FLICK's first pass (step 24). **Verified**: 1024x758 panel (same resolution as Inkplate6FLICK), waveform table transposed from `WAVEFORM3BIT`'s `[color][phase]` layout via an independent python3 script (same process as steps 15/22/23/24). `partial_reps=5` from the real `partialUpdate()`'s `for(k<5)` loop, standard `LUTW`/`LUTB` diff logic, no board-specific quirk. GS3 `display3b()` is the standard `for(k<9)` loop. Added `board_config_inkplate6plusv2` to `board_config.{h,c}`; `select_board()` extended; host test cases added to `test_board_config.c` (all pass). `boards/inkplate6plusv2/*.py` written C-usermod-backed from scratch, mirroring Inkplate6FLICK's structure.

    **Real bug found and fixed via HIL, not guessed**: mono `display1b()` initially came out uniformly dark/washed on real hardware (background never reached true white) while GS3 (a completely different waveform engine, sharing only `board_config`/`clean()`/`power_on()` with mono) rendered correctly — isolating the fault to the mono engine's `black_phases` parameter. This board's real `display1b()` loops `for(k<4)`, textually matching Inkplate6FLICK's own quirk (step 24), so `black_phases=4` was initially wired the same way. Bumping it to 5 (the default used by every non-FLICK board) made no visible difference — expected, since the shared engine's repeated phase skips white pixels regardless of count, which was the actual clue. Root-caused by decoding this board's own `GraphicsDefs.h` `LUTW`/`LUTB` tables against `display1b()`'s `~dram`-indexed main loop / `dram`-indexed final loop (python3 script, not hand-derived): this board's real per-pixel scheme is the *mirror image* of every other wired board's — repeated phases push **white** (black pixels skip), one final phase pushes **black** (white pixels skip) — the reverse of `inkplate_gen_mono_wave`'s `op_blk`/`op_bw` tables (verified correct for Inkplate6/10/5v2/6FLICK independently, so not touched). Added `inkplate_gen_mono_wave_white_first()` (`waveform.{h,c}`, its own op tables, host-tested in `test_waveform.c`) and special-cased `board_config_inkplate6plusv2` onto it in `inkplatemodule.c`'s `inkplate_mono_display`, by pointer check — every other board's path is untouched. **Verified (HIL)** on real Inkplate6PLUSV2 hardware after the fix: mono (shapes/rotations/partial update via a `basic_bw.py`-equivalent smoke test) renders correctly, background properly white, no ghosting; GS3 (8-level ramp) re-confirmed unaffected by the mono-only fix.

    SD card also wired this pass (user asked to test image loading with a card plugged in) — `init_sd_card()`/`sd_card_sleep()` added to `inkplate6plusv2.py`, following Inkplate6/5v2's exact `machine.SDCard` SPI-mode pattern (same miso/mosi/sck/cs pins, confirmed against this board's own pasted `spi2.begin(14, 12, 13, 15)`), but using this board's own real SD-P-MOS-enable pin (13, `SD_PMOS_PIN`/`IO_PIN_B5` in the pasted `pins.h` — different from Inkplate6/5v2's pin 10, a genuine hardware difference, not copied). Started at the same 4MHz SD clock those boards settled on (the pasted `sdCardInit()`'s own `SD_SCK_MHZ(25)` is an SdFat/Arduino-SPI value, not directly portable to MicroPython's `SDCard` driver) — not yet independently stress-tested at higher speeds on this board. **Verified (HIL)**: SD mounted clean, `os.listdir("/sd")` matches the same test card used for other boards. JPEG (`mountain.jpg`, dithered): 5.9s decode + 1.6s display, visually confirmed correct. PNG (`coastal.png`, dithered): 13.9s SD read + 3.9s decode + 1.4s display (isolated via a split timing script — matches Inkplate5V2's own ~13.8s+3.8s finding almost exactly), visually confirmed correct. BMP (`coastal.bmp`, dithered): 8.5s decode+read + 1.4s display, visually confirmed correct, no shift/corruption beyond expected dithering.

    Still open for a future pass: classic (non-V2) INKPLATE6PLUS variant; `power_on()`/`power_off()` PMIC sequence (carries over Inkplate6/5v2's generic sequence — this board's real pasted `einkOn()` shows a more specific one via `TPS65186::enableRails()`/`writeReg(UPSEQ0=0xE4)`/`writeReg(DWNSEQ0=0x1B)`/`readPowerGood()` polling, but `TPS65186::enableRails()`'s own internals were never pasted, so it can't be replicated exactly without guessing); touch, frontlight, VCOM/EEPROM, battery, temperature.
26. **[DONE]** Inkplate4TEMPERA: same treatment (classic ESP32, parallel-bus, confirmed by user) — user supplied the real Arduino reference driver (`Inkplate4TEMPERADriver.{h,cpp}`, `pins.h`, `waveforms.h`, `GraphicsDefs.h`). First square panel wired (600x600). Display path + SD card only, same precedent as Inkplate6FLICK/6PLUSV2's own first pass (steps 24/25) — touch/frontlight/battery/BME688/APDS9960/accelerometer/buzzer/VCOM-EEPROM deliberately out of scope despite this board's Arduino driver carrying all of them.

    Two genuine quirks found by reading the pasted source carefully rather than assuming precedent: (1) `display3b()`'s hardware push loop is `for(k<8)`, one phase short of every other wired board's `for(k<9)`. The first pasted `WAVEFORM3BIT` macro only initialized 8 of `waveform3Bit[8][9]`'s 9 declared columns (a paste truncation, not the real data) — the user then supplied the full 9-column macro, which turned out to follow the exact same bookend convention every other board already uses: phase 0 *and* phase 8 both all-zero discharge/park rows. `waveform.phases=9` (not 8) to match, since the 9th (unused-by-`display3b()`) phase is a harmless no-op push, and matching every other board's field convention was worth one extra no-op pass over hand-rolling a one-off `.phases=8`. (2) `display1b()` uses **10** black-push phases, not the usual 5 — `INKPLATE_MONO_WAVE_MAX_PHASES` bumped 6→11 in `waveform.h` to fit. Verified this is NOT a copy-paste artifact and NOT a reversed-role variant like Inkplate6PLUSV2 (step 25): this board's own pasted `GraphicsDefs.h` `LUTB`/`LUT2` arrays are byte-for-byte identical to `inkplate_gen_mono_wave`'s standard `op_blk`/`op_bw` output (cross-checked against `test_waveform.c`'s existing `expected_blk`/`expected_bw`, not hand-derived) — so it's the standard scheme, just run 10 times instead of 5. `inkplatemodule.c`'s `inkplate_mono_display` special-cased onto `black_phases=10` by pointer check, same convention as the Inkplate6FLICK/6PLUSV2 branches. `partial_reps=9` from the real `partialUpdate()`'s `for(k<9)` loop.

    **Verified (HIL)** on real Inkplate4TEMPERA hardware (600x600 panel confirmed): mono (rectangle/filled circle/crossing lines/text via a single full `display()`) and GS3 (8-level gray ramp across 8 bands + shapes at distinct levels, via a single full `display()`) both rendered correctly, correct polarity, no ghosting/washout/scrambling — confirming the 10-phase mono scheme and the corrected 9-phase (bookend-zero) GS3 waveform table are both right, not just internally consistent. Mono: 554ms clean + 136ms draw. GS3: 552ms clean + 305ms draw (includes the harmless extra no-op 9th phase). **Follow-up pass, verified (HIL)**: partial update and SD confirmed working on real Inkplate4TEMPERA hardware — no issues found.

    Also caught before writing any Python: this board's GS4 nibble-packing order (`pixelMaskGLUT`) is the *opposite* of Inkplate6PLUSV2's own `write_pixel_viper` — pixel index 0 (even x) writes the **high** nibble here (`color<<4`, mask `0x0F`), not the low nibble — derived directly from this board's own pasted `writePixelInternal`, not copied from a sibling board's Python file. 1bpp packing (`pixelMaskLUT[x_sub]=1<<x_sub`) is the same LSB-first convention every board already uses.

    Added `board_config_inkplate4tempera` to `board_config.{h,c}`; `select_board()` extended; host test cases added to `test_board_config.c` and a new `test_mono_wave_10phase_parity` to `test_waveform.c` (all pass). `boards/inkplate4tempera/*.py` written C-usermod-backed from scratch, mirroring Inkplate6PLUSV2's 4-file structure (`inkplate4tempera.py`/`inkplate_gs.py`/`inkplate_mono.py`/`inkplate_partial.py`). SD wired this pass (user asked for it up front, not after the fact like prior boards): `SD_PMOS_PIN` (pin 11) on the internal expander (0x20), SPI pins (miso=12/mosi=13/sck=14/cs=15) identical to every other classic-ESP32 board's own already-verified numbers, cross-checked against this board's own pasted `spi2.begin(14, 12, 13, 15)`/`SdSpiConfig(15, ...)`. `power_on()`/`power_off()` PMIC sequence carries over Inkplate6/5v2/6PLUSV2's generic register-poke sequence (not independently confirmed — this board's real `einkOn()` delegates to a `TPS65186::powerUp()` library call whose internals weren't pasted). **Verified (HIL)**: mono (10-phase) + 8-level GS (8-phase) + partial update + SD + basic examples all confirmed working on real Inkplate4TEMPERA silicon.
27. **[DONE]** Collapse duplicated Python (RTC, power) into shared `shared/` versions across all 6 parallel-bus boards; delete per-board copies. `gfx.py` dedup is superseded here if Phase 7 step 17's C port already landed for Inkplate10 — extend that C path to the other boards instead of deduping the Python copy. Once every board is off `shared/gfx.py`/no longer needs it, delete `shared/shapes.py` too — confirmed dead code since step 17 (grepped every board + example: `Shapes.__mix_me_in` runs on all 6 boards but none of its methods are ever called), left in place until now only because deleting unused code on boards not yet touched by this refactor felt premature. Fix the `_tps65186_read` missing-`return` bug once, in the shared layer. Fix the standing `power_off` TODO (`# TODO: also tri-state gpio pins to avoid current leakage during deep-sleep`, duplicated across board files) — real power-draw bug on a battery-powered device, cheap to fix once, here. `shared/mcp23017.py`'s original "delete as dead code" ask is superseded — step 22's own follow-up already revived it as a real MCP23017 driver for classic Inkplate6, so it stays. **Verify (HIL)**: all boards still render post-dedup; `_tps65186_read` returns real temperature; measure deep-sleep current draw before/after the tri-state fix to confirm the leakage is actually gone.

    **All pieces done.** TPS65186/RTC/gfx-port-extension/`shared/shapes.py`+`shared/gfx.py` deletion, and now the `power_off` tri-state fix below.

    User supplied the real Arduino `TPS65186.cpp/.h`; ported into `shared/tps65186.py` (`TPS65186` class: `begin`/`power_up`/`power_down`/`read_temperature`/`enable_rails`/`read_power_good`/`write_reg`/`read_reg`, plus a standalone `read_battery_voltage()` for the unrelated ESP32-ADC battery path), wired into all 6 boards' `power_on`/`power_off`/`read_temperature`, replacing every board's own `_tps65186_write`/`_tps65186_read`/raw register pokes. Real bugs fixed, not just deduped: `_tps65186_read`'s missing `return` is gone (whole method deleted, not patched); every board's guessed `power_on` (`# ???` register writes, VCOM asserted before rails/PWR_GOOD checked) replaced with the real enable-rails → poll `PWR_GOOD` (`0xFA`) → VCOM order, `power_up()` now returns/raises on timeout instead of proceeding blind; `power_off` previously never disabled rails via I2C at all (real battery-drain bug on a battery-powered device) — now does. Inkplate5v2's `read_temperature` no longer does a wasteful full `power_on()`/`power_off()` around the read (real driver only toggles WAKEUP+PWRUP). Incidental fix: `inkplate6.py` imports `mcp23017` but `package.json` never shipped it — fixed. **Real bug found+fixed via HIL, not review**: every board has two `begin()` methods — a dead `_Inkplate` classmethod and the real `Inkplate` instance method examples actually call, which duplicates the init sequence instead of calling the classmethod — first pass wired the TPS65186 construction into the dead one only, so `power_on`/`eink_on` would have thrown `AttributeError: no attribute '_tps'` on every board; fixed by also constructing `_Inkplate._tps` in the real instance-level `begin()` on all 6 boards. Also closed a scope gap while in here: `read_temperature()` (classmethod + instance wrapper) added to Inkplate6FLICK/Inkplate6PLUSV2 (never wired before, unlike 10/6/5v2) — free once `cls._tps` exists everywhere, no new pins needed. `read_battery()` also added to those two: user supplied the real `readBattery()` from `Inkplate6FLICKDriver.cpp`/`Inkplate6PLUSDriver.cpp` — expander pin 9 auto-detects PMOS-only vs PMOS+NMOS board revision instead of assuming one fixed polarity like 10/6/5v2 do, and uses the ADC's calibrated `read_uv()` (matching Arduino's `analogReadMilliVolts()`) rather than 10/6/5v2's raw-code magic-constant formula — new `read_battery_voltage_autodetect()` in `shared/tps65186.py`. Inkplate4TEMPERA's own battery reading is via a real fuel-gauge IC, not a resistor-divider+ADC — out of scope here, see the Phase 11 note below. **Verified (HIL)** on real Inkplate4TEMPERA, Inkplate6FLICK, and Inkplate6PLUSV2 hardware: `power_on`/`power_off` (real `PWR_GOOD`-polled sequence), `read_temperature` (23-26°C, plausible), and on 6FLICK/6PLUSV2 `read_battery` (4.21-4.214V, plausible charged-LiPo) all work; mono `display()` unaffected (timings match each board's own pre-existing baseline). Inkplate10/6/5v2 share the identical code shape but weren't re-flashed/HIL-checked this pass.

    **RTC dedup [DONE]**: byte-for-byte identical `rtc_dec_to_bcd`/`rtc_bcd_to_dec`/`rtc_set_time`/`rtc_set_date`/`rtc_get_rtc_data` on Inkplate10/6/5v2 (confirmed identical via diff before deduping) collapsed into `shared/rtc.py` (`RTC` class: `set_time`/`set_date`/`get_data`), same shape as the TPS65186 dedup — `cls._rtc = RTC(cls._i2c)` constructed in both `begin()` methods, old inline BCD/register code deleted. Confirmed Inkplate6FLICK/6PLUSV2/4TEMPERA's hardware also carries the same RTC chip at the same address (0x51) — user confirmed after asking rather than guessing — so `shared/rtc.py` was wired onto those 3 boards too, closing another scope gap the same way `read_temperature`/`read_battery` were closed above (was previously wired on 0 of those 3 boards, now on all 6). **Verified (HIL)** on real Inkplate6PLUSV2: `rtc_set_time(9,39,10)` + `rtc_set_date(5,9,2,2024)` read back byte-for-byte identical via `rtc_get_data()`. Inkplate10/6/5v2/6FLICK/4TEMPERA not re-flashed/HIL-checked this pass (same code shape, lower risk).

    **`gfx.py` C-port extension [DONE, discovered already complete]**: checking before writing any new code found all 5 other boards (6/5v2/6FLICK/6PLUSV2/4TEMPERA) already call the identical set of 12 `inkplate.gfx_*` C bindings Inkplate10 got in step 17 (`gfx_line/rect/fill_rect/circle/fill_circle/triangle/fill_triangle/round_rect/fill_round_rect/hline/vline/draw_char`, Inkplate5v2 additionally has its own `gfx_set_mirror_x`) — this happened organically during each board's own Phase 8 port (steps 22-26), just never logged as closing this step 27 item.

    **`shared/shapes.py` deletion [DONE]**: confirmed zero importers repo-wide (not even the 3 still-Python-gfx SPI boards touch it), deleted. **`shared/gfx.py` also deleted** (not originally scoped, found while investigating shapes.py's status at user's prompt): turned out fully orphaned too — Inkplate2/6COLOR/13SPECTRA (the only boards still using a `GFX` class, Phase 9 territory) each ship their own already-diverged local copy (`boards/<board>/gfx.py`, confirmed different from `shared/gfx.py` via diff — different sizes, different text-rendering logic for their dual BW+RED framebuffers), not the shared one. Nothing in the repo pointed at `shared/gfx.py` after Inkplate10's step-17 port landed. Dangling `shapes.py` manifest entries removed from Inkplate2/6COLOR/13SPECTRA's `package.json` (they listed it despite never importing it).

    **`power_off` tri-state fix [DONE]**: new `shared/epd_power_pins.py` (`tristate_display_pins`/`restore_display_pins`) sets the ESP32-native bit-banged control/data bus (CL=GPIO0, LE=GPIO2, CKV=GPIO32, SPH=GPIO33, D0-D7=4/5/18/19/23/25/26/27 — confirmed identical across all 6 boards from the "Confirmed in-repo facts" list at the top of this doc) plus the expander-driven OE/GMODE/SPV lines to `INPUT` (high-impedance) in `power_off()`, and restores them to their normal driven-`OUTPUT` idle levels in `power_on()` — ported from the real Arduino reference driver's `pinsZstate()`/`pinsAsOutputs()` (`Inkplate6FLICKDriver.cpp`, pasted earlier this pass), minus the I2S GPIO-matrix connect/disconnect step Arduino does inline there: this project's own `epd_i2s_init()`/`epd_i2s_deinit()` already own that at the right times, so only the plain pin-mode toggle needed replicating. Takes the expander-owned `GpioPin` objects (`cls.EPD_OE`/`EPD_GMODE`/`EPD_SPV`) directly and reaches into their already-stored `.pcal6416a`/`.pin` attributes rather than needing each board's differently-named raw expander attribute (`_PCAL6416A_1` vs `_expander1` vs `_PCAL6416A`) — works unchanged for MCP23017-backed classic Inkplate6 too, since `shared/mcp23017.py` already matches `PCAL6416A`'s `pin_mode`/`digital_write` shape. `TPS_WAKEUP`/`PWRUP`/`VCOM` deliberately left untouched (Arduino's own `pinsZstate()` doesn't tri-state these either — they're PMIC control lines meant to stay actively driven low when idle, not floated). Wired into all 6 boards' `power_on()`/`power_off()`. **Verified (HIL)** on real Inkplate6PLUSV2: full cycle — `eink_on()` → draw+`display()` (rect/circle/line) → `eink_off()` (tri-state; all 12 native pins read back cleanly as `INPUT` with no crash) → `eink_on()` again (restore + real PMIC re-power-up) → draw a *different* shape (triangle, to rule out stale-panel-content ambiguity) → `display()` again (694ms then 693ms, matching) — triangle-only content confirmed visible on the real panel, proving the restore path actually re-enables full display functionality, not just "doesn't crash." Inkplate10/6/5v2/6FLICK/4TEMPERA not re-flashed/HIL-checked this pass (identical code shape). **Not yet done**: the actual deep-sleep current-draw measurement (multimeter/power profiler) this step's own Verify criteria calls for — needs real hardware measurement the user will do separately; the fix's correctness (pins actually tri-state and un-tri-state, display still works either side of it) is HIL-confirmed, but whether it measurably reduces deep-sleep current is not yet independently verified.

28. **[ATTEMPTED, REVERTED]** Tried replacing `pngle.c`'s per-pixel-callback PNG decode with `stb_image.h` (`stbi_load_from_memory`); reverted back to `pngle.c` after real HIL failures, see revised plan below. Original motivation still stands: HIL timing on Inkplate5V2/6 showed PNG decode (~18s) 2-3x slower than JPEG (~6s) at the same resolution/dithering, traced to pngle firing its draw callback once per pixel (vs JPEG's ROM `tjpgd` firing once per 8x8/16x16 tile) plus several software divides per pixel (`adjust_color`'s RGBA scaling, ring-buffer modulo indexing) on classic ESP32's Xtensa LX6, which has no hardware integer divide. **Why it was reverted**: stb_image fully materializes the decoded image in RAM (unlike pngle's true streaming, no-full-buffer decode), and during PNG defiltering it holds a raw inflated buffer *and* a separate defiltered copy live at once -- peak ~2x the native image size, confirmed via an instrumented host allocator run against a real 800x600 test photo (`coastal.png`, peak ~2.75MB). On this board's 4MB PSRAM (~2.8-3.0MB free after `begin()`+SD init, and shrinking further over a session since MicroPython's own GC heap grows to accommodate large `bytes` reads and never gives that PSRAM back), that peak already exceeds available memory for ordinary, panel-sized-or-smaller photos -- `coastal.png` (800x600, smaller than Inkplate5v2's own 1280x720 panel) reliably failed to decode, while a smaller test file (`bf_part3.png`, 600x448) reliably succeeded alone but could also fail if run right after another decode attempt in the same session. A PSRAM pre-check (parse the PNG header, estimate peak, compare against `heap_caps_get_largest_free_block`) was added to fail fast with a specific reason instead of stb's opaque NULL, but even with a 15% safety margin it wasn't reliably conservative enough -- `coastal.png` still occasionally passed the pre-check and then hit a real allocation failure inside stb_image past that point. That specific failure surfaced a real crash bug during HIL testing: `stbi_failure_reason()` returns a global that starts `NULL` and isn't set by every internal failure path (some allocation-failure paths return `NULL` without calling `stbi__err()`), so handing it unguarded to `snprintf("%s", ...)` caused a `strlen(NULL)` Guru Meditation (LoadProhibited) crash on real hardware, confirmed via `addr2line` against the exact crash PC. Fixed (guard against `NULL` before formatting) and reconfirmed via HIL that the board survives the failure path cleanly after the fix -- but the underlying memory-ceiling problem remained, so the whole approach was reverted rather than keep patching around it.
    - **Revised plan, [ATTEMPTED, REVERTED]**: instead of swapping to a fully-materializing decoder, tried optimizing `pngle.c` itself, since its streaming architecture (no full-image buffer, ever, regardless of image size) is the right shape for this memory-constrained board. Two targeted, localized fixes were implemented: (a) batch pngle's draw callback to fire once per **row** instead of once per **pixel** (new `pngle_draw_callback_t` signature reporting a run of same-(w,h) values plus a count, buffered in `pngle_t` and flushed on row/pass/dimension change and at EOF); (b) replace `adjust_color`'s RGBA-scaling software divides with a precomputed scale strategy (`png_scale.h`, new file, host-tested in `test_png_scale.c`) -- identity for 8-bit (maxval=255, where the divide is already a no-op), `>>8` for 16-bit, a tiny LUT for 1/2/4-bit. Ring-buffer `%` indexing was also replaced with conditional-subtract. Clean host build/test and clean real xtensa (ESP-IDF v5.5.2) build.
    - **HIL measurement told a different story than the theory**: on real Inkplate5V2 hardware, same board/same `coastal.png` (800x600, dithered Floyd-Steinberg), decode-only timing isolated from SD-card read (a separate, already-known ~13.8s cost at this board's 4MHz SD clock -- see step 23's HIL notes) via `time.ticks_ms()` bracketing just the `png_draw_gs4()` call: unmodified `pngle.c` decodes in **3825-3826ms** (2 runs, consistent); with both fixes applied, **4139-4140ms** (3 runs, consistent) -- **~8% slower**, not faster. Isolating fix (b) alone (row-batching force-disabled by setting the batch buffer capacity to 1, so the callback still fires once per pixel through the new code path) measured **4378ms** (2 runs, consistent) -- worse still, meaning the divide-removal alone was a net regression and the row-batching only partially clawed it back. **Root cause, not fully confirmed**: this project's ESP-IDF build uses `CONFIG_COMPILER_OPTIMIZATION_SIZE=y` (`-Os`), under which `png_scale_apply` (a `static inline` helper called 4x/pixel, once per RGBA channel) plausibly isn't actually inlined at every call site -- trading one runtime software divide for up to 4 real function calls would explain a net loss. Forcing inlining (`__attribute__((always_inline))`) to confirm this diagnosis was proposed but not pursued -- **user called a stop to this investigation** rather than continuing to iterate on hardware. **Also surfaced**: the "~18.0s" Inkplate5V2 pngle baseline logged just above in this same step was SD-read + decode combined (13.8s + 3.8s ≈ 17.6s), not decode alone -- the original "PNG decode 2-3x slower than JPEG" framing conflated SD I/O with actual decode cost, so the real decode-only gap (if any) between JPEG and PNG on this board was never actually isolated. All code changes (`pngle.c`/`pngle.h`/`png_decode.c`, `png_scale.h`, `tests/test_png_scale.c`, `ci.yml`) reverted; board reflashed back to the unmodified firmware and reconfirmed working. **Not re-attempted for now** -- if revisited, profile first (confirm the inlining diagnosis, or use a real profiler) rather than trusting the algorithmic-complexity argument that motivated this attempt, since it didn't hold up against real measurement.

## Phase 9 — SPI-controller-panel family: C port (Inkplate6COLOR, Inkplate2, Inkplate13SPECTRA)

Confirmed in-scope for the C refactor, not deferred as optional. Architecturally separate from the I2S/parallel-bus work (these panels have an onboard timing-controller ASIC driven over SPI with CS/DC/RST/BUSY) — can proceed in parallel with Phases 2-8 once Phase 1's config-struct pattern exists to mirror.

29. **[DONE]** Define a per-panel SPI config struct (mirrors Phase 1's parallel-bus struct): SPI pins (CS/DC/RST/BUSY/SCK/MOSI), panel resolution, command set pointer. Note Inkplate13SPECTRA is ESP32-S3, not classic ESP32 — needs its own firmware target, separate from the Inkplate10-family build. **Verify**: host-compiled unit test on struct fields per panel. **Verified**: `firmware/usermods/inkplate/spi_panel_config.{h,c}` — new `spi_panel_config_t` (name/width/height/pin_rst/pin_dc/pin_cs/pin_busy/pin_clk/pin_din/spi_freq_hz/chip_count), deliberately separate from `board_config.h`'s parallel-bus struct (no data_pins/waveform/PMIC fields — this family has none of that). `chip_count` field added now (1 for Inkplate6COLOR/Inkplate2) so Inkplate13SPECTRA's real dual-chip requirement doesn't force a struct-shape change later — the field is not yet acted on by any transport code (see step 30). `tests/test_spi_panel_config.c` (host-compiled, gcc/clang-format-clean, wired into `ci.yml` build+test jobs) asserts `spi_panel_config_inkplate6color`'s fields against the real Arduino reference driver's `pins.h` (user-supplied directly this pass, not guessed).
30. **[DONE for Inkplate6COLOR]** Reordered ahead of Inkplate2 at user's request (user supplied Inkplate6COLOR's real Arduino reference driver — `Inkplate6COLORDriver.cpp/.h`, `pins.h` — directly in-conversation, so its command sequence is confirmed, not guessed; Inkplate2/Inkplate13SPECTRA's own `[NEEDS INPUT]` still stands). Ported the command-send loop to C, config-driven, generic across every single-chip panel in this family (not Inkplate6COLOR-specific) so Inkplate2 can reuse it unchanged later. `firmware/usermods/inkplate/epd_spi.{h,c}` — `epd_spi_init/deinit` (VSPI/SPI3_HOST + RST/DC/CS as GPIO outputs, BUSY as input; matches the pre-refactor Python driver's `machine.SPI(2)` bus choice, whose default pins already equal this panel's `pin_clk`/`pin_din`), `epd_spi_reset` (RST pulse, timings from the real Arduino `resetPanel()`), `epd_spi_set_rst` (holds RST low during deep sleep, matching the reference driver's `setPanelDeepSleep(true)` rather than leaving it floating), `epd_spi_wait_busy` (bounded-timeout or infinite-wait via `timeout_ms==0`, matching the reference's untimed busy-loops in `display()`/`clean()` vs its timed init-wait), `epd_spi_send_command`/`epd_spi_send_data` (CS/DC framing + explicit 10us/1ms settle delays transcribed from the real Arduino `sendCommand()`/`sendData()` — the pre-refactor Python driver had no such delays, relying on MicroPython interpreter call overhead as an implicit margin the same way `vscan_start`/`write`/`end` did before Phase 2 step 7's `esp_rom_delay_us(0)` fix; restored explicitly here rather than re-learning that lesson on real hardware). `epd_spi_transfer` chunks any payload into ≤4092-byte `spi_device_transmit()` calls (ESP-IDF's default single-transaction cap), since Inkplate6COLOR's full-framebuffer write (134400 bytes) is far larger than that limit — `bus_conf.max_transfer_sz` set to the same chunk constant explicitly so the two can't drift apart. Bound into the `inkplate` module as `select_spi_panel(name)`/`spi_panel_init()`/`spi_panel_deinit()`/`spi_panel_reset()`/`spi_panel_set_rst(level)`/`spi_panel_wait_busy(level, timeout_ms)`/`spi_panel_send_command(cmd)`/`spi_panel_send_data(bytes)` — a separate static selection slot from the parallel-bus family's `select_board()`/`active_board`, not reusing it. `boards/inkplate6color/inkplate6_color.py` rewired onto these bindings: `begin()`/`reset_panel()`/`send_command()`/`send_data()`/`display()`/`clean()`/`set_panel_deep_sleep()` all now go through C for the SPI transaction itself, while every command/data *sequence* (which register bytes, in what order) stays unchanged Python, since that's not a hot loop — only the actual bit transfer (and especially the one big framebuffer write per `display()` call) needed to move to C. Dropped the now-fully-owned-by-C `EPAPER_RST/DC/CS/BUSY/CLK/DIN` Python pin constants and the `machine.SPI` import entirely; normalized the register-byte call sites (previously an inconsistent mix of Python `str` constants like `PANEL_SET_REGISTER = "\x00"` and raw `bytes` literals like `send_command(b"\x61")`) to plain ints throughout, since the new C binding's signature (`uint8_t`) forced one canonical representation. **Verified (HIL)** on real Inkplate6COLOR hardware: firmware built clean (`make ... clean` first, since new usermod `.c` files were added — same gotcha as prior phases) and flashed; `begin()` completed (reset, busy-wait, full init command sequence) with `_panel_state=True`; `clean()` + shape/text drawing (`draw_rect`/`fill_circle`/`draw_line`/`print_text`) + `display()` rendered correctly on the real panel, repeatable. Timing comparison against the pre-refactor Python driver (same draw calls, same panel, both via `mpremote exec`, a hardware reset between runs to clear ESP-IDF's own SPI-peripheral-claim state which `machine.SPI`'s host-conflict check doesn't release on a mere soft-reset): C path 31238ms vs original Python path 30612ms — a wash (~2%, noise-level), not a regression. This is expected, not a miss: unlike the parallel-bus boards where I2S+C beat bit-banged GPIO because *that* loop was the real bottleneck, `display()` here is dominated by the panel ASIC's own ~30s internal refresh (BUSY-wait) — the 134KB SPI transfer itself is ~0.5s at 2MHz, negligible either way. The port's value is generic infrastructure for Inkplate2 (step 31) and centralized, explicit timing margins, not raw speed. `spi_freq_hz` (2MHz, from the Arduino reference, not the pre-refactor driver's untested 20MHz) not yet independently pushed higher — no evidence needed to raise it since current speed isn't the bottleneck.

    **SD card wired this pass too** (user request, "make it consistent with the parallel-bus boards"), same `machine.SDCard(slot=3, miso=12, mosi=13, sck=14, cs=15)`/4MHz pattern every parallel-bus board already uses, with this board's own real SD-power P-MOS pin: `SD_ENABLE = GpioPin(cls._PCAL6416A, IO_PIN_B2, mode_output)` (internal-expander pin 10, active-low, matches the real Arduino `pins.h`'s `SD_PMOS_PIN`/`sdCardInit()`). `init_sd_card()`/`sd_card_sleep()`/`sd_card_wake()` toggle it the same way Inkplate10/6/6FLICK/6PLUSV2/4TEMPERA already do. **Real, non-obvious bug found and fixed via HIL, not guessed**: SD mount initially failed with an opaque `OSError(-261, 'ESP_ERR_NOT_FOUND')` from inside `SDCard()` construction, but only *after* `begin()` had run — SD alone (no panel touched) always mounted fine. Root-caused through several wrong turns, corrected in the open rather than silently: first suspected (and confirmed via `ports/esp32/machine_sdcard.c`) that `machine.SDCard(slot=3)` and this panel's SPI transport shared `VSPI_HOST` — this was actually a misread of that file's `spi_dev_defaults[]` array (the `VSPI_HOST` entry is index 0/`slot=2`, not index 1/`slot=3`); moving the panel to `SPI2_HOST`/HSPI on that wrong theory *created* a real collision, since `slot=3` (confirmed via `esp_driver_sdspi/include/driver/sdspi_host.h`'s `SDSPI_DEFAULT_HOST`) is actually HSPI/`SPI2_HOST` on classic ESP32, not VSPI. Reverted the panel to its original, correct `VSPI_HOST` — but SD mount still failed post-`begin()`, proving the host-sharing theory was never the real cause. Actual root cause: classic ESP32 has only 2 SPI-DMA channels total; `machine.SDCard`'s `slot=3` path hardcodes DMA channel 1 (`spi_dma_channel_defaults[1] == 1` in the same MP source), while `epd_spi_init()` requested `SPI_DMA_CH_AUTO` — since the panel's `begin()` always runs first, its auto-pick could claim channel 1 before SD ever asked for it, leaving none free and breaking SD's mount with that same opaque error. Fixed by pinning the panel to DMA channel 2 explicitly (`EPD_SPI_DMA_CHAN`, matching the *other* table entry's own VSPI/`slot=2` default), so the two boards' channel choices can never collide regardless of init order. **Verified (HIL)** on real Inkplate6COLOR hardware, fresh-reset each time to rule out stale cross-test state (a real confound hit repeatedly while chasing this): `begin()` → `init_sd_card()` → `clean()` (SD still mounted after) → draw shapes/text → `display()` (31224ms, matching the no-SD baseline) → SD file read afterward, all in one uninterrupted run, repeatable. Also drew a real image end-to-end: `draw_bmp_from_sd('/sd/coastal.bmp', ..., dither=True)` (46226ms decode+dither) then `display()` (31205ms), visually confirmed correct on the panel by the user. That decode time is expected, not a bug — it's the pre-refactor `@micropython.viper` BMP-decode-plus-dither path (`bmp24_to_rgb565`/`write_image`), not a C decoder; the C decoders from Phase 7 (`bmp_decode.c`/`jpeg_decode.c`/`png_decode.c`/`dither.c`) exist but output grayscale (GS4) for the parallel-bus boards, with no 7-color-palette nearest-match+dither path yet — that's Phase 10's job, not this step's. `draw_jpg_from_sd` was also tried and confirmed **not working on this firmware build** (`ImportError: no module named 'jpeg'` — a MicroPython built-in JPEG-decoder module this build doesn't include, unrelated to and out of scope for this SPI-transport step).
31. **[DONE]** Repeat command-port for Inkplate2, then Inkplate13SPECTRA (ESP32-S3 build, and its real 2-SPI-chip requirement — `epd_spi.c` today only implements the `chip_count==1` case, see step 29's struct note). Image decode/dithering for these panels is handled in Phase 10, not here. **Verify (HIL)**: each panel's examples render correctly post-port.
    Added `spi_panel_config_inkplate2` (`firmware/usermods/inkplate/spi_panel_config.{h,c}`, pins/resolution/1MHz SPI clock from the real Arduino reference driver's `Inkplate2Driver.cpp/.h`/`pins.h`, user-supplied directly this pass, not guessed) and registered it in `inkplatemodule.c`'s `select_spi_panel(name)`. `tests/test_spi_panel_config.c` extended with a second assertion block for it (host-compiled, passing). `boards/inkplate2/inkplate2.py` rewired onto the shared `epd_spi` C transport (`select_spi_panel`/`spi_panel_init`/`spi_panel_reset`/`spi_panel_set_rst`/`spi_panel_wait_busy`/`spi_panel_send_command`/`spi_panel_send_data`) the same way `inkplate6_color.py` was in step 30 — dropped its own `machine.SPI`/pin-toggling code entirely, normalized register command bytes from `bytes` literals to plain ints (the C binding's `uint8_t` signature forces this), and fixed the driver's busy-wait timeouts, which didn't match either of the reference driver's own two distinct values (`BUSY_TIMEOUT_MS 1000` for init/sleep waits vs. an inline `60000` for `display()`'s post-refresh wait — the pre-refactor driver had incorrectly conflated both into one `busy_timeout_ms = 30000` constant).

    Three real hardware-behavior gaps surfaced comparing the reference driver against the already-HIL-verified shared `epd_spi.c` (built for Inkplate6COLOR in step 30) — resolved with the user rather than guessed:
    - Sleep/wake pattern: Inkplate2's reference driver fully tears down SPI + floats all control pins on every sleep, then fully reinits on every wake. Kept 6COLOR's simpler existing pattern instead (init once in `begin()`, only `spi_panel_set_rst(0/1)` toggles across sleep/wake) — user's call, accepting the small idle-current tradeoff rather than extending `epd_spi.c`'s teardown/reinit surface for this one panel.
    - Reset pulse width: reference driver's `resetPanel()` uses 100ms low / 100ms settle; the shared `epd_spi_reset()` (calibrated only against Inkplate6COLOR's own spec) hardcoded 1ms low / 200ms settle. Bumped the shared low-pulse to 100ms (settle unchanged, already ≥ both panels' specs) — longer reset pulse is safe for 6COLOR too, but this is a change to already-HIL-verified shared code, **not re-verified via HIL on Inkplate6COLOR after this change**.
    - BUSY pin pull-up: reference driver uses `INPUT_PULLUP`; shared `epd_spi_init()` had no pull configured. Changed to `GPIO_PULLUP_ENABLE` in the shared config (same reasoning — harmless for 6COLOR, matches Inkplate2's real spec).
    - Also identified and skipped a likely copy-paste artifact in the reference driver's `initDriver()` (a one-time block driving pins `5,18,19,23,33,32` low with a 2s delay — pin 5 isn't any of Inkplate2's named pins, and CS(27) is conspicuously absent from that list) — the pre-refactor Python driver never ported it either.

    **Verified**: host-compiled `test_spi_panel_config` passes; `clang-format`/`ruff format`/`ruff check` clean on all touched files (the 11 pre-existing `ruff` `F821`s on MicroPython's `micropython.native`/`ptr8`/`ptr32` builtins are unrelated, present before this change too). **Verified (HIL)** on real Inkplate2 hardware: firmware built clean (full rebuild + reflash, same USER_C_MODULES path as 6COLOR) and flashed; `begin()` returned `True` (reset, `0x04` busy-wait, full init sequence all completed without timing out — confirms the 100ms reset pulse and BUSY pull-up changes work on real hardware, not just in theory); `clear_display()` + `fill_rect` (black + red) + `print_text` + `display()` rendered correctly on the real panel (colors correct, not garbled/inverted/mirrored, user-confirmed visually) in ~9.3s, panel correctly back in deep sleep after. One transient USB-serial disconnect during a `mpremote run` mid-test (board stayed alive/responsive throughout, unrelated to firmware — did not recur on retry).

    **Inkplate13SPECTRA** (user supplied the real Arduino reference driver directly — `Inkplate13SPECTRADriver.cpp/.h`, `pins.h`, including `display()`/`displayPartial()` — in-conversation). Unlike 6COLOR/Inkplate2, this panel's real protocol has no DC phase at all (`sendCommand()` never touches DC) and needs a dual-chip CS selection (master/slave/both, one SPI-controller ASIC per screen half sharing SCK/MOSI) — rather than force-fitting that onto `epd_spi_send_command`/`epd_spi_send_data`'s DC-framed single-CS shape, added a parallel `epd_spi_dual_*` API (`firmware/usermods/inkplate/epd_spi.{h,c}`): `epd_spi_dual_pins_low`/`power_up_io`/`power_down_io` (pin mode transitions, matching the reference's `setPanelPinsToLow()`/`setIO()`/power-down pin-float step byte-for-byte, including reconstructing the SPI bus+device on every power-on cycle the way the Arduino `SPI` object does), `epd_spi_dual_set_power` (PWR_EN), `epd_spi_dual_select`/`deselect` (chip-mask CS control, slave-then-master order matching the reference's `sendCommand()` exactly), and `epd_spi_dual_write` (raw chunked SPI write, no framing of its own — callers bracket it with select/deselect, same shape as the reference's CS-window-then-several-writes pattern). `epd_spi_reset`/`epd_spi_set_rst`/`epd_spi_wait_busy` are reused unchanged (RST/BUSY are single shared lines here too).

    Added `spi_panel_config_inkplate13spectra` (`chip_count = 2`, plus new `pin_cs2`/`pin_pwr_en`/`pin_bs0`/`pin_bs1` fields on `spi_panel_config_t` — read only when `chip_count == 2`, zero/unread for the two single-chip boards, same convention `chip_count` itself already established) and registered it in `inkplatemodule.c`'s `select_spi_panel(name)`, alongside new `spi_dual_*` Python bindings. `tests/test_spi_panel_config.c` extended with a third assertion block (host-compiled, passing). `boards/inkplate13spectra/inkplate13_spectra.py` rewired the same way as step 30/31's other two boards: dropped its own `machine.SPI`/`Pin` object management for the panel entirely; `screen_init()`'s register sequence, `set_panel_state()`'s power on/off sequencing, and `display()`/`clean()`'s per-chip framebuffer-row loop all stay as Python business logic (unchanged shape from the pre-refactor driver, just now calling `inkplate.spi_dual_*` instead of toggling `Pin`/`SPI` objects directly) — matching this family's established principle that only the actual SPI bit-transfer moves to C, not the command sequencing.

    Reset pulse width follows the same precedent set for Inkplate2 in step 31 above: the reference's `resetPanel()` asks for 100ms low / 100ms settle, reused the shared `epd_spi_reset()` (100ms low / 200ms settle) unchanged rather than adding a third variant — longer settle is safe, not re-verified via HIL on the other two panels after 6COLOR/Inkplate2's own prior bump (unchanged by this step).

    **Deliberately not ported this pass**: `displayPartial()` (windowed partial update via the `PTLW` register, with its column/row alignment math and per-chip null-window fallback) — Phase 9's own scope for steps 29-31 is the SPI transport only; partial update for this panel would be new functionality beyond what the pre-refactor Python driver had (it only had `display()`/`clean()`), not a like-for-like port, and its rotation/alignment math is intricate enough that shipping it unverified (no ESP32-S3 hardware access this pass) risked a silently-wrong window. Flagged here rather than silently dropped — a real gap vs. the reference driver, pick up as its own task if wanted.

    **Real ESP32-S3 build bug found and fixed via first real firmware build attempt, not guessed**: `epd_i2s.c` (parallel-bus I2S transport, Phase 3 — irrelevant to this SPI-only board but still compiled into every board's usermod binary) failed outright on the `ESP32_GENERIC_S3` target: classic ESP32's I2S1 register fields it uses (`out_link`, `lc_conf`, `conf`/`conf1`/`conf2`/`conf_chan`, `sample_rate_conf`, `clkm_conf`, `timing`, the legacy linked-list-descriptor DMA layout) don't exist on ESP32-S3's `i2s_dev_t` at all — S3 replaced that whole mechanism with GDMA. Fixed by wrapping the file's body in `#if CONFIG_IDF_TARGET_ESP32` with no-op stub definitions on the `#else` branch (`firmware/usermods/inkplate/epd_i2s.c`) — these functions are only ever reached via `select_board()` (parallel-bus boards), which no ESP32-S3 board in this repo calls, so the stubs exist purely so the usermod still links. `epd_bitbang.c` (also compiled unconditionally) uses only `GPIO.out_w1ts`/`out_w1tc`, which exist identically on S3, so it needed no change.

    **Verified**: host-compiled `test_spi_panel_config` passes (3rd assertion block for `spi_panel_config_inkplate13spectra`); `clang-format`/`ruff format` clean on all touched files. **Verified (HIL)** on real Inkplate13SPECTRA (ESP32-S3) hardware: full firmware build (`BOARD=ESP32_GENERIC_S3 BOARD_VARIANT=SPIRAM_OCT`) succeeded clean after the `epd_i2s.c` fix above, flashed; `begin()` returned `True`; `clear_display()` + `fill_rect()` in all four testable colors (black/red/yellow/blue) + `display()` rendered correctly on the real panel — correct colors in all four quadrants, not garbled/mirrored/wrong-half, user-confirmed visually — in ~21.7s (full dual-chip 6-color refresh, dominated by the panel ASIC's own BUSY-wait, same expected shape as 6COLOR's step 30 finding). Also verified `init_sd_card()`'s pre-existing separate-SPI-bus SD path (untouched by this port) has no DMA-channel conflict with the new dual-chip panel transport's `SPI_DMA_CH_AUTO` choice on ESP32-S3 (unlike classic ESP32's 2-DMA-channel constraint that bit Inkplate6COLOR in step 30) — mounted SD, listed files, ran `display()` (21.7s, unchanged), re-listed SD afterward, all in one uninterrupted run, no crash, no mount failure. `displayPartial()` remains un-ported (see above) — everything else in this step is now HIL-confirmed.

## Phase 10 — Image decode + dithering: color/palette path (SPI panels)

Completes the shared dithering engine scaffolded in Phase 7 (JPEG/PNG/BMP decoders, grayscale dithering) with the general-palette path — deferred until here since it needs Phase 9's SPI-controller boards to verify against, not just Inkplate10.

32. **[DONE, HIL-verified on Inkplate13SPECTRA and Inkplate6COLOR]** Extended `dither.c`/`dither.h` with the general N-color RGB palette path: `dither_palette_entry_t` (RGB + output `value`, since 13SPECTRA's controller skips register value 4), `dither_quantize_palette` (brute-force nearest-search, one shared function, not per-board unrolled), `dither_invert_palette_bw` (swaps only a pure-black/pure-white entry pair — replaces each board's old whole-nibble XOR invert, which produced out-of-palette values for every board in scope, 3/6/7 entries, none a full 16), and a 3-channel `dither_rgb_ctx_t` (error diffusion needs the full RGB vector here, not one luma channel). Added matching `bmp_draw_palette`/`jpeg_draw_palette`/`png_draw_palette` to the existing decode files — callback-shaped (`write_pixel(ctx, x, y, value)`), not `fb`/`phys_w`/`phys_h`/`rotation`/`x0`/`y0` params, because 6COLOR/13SPECTRA/Inkplate2 turned out to each need real per-board packing, not one shared layout: 6COLOR/13SPECTRA's 4bpp nibble order is the *opposite* of `gfx_set_pixel`'s existing GS4 packing (confirmed from their real write_pixel/write_image), their two rotation formulas are mirror-opposite of each other and don't match `gfx_set_pixel`'s rotation math either, and Inkplate2 is a fundamentally different format (two 1bpp bitplanes, not one indexed buffer) with rotation honored dynamically (the other two hardcode a fixed orientation). All of that per-board packing/rotation logic is now in a new `spi_panel_palette.c`/`.h`, ported verbatim from each board's current viper code and unit-tested against hand-computed expected byte values (`tests/test_spi_panel_palette.c`). Palette RGB values are **not** a `[NEEDS INPUT]` blocker: 6COLOR's 7 and 13SPECTRA's 6 entries are transcribed verbatim from their current shipped nearest-color search (already HIL-validated per step 30's real-image draw), including 13SPECTRA's register-value gap. Inkplate2 is the one exception, flagged explicitly rather than silently ported: its current code doesn't do a real nearest-palette search at all (three inconsistent RGB-threshold heuristics across its JPEG/PNG/BMP paths, one of which — the JPEG path — even swaps black and white relative to the real `WHITE=0`/`BLACK=1` constants), so it's been generalized to a real 3-point (black/white/red) nearest-RGB search per this phase's stated design instead of byte-for-byte matching any one of the old heuristics. **Still needs HIL on Inkplate2 specifically** (not tested this pass, no board on hand): whether this reclassifies borderline reddish/brown pixels differently than the old heuristic, and that PNG alpha=0 pixels now nearest-match on RGB (0,0,0) (black) instead of the old code's explicit "transparent → white" special case (the shared engine ignores alpha generically, like the mono/GS4 path already did).

    **Real bug found via HIL on Inkplate6COLOR, not caught by any host test or code review, fixed through three iterations**: `jpeg_draw_palette`/`png_draw_palette`'s dither path buffers the full decoded RGB image before diffusing (tiles/pixels can arrive out of row order). The first cut capped that buffer at a fixed 1200x1600 (13SPECTRA's physical size, ~5.76MB at 3 bytes/pixel) regardless of which board called it — `heap_caps_malloc` returned NULL outright on 6COLOR (confirmed via `esp32.idf_heap_info()`: 4.1MB total free PSRAM, but only a ~2.8MB largest *contiguous* block — fragmentation, not a true OOM). Capping at the calling board's own panel size instead (600x448 for 6COLOR) was tried next and was also wrong in a different way: it rejected a completely ordinary 1200x825 source photo outright (tile-bounds check aborted the decode), when oversized source images should just lose dithering, not fail to draw at all — draw already clips oversized sources to the panel elsewhere in this codebase. Final fix: cap the buffer at the panel's own size (avoids allocating PSRAM for pixels that would be clipped away anyway), but when the source image turns out bigger than that cap, `jpeg_draw_palette`/`png_draw_palette` now retry the whole decode with dithering disabled instead of failing (both decoders are stateless/re-entrant across calls, so this is a clean full redo, not a half-drawn image) — same graceful-degrade-on-transient-OOM philosophy `jpeg_palette_dither_pass`'s own small error-diffusion-context fallback already used, just extended to the big buffer too. **Verified (HIL)**, both boards, SD-card images, `clean()`+`display()` each time, visually confirmed correct by the user:
    - **Inkplate13SPECTRA** (1200x1600 panel, so the cap covers real photos directly): `coastal.bmp`/`coastal.png` dithered-decode timings 23s/2.7s (vs 0.56s/1.93s undithered) plus a `mountain.jpg` (1200x825) dither-only isolated timing of 1.95s (3.72s decode+dither minus 1.77s decode-only) — all rendered correctly.
    - **Inkplate6COLOR** (600x448 panel, smaller than every test image on hand): `coastal.bmp` (800x600) genuinely dithered (2226ms vs 899ms undithered — a real, measured cost, not a coincidence); `coastal.png` (800x600) and `mountain.jpg` (1200x825) both deterministically fell back to non-dithered (identical dither/no-dither timings each time) since both exceed this panel's own size — correct, intended behavior post-fix, not a bug (pre-fix, this same case either silently degraded by fragmentation luck or hard-crashed, depending on which buggy interim version was flashed).
    - Not exercised on real hardware this pass: a source image smaller than 6COLOR's panel with dithering (would confirm the "fits, dithers for real" path on a board this constrained) — everything on the test SD card happened to exceed 600x448.

    All new host-testable logic (`dither.c` extensions, `bmp_draw_palette`, `spi_panel_palette.c`) passes `gcc -Wall -Wextra -Werror` and its own unit tests; `jpeg_draw_palette`/`png_draw_palette` are ESP-IDF-only like their `_gs4` siblings, syntax-checked against a stub `esp_heap_caps.h`.

    **Follow-up, same HIL session, user pushback on the fallback rate above was correct to push on**: switched `jpeg_draw_palette`/`png_draw_palette`'s scratch buffer from RGB888 (3 bytes/pixel) to RGB565 (`dither_pack_rgb565`/`dither_unpack_rgb565`, new in `dither.h`/`.c`, unit-tested round-trip in `tests/test_dither.c`) — matches what the pre-refactor Python driver decoded to before dithering, a real 33% memory cut, and let `inkplatemodule.c` restore the generous `max(panel_size, 1200x825)` cap (the RGB888 version of that same cap, ~2.97MB, is what originally failed on 6COLOR; at RGB565 it's ~1.98MB). **This alone did not fix it** — retested `coastal.png` (800x600) on real Inkplate6COLOR hardware and it still silently fell back to non-dithered (identical timing again). Root-caused via `esp32.idf_heap_info()` queried at the actual point of allocation (not at fresh boot, which is what the earlier ~2.8MB-largest-contiguous-block figure above came from): reading the 947KB PNG file into RAM via plain `f.read()` — a normal, unavoidable step, nothing exotic — shrank the largest heap-caps-visible contiguous PSRAM block from 2.8MB down to **0.54MB**, i.e. loading one moderately-sized file cost several times its own size in *usable-by-heap_caps_malloc* headroom. Read via `micropython.mem_info()`'s "max new split" figure, this is consistent with MicroPython's own GC heap growing by claiming large fixed-size chunks of fresh (never-yet-allocated) PSRAM on demand, rather than the incremental few-hundred-KB you'd expect from the file's own size — and once claimed, that PSRAM belongs to MicroPython's heap arena permanently (doesn't shrink back), so it's gone from `heap_caps_malloc(MALLOC_CAP_SPIRAM)`'s pool of fragmented leftovers for the rest of the session, regardless of `gc.collect()`. **Leading hypothesis, not yet implemented or verified**: this scratch buffer should probably be allocated as a MicroPython `bytearray` (going through the *same* growing-heap mechanism the file read itself used, which reliably finds room by extending into virgin PSRAM) and handed into the C draw functions via a raw buffer pointer (same `mp_get_buffer_raise` pattern already used for every other buffer in `inkplatemodule.c`), instead of calling `heap_caps_malloc(MALLOC_CAP_SPIRAM)` directly from C, which is stuck searching only the fragmented remainder MicroPython's own heap hasn't already claimed. This is plausibly why the pre-refactor Python driver's own RGB565 buffer (also a `bytearray`, allocated the same way) didn't hit this wall. Not yet implemented -- would touch `jpeg_draw_palette`/`png_draw_palette`'s signatures (accept a caller-supplied buffer instead of allocating one), `inkplatemodule.c`'s two wrappers, and the three boards' `draw_jpg_from_sd/web`/`draw_png_from_sd/web` (allocate + pass the `bytearray`). The C-side dithering/quantization loop itself is unaffected either way — this is purely about who calls the allocator, not the hot loop, so no speed regression expected from making this change. Two smaller alternatives considered and set aside for now: streaming the compressed source file into the decoders instead of `f.read()`-ing it whole (real surgery on both `jpeg_decode.c`'s tjpgd integration and `png_decode.c`'s zlib streaming, and wouldn't fix the bigger cost anyway -- the *output* RGB565 buffer, not the input read, is what's actually failing to allocate); and capping MicroPython's own heap size in this board's firmware build to reserve guaranteed room for `heap_caps_malloc` (a global tuning knob, not scoped to images, with real risk of trading this `MemoryError` for a different one in unrelated code on a board with only 4MB PSRAM total).

    **Follow-up, separate session, the "leading hypothesis" above was implemented and then partially overturned by HIL**: wired the caller-supplied-`bytearray` scratch buffer into `jpeg_draw_palette`/`png_draw_palette` (new `scratch_rgb`/`scratch_cap` params, boards allocate a fresh `bytearray(_DITHER_SCRATCH_BYTES)` per draw call when `dither=True`) exactly as hypothesized. Real Inkplate6COLOR HIL then found the hypothesis only half right: `mountain.jpg` (1200x825, exceeds the panel) dithered clean, `gc.mem_free()` identical before/after -- but `coastal.png` (800x600, well under the 1200x825 cap) threw `MemoryError: memory allocation failed, allocating 1980000 bytes` from the plain `bytearray()` call itself, **with `gc.mem_free()` reporting 3,271,936 bytes free right before it** -- 1.7x the request. So a MicroPython `bytearray` doesn't reliably avoid this after all: the GC heap grows by adding separate fixed-size *areas* on demand (not by extending one contiguous arena), and a single large object must still fit inside one area -- the same "biggest available contiguous block" constraint `heap_caps_malloc` had, just one level up.

    Given that, JPEG's palette path was redesigned instead of patched: tjpgd delivers MCU tiles in strict row-band raster order (`jpeg_decode.h`'s own contract), so unlike PNG's Adam7 interlacing (genuinely non-monotonic, needs the whole image), JPEG only ever needs one MCU-band's rows buffered at a time. `jpeg_draw_palette` now dithers through a small **static** buffer (`JPEG_PALETTE_BAND_MAX_W` x `JPEG_PALETTE_MCU_MAX_H`, ~38KB, `jpeg_draw.c`) instead of any allocation at all -- `scratch_rgb`/`scratch_cap` removed from its signature entirely, boards no longer allocate anything for JPEG dithering. HIL-verified on real Inkplate6COLOR: `mountain.jpg` dithered twice back-to-back, `gc.mem_free()` byte-identical across every draw (3,271,808, zero drift) -- confirms zero heap allocation, so no fragmentation is possible even in principle. PNG keeps the caller-`bytearray` approach (still subject to the same-class `MemoryError`, not fixed this pass) since Adam7 rules out the same trick without also threading pngle's interlace-detection through `png_decode()` -- flagged as follow-up, not done here.

    Also changed, per explicit user request while HIL-testing the above: an oversized-for-dither image no longer silently redraws non-dithered (the design used everywhere above) -- across `jpeg_draw_palette`, `png_draw_palette`, and `bmp_draw_palette` alike, this now returns a distinct code (`-2`) that `inkplatemodule.c` turns into a `ValueError` ("Image too wide to dither" for jpeg/png, "Image too wide" for bmp -- bmp's row-scratch-buffer cap, `BMP_DRAW_MAX_WIDTH`, applies whether or not `dither` is set, unlike jpeg/png's dither-only cap, so its message doesn't suggest `dither=False`). For JPEG this check moved to right after `jd_prepare()` (header parsed, before any MCU entropy decoding -- `jpeg_decode()` gained an optional `pre_cb` hook for this) -- HIL-confirmed near-zero cost (3.4s file read, 1ms decode-abort) on a real oversized `christmas.jpg`. PNG's equivalent (`png_peek_dimensions`, `png_decode.c`) reads IHDR's fixed-offset width/height directly, bypassing pngle entirely -- a first attempt routed the check through pngle's `init_callback` instead (requiring the whole file to be fed in small chunks so the callback could fire before decode finished) and this **caused a real, HIL-observed hang**: a large PNG (`christmas.png`, ~1.5MB) made the board unresponsive to `mpremote`/serial for minutes, requiring a physical power-cycle -- reverted before landing, in favor of the byte-peek (zero interaction with pngle's decode loop, so nothing it does can destabilize). Verified via synthetic in-memory PNG/BMP headers (no real decode, no SD read) rather than real files, after that hang made testing via real oversized files on this board too risky to repeat casually: oversized headers reject in ~0ms with the right message; in-bounds headers correctly fall through to a real decode attempt (a different failure) instead of false-positiving.

    **Not fixed, same underlying GC-heap-fragmentation class, flagged for later**: `f.read()` of a large source file (unrelated to the dither scratch buffer) can itself throw `MemoryError` -- confirmed on `plitvice.jpg` (1.77MB) and `christmas.png` (~1.5MB) -- and the failing allocation search can take 30+ seconds before surfacing, during which the board looks hung (indistinguishable from a real infinite loop over serial) even though it isn't one. Out of scope for this pass; same fix shape as PNG's remaining scratch-buffer gap likely applies (streaming reads instead of one big `f.read()`), not attempted here.
33. **[DONE, HIL-verified on Inkplate13SPECTRA and Inkplate6COLOR]** Wired `draw_bmp_from_sd/web`, `draw_jpg_from_sd/web`, `draw_png_from_sd/web` across all three SPI color boards to the new C pipeline (`inkplate.bmp_draw_palette`/`jpeg_draw_palette`/`png_draw_palette`, panel/palette/packing auto-resolved server-side from the already-`select_spi_panel()`-selected board — no new args needed on the Python side beyond the usual `fb`/`fb2`/rotation/x0/y0/invert/dither/kernel_type/bytes shape every other board's `_gs4` calls already use); deleted the old per-board viper `write_image`/`png_to_rgb565`/`bmp24_to_rgb565` (6COLOR, 13SPECTRA) and `rgb565_to_2bpp`/`draw_color_image_viper`/`decode_png_to_framebuffer`/`decode_bmp` (Inkplate2) once every call site was repointed — confirmed no other file (examples included, which all call the stable `draw_image()` entry point) referenced any of them. Two side-effect fixes worth flagging, not asked-for scope creep: (a) 6COLOR/13SPECTRA's JPEG path previously used a separate MicroPython built-in `jpeg` hardware-decoder module instead of this repo's own tjpgd-based `jpeg_decode.c` — moot on 6COLOR specifically, since step 30 already found that module isn't even present on this firmware build (`ImportError: no module named 'jpeg'`), so this was dead code, not a working feature being swapped out from under it (confirmed live via HIL this pass: `jpeg_draw_palette` decoded/drew real JPEGs on both boards with no such import involved); (b) Inkplate2's JPEG path previously hardcoded `x=0, y=0` in `draw_color_image_viper` regardless of the caller's real `x0`/`y0` args — the new path honors them like every other format already did (not HIL-verified on Inkplate2 itself, no board on hand this pass). 6COLOR's decode-time 180° pre-flip (`bmp24_to_rgb565`/`png_to_rgb565`'s manual flip, the old `jpeg.Decoder(rotation=180)`) doesn't exist in the new generic C decoders, so the equivalent flip was moved into `spi_panel_palette.c`'s `write_pixel_6color` (now bakes in `width-1-x, height-1-y`, matching that board's own `write_pixel`'s rotation==0 case) — HIL-confirmed correct orientation on real 6COLOR hardware (`coastal.bmp`/`.png`, `mountain.jpg` all displayed right-side-up, not mirrored/upside-down), on top of the byte-level unit test. **Verified (HIL)**: SD-card image loading on both Inkplate13SPECTRA and Inkplate6COLOR (jpg/png/bmp each), correct orientation and color, both boards' `select_spi_panel()`-driven palette/packing dispatch confirmed working from the same compiled binary. **Not yet HIL-verified**: web-based loading (`draw_*_from_web`, untested this pass — only SD paths were exercised) on any of the three boards, and anything at all on Inkplate2 (no board on hand this pass).

## Phase 11 — Remaining peripherals, touch

34. **[DONE, HIL-verified]** Finish Inkplate6FLICK Cypress touch placeholders (`tsGetXY`/`tsGetResolution`). User supplied the real Arduino `TouchCypress.cpp/.h`/`TouchCypressTypedefs.h` directly in-conversation. Comparing line-by-line against it surfaced more than the two named placeholders:
    - `ts_get_xy` (`touch_cypress.py`) had no reference equivalent and nothing called it — deleted, along with its now-dead `xraw`/`yraw`/`_x_pos`/`_y_pos` backing vars (user's call, confirmed unused first).
    - `ts_get_resolution` had no reference equivalent either (resolution is just `CYPRESS_TOUCH_MAX_X/Y`, 682/1023) and nothing called it — deleted, along with `_ts_x_resolution`/`_ts_y_resolution`.
    - `touch_in_area`'s rescale from a hardcoded "touch controller resolution 1535x560" to 1024x758 doesn't exist anywhere in the real driver — invented and wrong (`ts_get_data` already scales to screen size via `ts_scale`). Deleted.
    - `ts_get_data`'s per-finger loop used `range(max(fingers, 2))` instead of `range(fingers)` — reference loops exactly `fingers` times. Fixed.
    - `ts_get_touch_data` didn't clamp finger count; the real driver does (comment: hardware can report spurious high counts, e.g. palm-swipe). Without the clamp, a bogus `fingers=3+` reading would index past `touch_in_area`'s 2-slot arrays. Fixed: `fingers = min(regs[2], 2)`.
    - Real driver's `getData()` applies a per-rotation coordinate remap (swap/flip for rotation 1/2/3) that had zero Python equivalent — `Touch.rotation` was a dead class var, never read. Ported verbatim as `_arduino_map()` (replicates Arduino `map()`'s C-integer truncate-toward-zero division, which the rotation branches' inverted ranges depend on) plus the rotation 1/2/3 branches, now reading the live `Inkplate.rotation` via a new `Touch._inkplate` back-reference (mirrors the real driver's `Touch::begin(Inkplate*)`/`_inkplate` pointer).
    - **Bigger gap**: `touch_cypress.Touch` wasn't wired into `inkplate6_flick.py` at all — no import, no `ts_init`/`touch_in_area`/etc. on the `Inkplate` class, so `examples/inkplate6flick/touchscreen.py` would `AttributeError` on real hardware. Wired in: `Touch.init(i2c, PCAL6416A_1, self)` added to `begin()`; thin delegating methods (`ts_init`, `ts_shutdown`, `ts_available`, `ts_set_power_state`, `ts_get_power_state`, `ts_get_data`, `ts_get_raw_data`, `ts_handshake`, `touch_in_area`) added to `Inkplate`, mirroring the existing delegation style (`read_temperature`/`rtc_set_time` etc.).
    - **Known real bug in the reference itself, deliberately not ported**: `Touch::scale()` computes both the mapped X and Y into the same local var (`_mappedX` reused for Y — a typo) and never writes either result back into the touch data, so shipped hardware never actually rescales to screen pixels, only raw 0–682/0–1023 controller units post flip/swap. User's call: kept Python's existing bug-free `int(x * size / MAX)` scaling rather than matching this bug.
    - `ruff format`/`ruff check` clean on both touched files (37 pre-existing F403/F405 star-import warnings unrelated, present before this change too, confirmed via `git stash` diff).

    **Verified (HIL)** on real Inkplate6FLICK hardware, same session. Board's on-device file set was stale/incomplete independent of this change (`rtc.py`/`epd_power_pins.py` missing entirely, root-level `inkplate6_flick.py` an untracked intermediate snapshot matching no commit) — pushed current `inkplate6_flick.py`/`touch_cypress.py` plus the two missing shared files to device root (sys.path is `['', '.frozen', '/lib']`, root shadows `/lib`) before testing; no firmware rebuild needed (Python-only change). `begin()` → `ts_init(1)` returned `True` (full Cypress ping/bootloader-exit/sysinfo handshake succeeded for the first time via this wiring).

    **Two real bugs found live, not caught by code review, fixed and re-verified same session**:
    - First raw-coordinate poll showed X readings up to 1507 against a 1024-wide panel. Root cause: `ts_scale`'s `swap_xy` swaps the raw X/Y values, but the final "scale to screen size" lines still divided by the *original* channel's max constant — `x[i]` (now holding the raw Y reading, native range 0-1023) was divided by `CYPRESS_TOUCH_MAX_X` (682) instead of `CYPRESS_TOUCH_MAX_Y`, overscaling by ~1.5x; Y had the inverse problem (undershooting, dividing by 1023 instead of 682). This bug is latent in the real Arduino driver's own (never-executed, per the scale() typo noted above) intended math too — nobody could have caught it there since that code path never runs on shipped hardware. Fixed by swapping which constant divides which axis when `swap_xy` is set. Re-tested: X now bounded ~0-1023 (max observed 1014), Y still slightly overshot (up to 829 against a 757 max).
    - That remaining Y overshoot was real sensor overscan, not a math bug — edge touches read a few percent past the documented `CYPRESS_TOUCH_MAX_X`/`_Y` constants. Added a clamp to `[0, x_size]`/`[0, y_size]` on the final scaled output. Re-tested: all four physical corners (top-left/top-right/bottom-left/bottom-right) now clamp cleanly to (~0,~0)/(~1023,~0)/(~0,757)/(1023,757) matching where they were actually tapped.
    - Rotation 1 remap smoke-tested (set_rotation(1), live taps): no crash, output stayed within the rotated logical bounds (758x1024 swapped), though not independently cross-checked against an on-screen marker at that orientation.
    - Ran the real `examples/inkplate6flick/touchscreen.py` end-to-end (bounded to 30s via a host-side alarm, script itself is normally an infinite loop): initial mono draw, then 9 successful in-rectangle touch detections with partial-update redraws each time — the plan's literal verify criterion, satisfied on real hardware with the post-fix scaling.

    **Inkplate4TEMPERA touch, Elan controller [DONE]**: Inkplate4TEMPERA and Inkplate6PLUS/6PLUSV2 share the same touch controller/driver (`TouchElan.cpp/.h`, gated `#if defined(ARDUINO_INKPLATE6PLUS) || defined(ARDUINO_INKPLATE6PLUSV2) || defined(ARDUINO_INKPLATE4TEMPERA)`) — separate from Inkplate6FLICK's Cypress controller above. User supplied the real driver plus Inkplate4TEMPERA's own `pins.h` touch defines directly in-conversation; wired for **Inkplate4TEMPERA only** this pass (tested first, per user). Inkplate6PLUS/6PLUSV2 wired in a follow-up pass below once its own `pins.h` touch defines were supplied.
    - New `boards/inkplate4tempera/touch_elan.py`, mirroring `touch_cypress.py`'s structure (`Touch` classmethods, thin delegation from `Inkplate`). `TOUCHSCREEN_ADDR=0x15`, `EN=0`/`RST=1` on the *second* PCAL6416A expander (`0x21`, `IO_EXT_ADDR` — confirmed via the real pins.h's `TOUCHSCREEN_IO_EXPANDER`), `INT=36` direct ESP32 GPIO — all from this board's own pasted `pins.h`, not guessed.
    - **Two real bugs found in the vendor source while porting, not guessed, both fixed rather than ported literally** (same precedent as the Cypress `scale()` typo, step 34 above):
      - `Touch::power()` hardcodes `expander1` for EN/RST regardless of board, but on Inkplate4TEMPERA those same bit numbers (0/1) are expander1's `OE`/`GMOD` display-control pins (already wired, actively used by `epd_bitbang`). Ported as-is it would mean `ts_shutdown()` flips the panel's OE/GMOD instead of touch EN/RST. Fixed: `power()` routed onto expander2, matching `init()`/`tsHardwareReset()`'s own (correct) `#ifdef ARDUINO_INKPLATE4TEMPERA` branch.
      - `touchInArea()`'s `memcpy(touchX, x, 2)` copies 2 *bytes*, not 2 array elements — finger 2's cached X/Y never actually updates from a fresh read in the 100ms-hold path (a direct `ts_get_data()` call is unaffected). Fixed: both fingers copied.
    - `_ts_init_done` is never set `True` anywhere in the real driver either (so `end()`'s `detachInterrupt()`-equivalent is dead code there too) — ported literally, not fixed, since it's inert rather than hazardous.
    - Wired into `inkplate4tempera.py`: `Touch.init(i2c, _PCAL6416A_2, self)` in `begin()`; `ts_init`/`ts_shutdown`/`ts_available`/`ts_set_power_state`/`ts_get_power_state`/`ts_get_data`/`ts_get_raw_data`/`touch_in_area` delegation methods on `Inkplate`, same names as Inkplate6FLICK's. `touch_elan.py` added to `package.json`. New `examples/inkplate4tempera/touchscreen.py`, adapted from Inkplate6FLICK's (600x600 bounds instead of 1024x758).
    - `ruff format`/`ruff check` clean (only the repo's existing star-import F403/F405 noise, same as every other board file; confirmed no new categories via a pre-change baseline diff).

    **Verified (HIL)** on real Inkplate4TEMPERA hardware. `ts_init(1)` handshake (soft reset + hello-packet check) succeeded first try. INT line (GPIO36) confirmed electrically sound: quiet when idle, toggling in proportion to real touches when active -- ruled out a floating-pin/wiring problem before touching software again. `ts_x_resolution`/`ts_y_resolution` both queried as 1152 (dynamic, not hardcoded, matching the real `tsGetResolution()`'s own register read). Coordinate decode confirmed internally consistent (a sustained hold decoded to the same (x,y) across repeated reads).

    **One real bug found via HIL, not guessed, third one this pass**: with the vendor's `rotation==0` formula ported verbatim, ~140 real touches aimed at a known on-screen target box at true `(x,y) ∈ [0,80]×[0,80]` decoded with `x_pos` tracking true X correctly but `y_pos` landing consistently in `[510,593]` -- the opposite end of a 600-tall panel, i.e. `decoded_y == (E_INK_HEIGHT-1) - true_y` almost exactly, across every single reading, not noise. Root cause: this unit's `x_raw` axis (the touch chip's native reading that feeds `y_pos` in the `rotation==0` branch) increases toward the physical bottom, opposite of what the vendor's formula assumes -- a real per-unit mounting/polarity fact, not a porting mistake (the C formula was transcribed correctly). Fixed by inverting `x_raw` once, right after `ts_get_xy()`, before the rotation branch runs (`touch_elan.py`'s `ts_get_data`) -- keeps all four rotation branches internally consistent rather than patching just the `rotation==0` case. Re-tested post-fix: ~150 more real touches at the same target box now land tightly inside/near it (x mostly 17-95, y mostly 15-95 against a true 0-80 box).

    **Second real bug found via HIL, not guessed**: the single-corner test above only exercised the top-left corner, where true X and true Y happen to be numerically close (both near 0) -- not enough to catch an axis swap. A follow-up 4-corner test (true target boxes at each of the panel's four corners in turn) showed `x_pos`/`y_pos` landing in *each other's* true range specifically at the two off-diagonal corners: true `(x=560,y=40)` (top-right) decoded to roughly `(x=24-119, y=474-552)` -- i.e. `decoded_x == true_y` and `decoded_y == true_x`. Invisible at top-left/bottom-right (the `x==y` diagonal) since a swap looks identical to correct there, which is exactly why the first test passed clean. Fixed by swapping the two already-computed values right before they're written into the caller's `x_pos[i]`/`y_pos[i]` arrays, independent of which rotation branch computed them. Re-tested (HIL): all four corners now decode inside/near their true target box independently (top-left x:39-104/y:30-85, top-right x:495-543/y:26-84, bottom-left x:41-90/y:527-576, bottom-right x:503-580/y:523-579).

    Only `rotation==0` has been exercised on real hardware; rotations 1-3 use the same (now-corrected) `x_raw` flip and the same swap but haven't been individually HIL-confirmed.

    **Inkplate6PLUS/6PLUSV2 touch, same Elan controller [DONE, HIL-verified]**: user supplied this board's own `pins.h` touch defines (`TOUCHSCREEN_EN=12`, `RST=10`, `INT=36`, `ADDR=0x15`, `TOUCHSCREEN_IO_EXPANDER=IO_INT_ADDR`). Unlike Inkplate4TEMPERA, EN/RST sit on the *internal* expander (`0x20`) here, the same one used for `OE`/`GMOD`/`SPV` -- but at pins 12/10, no collision with that expander's other assignments (0/1/2/3/4/5/11/13), so `Touch::power()`'s vendor-literal `expander1` target is correct as written for this board, unlike the fix Inkplate4TEMPERA needed. `IO_INT_ADDR` (and therefore touch) is identical between the classic and V2 revisions -- only the unrelated `IO_EXT_ADDR` (0x21 vs 0x22, not used by touch) differs, so this port covers both, though only the V2 revision is otherwise wired in this repo.
    - `touch_elan.py` promoted from a per-board file to `shared/touch_elan.py`, parameterized (`Touch.init(i2c, pcal, inkplate, en_pin, rst_pin, width, height, int_pin_num=36, addr=0x15, x_raw_inverted=False, xy_swapped=False, xy_flipped=False)`) now that two boards use it. All three correction flags default `False` (vendor-literal); each board's own `Touch.init(...)` call passes only the ones its own HIL testing confirmed it needs -- Inkplate4TEMPERA passes `x_raw_inverted=True, xy_swapped=True`; Inkplate6PLUSV2 passes `xy_flipped=True` (added once this board's own four-corner test showed a different bug shape, see below) -- confirming the "don't assume, re-measure per board" caution below was warranted: this board needed neither of Inkplate4TEMPERA's two flags, just a new third one.
    - Wired into `inkplate6plusv2.py`: `Touch.init(i2c, _PCAL6416A_1, self, en_pin=12, rst_pin=10, width=D_COLS, height=D_ROWS)` in `begin()`; same `ts_init`/`ts_shutdown`/`ts_available`/`ts_set_power_state`/`ts_get_power_state`/`ts_get_data`/`ts_get_raw_data`/`touch_in_area` delegation methods as Inkplate4TEMPERA/Inkplate6FLICK. `touch_elan.py` added to `package.json` (pointing at the shared path). New `examples/inkplate6plusv2/touchscreen.py`, adapted from Inkplate6FLICK's (same 1024x758 panel, same bounds).
    - `ruff format`/`ruff check` clean; confirmed no new lint categories vs. a pre-change baseline diff of `inkplate6plusv2.py`.
    **Verified (HIL)** on real Inkplate6PLUSV2 hardware. `ts_init(1)` handshake succeeded first try (same protocol, different pins). A single-corner test (top-left) decoded consistently near the *opposite* corner (x≈978-1011 of 1023 max, y≈639-721 of 757 max) — same "diagonal corner can't distinguish flip from flip+swap" trap as Inkplate4TEMPERA, so went straight to the four-corner test rather than trusting it. That test showed a clean, different bug shape than Inkplate4TEMPERA's: `decoded_x == (width-1) - true_x` and `decoded_y == (height-1) - true_y` at **every** corner, no axis swap — a straight double mirror. Added a third correction flag, `xy_flipped` (distinct from Inkplate4TEMPERA's `x_raw_inverted`/`xy_swapped` pair — confirms these really are independent per-board facts, not one bug wearing two faces), applied to the final `x_pos`/`y_pos` after the rotation branch, same pattern as the swap. Re-tested (HIL): all four corners now decode inside/near their true target box independently (top-left x:17-51/y:41-74, top-right x:927-958/y:16-71, bottom-left x:16-73/y:661-718, bottom-right x:935-953/y:632-719).

    Only `rotation==0` has been exercised on real hardware for this board too; rotations 1-3 haven't been individually HIL-confirmed.
35. Consolidate frontlight control into shared layer behind a `has_frontlight` config flag. **Verify (HIL)**: brightness ramp example works.

    **Also flagged for later, found during the Phase 8 step 27 TPS65186 pass**: Inkplate4TEMPERA reads battery via a real fuel-gauge IC (`INKPLATE_FUEL_GAUGE` sensor-select bit, `FG_GPOUT` pin — confirmed from this board's own pasted `pins.h`), not a resistor-divider+ADC like every other wired board (10/6/5v2/6FLICK/6PLUSV2). Unlike those boards' `read_battery()`, this can't be wired from `pins.h` alone — needs the fuel gauge's real I2C address and register map. **[NEEDS INPUT]** user to supply the fuel-gauge driver source/datasheet.

## Phase 12 — Test hardening (threaded throughout, not just at the end)

36. Promote the host-compiled unit tests from Phases 1/4/5/7/9/10 into a native test target (board-config, waveform LUT, framebuffer packing, image decode/dither, SPI config). **Verify**: tests catch a deliberately introduced regression.
37. Documented, repeatable manual HIL checklist per board (clear, mono text, grayscale ramp, photo, partial update, battery/temp read). Not automated, but standardized across releases.
38. Merge each parallel-bus board's `<board>_partial.py`/`<board>_gs.py`/`<board>_mono.py` into one file per board, now that Phase 8 has reduced all of them to thin C-binding wrappers (same shape as Inkplate10's post-step-21 versions) — fewer files to keep in sync when iterating (`:lib/` resync friction observed firsthand during Phase 7 step 21 HIL). **Verify (HIL)**: each board's examples still render correctly post-merge.
39. Reorganize `firmware/usermods/inkplate/`'s flat layout (31 files and growing) into subfolders before it gets worse — `image/` for the bmp/jpeg/png decode+draw pairs, pngle, and dither.c; `display/` for epd_bitbang, epd_i2s, epd_partial_lut, waveform, board_config, expander_bridge, gfx; `inkplatemodule.c`/`micropython.cmake` stay at root as the module entry point. Update `micropython.cmake`'s source list, `ci.yml`'s per-file gcc invocations, and every `#include` path accordingly. **Verify**: CI build/test/format jobs stay green; HIL smoke-test one board post-move to confirm nothing broke.
40. Audit every board's `package.json` install manifest for drift — `shared/` dedup (step 27) and the per-board file merge (step 38) both change which files each board actually needs; step 17 already updated Inkplate10's manifest when `gfx.py`/`shapes.py` dropped out of its dependency list, but the other 5 boards' manifests won't have been touched until their own ports land. **Verify (HIL)**: fresh `mip install` of each board pulls exactly the files it needs, nothing stale, nothing missing.
41. Unify `write_pixel`/`draw_bitmap` onto the C drawing path — deferred explicitly in Phase 7 step 17 (`gfx_set_pixel` is a second, C-side reimplementation of the same per-pixel logic `write_pixel_viper` already does, kept separate at the time rather than risk a regression mid-port). Revisit now that the rest of the drawing/image pipeline is fully C-side: replace `write_pixel_viper` with a thin call into `gfx_set_pixel`, and give `draw_bitmap` a real C bitmap-blit primitive (source bitmap + w/h + dest x/y/color, looping entirely in C) instead of its current per-set-bit `write_pixel` Python loop. **Verify (HIL)**: bitmap/pixel-level examples render identically; timing improves for `draw_bitmap` the same way `write_fill_rect` did in step 17.
42. Make `ci.yml`'s build/test jobs self-discovering instead of hand-listed — every new host-testable `.c` file currently needs a manually-added `gcc -c ...` line (build job) and matching test-run block (test job), and forgetting one has caused two real bugs already (steps 11 and 14 both forgot to wire a new file into `micropython.cmake`/the test job, only caught later). Replace the hardcoded per-file lines with something that globs `firmware/usermods/inkplate/**/*.c` for the build job and pairs each `tests/test_*.c` with its same-named source for the test job. **Verify**: CI still catches a deliberately broken/omitted file the same as before; adding a new source file requires no `ci.yml` edit.
43. Prune the `display_image_sd_{jpeg,png,bmp}_c.py` examples now that they're redundant with the high-level API — they were written as placeholders demonstrating the raw `jpeg_draw_gs4`/`png_draw_gs4`/`bmp_draw_gs4` bindings directly "ahead of step 21," but `draw_jpg_from_sd`/`draw_png_from_sd`/`draw_bmp_from_sd` now call those same bindings, making the low-level examples mostly duplicate demonstrations. Keep one example per format (jpeg/png/bmp stay as three separate files, not merged into one combined example — each format has its own sample-file setup/caveats worth keeping visible), just updated to show the real `draw_*_from_sd` high-level call instead of the raw binding. **Verify (HIL)**: all three updated examples still render correctly.
44. Clean up code comments across `firmware/usermods/inkplate/*.c` and the board `.py` files — this refactor's own working style has been to narrate real-bug-found-on-real-hardware stories inline as they happened (useful mid-refactor, since each step had to re-derive context fresh), but a lot of that is commit-message/plan-doc material, not something a future reader needs sitting in the source permanently. Pass to trim comments down to WHY-only (hidden constraints, subtle invariants, non-obvious workarounds), cutting narrative history, "step N found X" callouts, and anything a well-named identifier already says. **Verify**: `git diff` is comment-only (no logic changes); CI still green. Same trim for `docs/refactor_plan.md` itself — each step's own "**Verified**" writeup has grown into a full HIL narrative (step 21's is a wall of text) that made sense to capture in the moment but bloats the doc once the work it describes is long done; split into a lean plan (what/why/verify-criteria per step, kept) plus an archived log (the blow-by-blow "here's what actually happened" detail, moved out) once this phase's steps are all done.
45. Update `README.md` (currently stale — doesn't list Inkplate5v2/6FLICK/13SPECTRA) once the new architecture stabilizes.

---

## Critical files (starting points)

- `boards/inkplate10/inkplate10.py` — canonical reference: pin/mask constants, `vscan_*`, `byte2gpio`, `fill_screen`, `Inkplate` API, image loaders. Source of truth for the C interface contract and board-config struct.
- `boards/inkplate10/inkplate_gs.py` — 2-bit waveform/LUT generation (`_gen_wave`/`genlut`) — primary target for the N-bit engine and 2→3-bit switch.
- `boards/inkplate10/inkplate_mono.py` — 1bpp waveform + row shift-out — the walking-skeleton target (Phase 4).
- `boards/inkplate10/inkplate_partial.py` — partial-update diff/skip logic (Phase 6).
- `firmware/usermods/inkplate/gfx.c` — pixel-callback drawing primitives (C port landed Phase 7 step 17, extended to all 6 parallel-bus boards by step 27); `shared/gfx.py`/`shared/shapes.py` deleted (step 27) once nothing imported them anymore.
- `shared/pcal6416a.py` — shared I2C GPIO-expander driver, stays Python-side per Phase 1 decision.
- `shared/tps65186.py`, `shared/rtc.py` — shared PMIC/RTC drivers (step 27), stay Python-side, same reasoning as `pcal6416a.py`.
- Same 4-file pattern under `boards/inkplate6/`, `boards/inkplate5v2/`, `boards/inkplate6flick/` for Phase 8.
- `boards/inkplate6color/inkplate6_color.py`, `boards/inkplate2/inkplate2.py`, `boards/inkplate13spectra/inkplate13_spectra.py` — inline SPI command sequences (Phase 9); image decode/dithering (Phase 10) now thin wrappers over `inkplate.{bmp,jpeg,png}_draw_palette`.
- `firmware/usermods/inkplate/spi_panel_palette.{c,h}` — Phase 10's per-board palette tables + packing/rotation, kept separate from `dither.c`/`bmp_draw.c` etc. (which stay board-agnostic) and from `board_config.c`/`spi_panel_config.c` (pins/transport, not pixel format).
- `boards/inkplate6flick/touch_cypress.py` — placeholder touch logic (Phase 11).

## Verification approach summary

Every task falls into one of three checks: (a) build/CI check for scaffolding, (b) host-compiled C unit test for pure logic (LUT generation, config struct, framebuffer packing) — runnable without hardware, (c) hardware-in-the-loop check on real Inkplate hardware comparing against the Phase 0 baseline. No task is marked done on "should work" — timing and visual output get compared against baseline photos/logs at each hardware checkpoint.

## Old vs new, measured head-to-head (Inkplate10, real hardware)

Every number below was measured back-to-back on the same physical Inkplate10, same SD card, same 3 sample files (`mountain.jpg`, `coastal.png`, `coastal.bmp`), in one session — not pulled from the per-step HIL notes above, which were taken at different points in the refactor and aren't always directly comparable to each other.

**Method**: the pre-refactor code is pure Python + `@micropython.viper` — it never calls the `inkplate` C module, so it doesn't need a separate firmware build. "Old" and "new" runs were done by swapping which `.py` files sit in the device's `:lib/` (old files pulled from `git show 447426f:...`, the commit `refactor` forked from) on top of the *same already-flashed* current C firmware, running a timed script, then swapping back. No reflashing either direction.

One pre-existing bug in the old code had to be worked around to get any numbers at all: `initSDCard` hardcoded `freq=80000000`, which doesn't mount on this hardware/card (same failure this refactor already root-caused and fixed in Phase 7 step 18) — patched to `freq=4000000` (matching the current fixed value) so the SD clock isn't a second variable on top of decode/draw. Everything else is the old code unmodified.

| Operation | Old (pure Python) | New (C path) | Delta | Notes |
|---|---|---|---|---|
| Mono full display (clear→display) | 2295 ms (self-reported: 1332 clean + 402 draw = 1734) | 1413 ms (self-reported: 923 clean + 177 draw = 1100) | ~1.6x faster | wrapper time includes trailing clean/power-off the self-reported figure doesn't |
| Mono full display (after drawing shapes) | 2071 ms | 1638 ms | ~1.3x faster | second `display()` call, same shapes as `basic_bw.py` rotation 0 |
| Mono partial update (1 changed region) | 678 ms (self-reported draw-only: 418 ms, 101us/row) | 671 ms (self-reported draw-only: 211 ms) | draw-only ~2x faster; wall time ~same | wall time dominated by a fixed `einkOn`/`einkOff` power-sequencing cost common to both — the actual shift-out is 2x faster, it's just not the bottleneck at 1-frame scale |
| Full-screen `fill_rect` (824x825, single call) | 15417 ms | 238 ms | **~65x faster** | old path is a per-pixel `writePixel` Python loop (~680k calls); new path is one C call — biggest single number in this table |
| Grayscale full display (bar pattern) | 2451 ms (self-reported: 1173 clean + 1073 draw = 2246), **4 gray levels only** | 1792 ms (self-reported: 810 clean + 783 draw = 1593), **8 gray levels** | ~1.4x faster, *and* 2x the gray levels | old code never had a 3-bit path — this is a quality upgrade bundled with the speedup |
| JPEG load+draw, no dither (`mountain.jpg`) | **dead code** — `import jpeg` raises `ImportError: no module named 'jpeg'` | 4257 ms draw + 2022 ms display | new capability, not a speedup | old `drawJPGFromSd` never worked on this firmware; pre-existing, unrelated to this refactor |
| JPEG load+draw, dithered | dead code (same) | 6022 ms draw + 2025 ms display | new capability | — |
| PNG load+draw, no dither (`coastal.png`, 947KB) | **fails**: `MemoryError: memory allocation failed, allocating 947302 bytes` | 17413 ms draw + 1791 ms display | new capability, not a speedup | old `drawPNGFromSd` does one whole-file `f.read()`; pre-existing heap-fragmentation bug, unrelated to this refactor |
| PNG load+draw, dithered | fails (same) | 18330 ms draw + 1791 ms display | new capability | — |
| BMP load+draw, no dither (`coastal.bmp`, 1.44MB, 24-bit) | 15144 ms draw + 2458 ms display | 7480 ms draw + 1792 ms display | ~2x faster draw, ~1.4x faster display | old code is 24-bit-only; new code also decodes indexed (1/4/8-bit) and 16-bit BMP, old can't open those at all |
| BMP load+draw, dithered | 19131 ms draw + 2455 ms display | 8216 ms draw + 1792 ms display | ~2.3x faster draw | — |

Takeaways: the shape/fill-rect path is where C wins biggest (per-pixel Python loop vs one C call — 65x). Whole-frame display/clean timing improves more modestly (~1.3–1.6x) since a chunk of that time is fixed panel power-sequencing, not compute. Image loading isn't just faster — JPEG and PNG (this file size) didn't work pre-refactor at all; those are new capabilities, and the BMP/grayscale comparisons come with a format/quality upgrade bundled in (indexed+16-bit BMP, 8 vs 4 gray levels), not just a raw speed delta.
